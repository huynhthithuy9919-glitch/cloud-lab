import type { Subject, Extensions, FPDeviceIDCSRPayload } from "../types/device-id-payload.js";
import type { SuperThis } from "../types/sthis.js";
import { Result } from "@adviser/cement";
import { DeviceIdKey } from "./key.js";
export declare class DeviceIdCSR {
    #private;
    constructor(sthis: SuperThis, key: DeviceIdKey);
    createCSRPayload(subject: Subject, extensions?: Partial<Extensions>): Promise<FPDeviceIDCSRPayload>;
    signCSR(payload: FPDeviceIDCSRPayload): Promise<Result<string>>;
    createCSR(subject: Subject, extensions?: Partial<Extensions>): Promise<Result<string, Error>>;
}
