import type { SysFileSystem } from "../../types/sthis.js";
type FsPromises = typeof import("node:fs/promises");
export declare class NodeFileSystem implements SysFileSystem {
    fs?: FsPromises;
    start(): Promise<SysFileSystem>;
    mkdir(path: string, options?: {
        recursive: boolean;
    }): Promise<string | undefined>;
    readdir(path: string, options?: unknown): Promise<string[]>;
    rm(path: string, options?: {
        recursive: boolean;
    }): Promise<void>;
    copyFile(source: string, destination: string): Promise<void>;
    readfile(path: string, options?: {
        encoding: BufferEncoding;
        flag?: string;
    }): Promise<Uint8Array>;
    stat(path: string): Promise<import("fs").Stats>;
    unlink(path: string): Promise<void>;
    writefile(path: string, data: Uint8Array | string): Promise<void>;
}
export {};
