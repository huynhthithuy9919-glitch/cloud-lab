export const INTERNAL_MEDIA_DB_ACCESS = Symbol("vibes.diy.internal.mediaDbAccess");
let vibeApiRef;
export async function registerFirefly(api) {
    vibeApiRef = api;
}
export function isFireflyRegistered() {
    return vibeApiRef !== undefined;
}
export function registeredAppKey() {
    const app = vibeApiRef?.svc.vibeApp;
    return app === undefined ? undefined : `${app.ownerHandle}/${app.appSlug}`;
}
export async function listDbNames() {
    if (!vibeApiRef) {
        throw new Error("Firefly not initialized — registerFirefly() must be called before listDbNames()");
    }
    const rRes = await vibeApiRef.listDbNames();
    if (rRes.isErr()) {
        throw new Error(`Failed to list db names: ${rRes.Err()}`);
    }
    return rRes.Ok().dbNames;
}
//# sourceMappingURL=use-firefly.js.map