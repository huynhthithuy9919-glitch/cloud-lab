import { uploadFiles } from "./firefly-files-write.js";
import { getRegisteredVibeApi } from "./register-dependencies.js";
export async function uploadDocFiles(doc) {
    const api = getRegisteredVibeApi();
    if (api === undefined)
        return doc;
    return uploadFiles(doc, api);
}
//# sourceMappingURL=upload-doc-files.js.map