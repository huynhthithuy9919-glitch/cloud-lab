import React, { Component, Fragment, useEffect, useLayoutEffect, } from "react";
import { VibeContextProvider } from "./VibeContext.js";
import { noteHotSwapPainted, onUncaughtVibeError } from "./calm-errors.js";
const useIsomorphicLayoutEffect = typeof window !== "undefined" ? useLayoutEffect : useEffect;
function HotSwapCommitConfirmation({ attempt, rollback, }) {
    useIsomorphicLayoutEffect(() => {
        if (attempt !== undefined)
            noteHotSwapPainted(attempt);
        else
            rollback?.onCommit();
    }, [attempt, rollback]);
    return null;
}
class HotSwapErrorBoundary extends Component {
    state = { hasError: false, attempt: undefined, rollback: undefined };
    static getDerivedStateFromProps(props, state) {
        if (props.attempt === state.attempt && props.rollback === state.rollback)
            return null;
        return { hasError: false, attempt: props.attempt, rollback: props.rollback };
    }
    static getDerivedStateFromError() {
        return { hasError: true };
    }
    componentDidCatch(error, errorInfo) {
        if (this.props.rollback !== undefined) {
            this.props.rollback.onError(error, errorInfo);
            return;
        }
        if (this.props.attempt !== undefined)
            onUncaughtVibeError(error, errorInfo, this.props.attempt);
        else
            onUncaughtVibeError(error, errorInfo);
    }
    render() {
        if (this.state.hasError)
            return null;
        return this.props.children;
    }
}
export function buildVibeTree(comps, props, hotSwapAttempt, hotSwapRollback) {
    const appChildren = React.createElement(Fragment, null, ...comps.map((Comp, index) => React.createElement(Comp, { key: index })), React.createElement(HotSwapCommitConfirmation, { attempt: hotSwapAttempt, rollback: hotSwapRollback }));
    const vibeElement = React.createElement(HotSwapErrorBoundary, { attempt: hotSwapAttempt, rollback: hotSwapRollback }, appChildren);
    return React.createElement(VibeContextProvider, {
        mountParams: { ...props },
        children: vibeElement,
    });
}
//# sourceMappingURL=vibe-tree.js.map