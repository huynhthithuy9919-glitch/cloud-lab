import type { DocTypes, DocWithId } from "./firefly-database.js";
export declare class EphemeralOverlay {
    private readonly overlay;
    private readonly peerDocs;
    private seq;
    private cancelSweep;
    private readonly ttlMs;
    private readonly onChange;
    private readonly now;
    private readonly schedule;
    constructor(opts: {
        ttlMs?: number;
        onChange: (docIds: string[]) => void;
        now?: () => number;
        schedule?: (fn: () => void, ms: number) => () => void;
    });
    get size(): number;
    apply(docId: string, doc: Record<string, unknown>, originPeer: string): void;
    dropPeer(originPeer: string): void;
    merge<T extends DocTypes>(docs: DocWithId<T>[]): DocWithId<T>[];
    private prune;
    private ensureSweep;
    stop(): void;
}
