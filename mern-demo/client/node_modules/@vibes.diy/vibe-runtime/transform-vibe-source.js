import { transform } from "sucrase";
export function transformVibeSource(src) {
    const { code } = transform(src, {
        transforms: ["jsx", "typescript"],
        production: true,
        jsxRuntime: "automatic",
    });
    return { module: code };
}
//# sourceMappingURL=transform-vibe-source.js.map