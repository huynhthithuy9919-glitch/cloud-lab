import { type } from "arktype";
import { viewerPayload, docAccessLevel, socialEdgeView, viewerCapabilities } from "@vibes.diy/vibe-types";
export const vibeEnv = type("Record<string, string>");
export const viewerEnv = type({
    viewer: viewerPayload.or("null"),
    access: docAccessLevel,
    "isOwner?": "boolean",
    "grants?": type({ "[string]": type({ channels: "string[]", publicChannels: "string[]", roles: "string[]" }) }),
    "capabilities?": viewerCapabilities,
    "adminMode?": "boolean",
    "pending?": "boolean",
    "lapsed?": "boolean",
});
export function isViewerEnvPending(env) {
    return env === undefined || env.pending === true;
}
export const socialEnv = type({
    handle: "string",
    private: "boolean",
    following: socialEdgeView.array(),
    followers: socialEdgeView.array(),
    requests: socialEdgeView.array(),
    followersEnabled: "boolean",
    visibility: "'public' | 'followers' | 'private'",
    "stale?": "boolean",
});
export const vibeMountParams = type({
    usrEnv: vibeEnv,
    "viewerEnv?": viewerEnv,
    "socialEnv?": socialEnv,
    "accessFnBindings?": type({ dbName: "string", accessFnCid: "string" }).array(),
    "seedDocs?": { "[string]": "object[]" },
});
//# sourceMappingURL=vibe.js.map