import { exception2Result } from "@adviser/cement";
const PROBE_KEY = "firefly:local-storage-probe";
export function safeLocalStorage() {
    const probed = exception2Result(() => {
        const s = globalThis.localStorage;
        if (s === undefined)
            return undefined;
        s.setItem(PROBE_KEY, "1");
        s.removeItem(PROBE_KEY);
        return s;
    });
    return probed.isErr() ? undefined : probed.Ok();
}
//# sourceMappingURL=safe-local-storage.js.map