export function mediaDbName(hostDbName) {
    return `media:${hostDbName}`;
}
export function mediaVersionDocId(hostDocId, promptKey, seq) {
    return `${hostDocId}:version:${promptKey}:${seq}`;
}
export function mediaPointerDocId(hostDocId, authorHandle) {
    return `${hostDocId}:pointer:${authorHandle}`;
}
export function buildMediaVersionDoc(input) {
    return {
        type: "version",
        hostDocId: input.hostDocId,
        promptKey: input.promptKey,
        seq: input.seq,
        authorHandle: input.authorHandle,
        ...(input.prompt !== undefined ? { prompt: input.prompt } : {}),
        ...(input.model !== undefined ? { model: input.model } : {}),
        ...(input.sourceKey !== undefined ? { sourceKey: input.sourceKey } : {}),
        _files: input.files,
    };
}
export function buildMediaPointerDoc(input) {
    return {
        type: "pointer",
        hostDocId: input.hostDocId,
        promptKey: input.promptKey,
        authorHandle: input.authorHandle,
        currentSeq: input.currentSeq,
    };
}
//# sourceMappingURL=media-doc.js.map