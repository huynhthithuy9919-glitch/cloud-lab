export const ANON_CALLER = { access: "none", authenticated: false };
export const canRead = (level) => level === "override" || level === "editor" || level === "viewer";
export const canWrite = (level) => level === "override" || level === "editor" || level === "submitter";
export function inGroup(caller, group) {
    if (caller.access === "override")
        return true;
    if (group === "anyone")
        return true;
    if (caller.blocked === true)
        return false;
    const level = caller.access;
    switch (group) {
        case "signed-in":
            return caller.authenticated;
        case "members":
            return level === "editor" || level === "viewer" || level === "submitter";
        case "editors":
            return level === "editor";
        case "submitters":
            return level === "submitter";
        case "readers":
            return level === "editor" || level === "viewer";
    }
}
export function aclAllows(acl, cap, caller) {
    const subjects = acl?.[cap];
    if (subjects === undefined) {
        return cap === "read" ? canRead(caller.access) : canWrite(caller.access);
    }
    return subjects.some((g) => inGroup(caller, g));
}
const SUBJECT_MEMBERS = Object.freeze({
    anyone: ["anonymous", "signed-in", "viewer", "editor", "submitter"],
    "signed-in": ["signed-in", "viewer", "editor", "submitter"],
    members: ["viewer", "editor", "submitter"],
    readers: ["viewer", "editor"],
    editors: ["editor"],
    submitters: ["submitter"],
});
const ALL_SUBJECTS = Object.keys(SUBJECT_MEMBERS);
export function subjectSubsetOf(a, b) {
    return SUBJECT_MEMBERS[a].every((c) => SUBJECT_MEMBERS[b].includes(c));
}
export function intersectSubjects(rows) {
    const admitted = ALL_SUBJECTS.filter((s) => rows.every((row) => row.some((g) => subjectSubsetOf(s, g))));
    return admitted.filter((s) => !admitted.some((o) => o !== s && subjectSubsetOf(s, o)));
}
//# sourceMappingURL=db-acl-eval.js.map