import { type } from "arktype";
export const PaletteHueControl = type({ mode: "'derived'" }).or({ mode: "'pinned'", h: "number" });
export const PaletteCharacter = type({
    chroma: "number",
    contrast: "number",
    neutralTint: "number",
    surfaceLift: "number",
    fillLevel: "number",
    scheme: "'analogous' | 'split-complementary' | 'triadic' | 'complementary'",
});
export const PaletteSeed = type({
    hues: type(["number", PaletteHueControl.optional(), PaletteHueControl.optional()]),
    character: PaletteCharacter,
});
export function isPaletteSeed(obj) {
    return !(PaletteSeed(obj) instanceof type.errors);
}
export const StyleOrnamentPins = type({
    "spacing?": "string",
    "font-size-base?": "string",
    "radius?": "string",
    "radius-sm?": "string",
    "radius-lg?": "string",
    "border-width?": "string",
    "shadow?": "string",
    "shadow-sm?": "string",
    "shadow-lg?": "string",
    "letter-spacing?": "string",
    "letter-spacing-wide?": "string",
    "font-weight?": "string",
    "text-transform?": "string",
});
export const StyleOrnament = type({
    pad: type({ x: "number", y: "number" }),
    "pins?": StyleOrnamentPins,
});
export const Style = type({
    "palette?": PaletteSeed,
    "font?": "string",
    "ornament?": StyleOrnament,
});
export const ActiveStyle = type({
    type: "'active.style'",
    "palette?": PaletteSeed,
    "font?": "string",
    "ornament?": StyleOrnament,
});
export function isActiveStyle(obj) {
    return !(ActiveStyle(obj) instanceof type.errors);
}
export const DraftStyle = type({
    type: "'draft.style'",
    "palette?": PaletteSeed,
    "font?": "string",
    "ornament?": StyleOrnament,
});
export function isDraftStyle(obj) {
    return !(DraftStyle(obj) instanceof type.errors);
}
//# sourceMappingURL=style.js.map