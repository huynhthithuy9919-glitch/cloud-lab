export const TRACKING_QUERY_PARAMS = new Set([
    "fbclid",
    "gclid",
    "gbraid",
    "wbraid",
    "msclkid",
    "ttclid",
    "twclid",
    "li_fat_id",
    "mc_eid",
    "igshid",
    "dclid",
]);
export const TRACKING_QUERY_PARAM_PREFIXES = ["utm_"];
export function isTrackingParam(name) {
    if (TRACKING_QUERY_PARAMS.has(name))
        return true;
    return TRACKING_QUERY_PARAM_PREFIXES.some((p) => name.startsWith(p));
}
export function stripTrackingParams(params) {
    return Object.fromEntries(Object.entries(params).filter(([k]) => !isTrackingParam(k)));
}
//# sourceMappingURL=tracking-params.js.map