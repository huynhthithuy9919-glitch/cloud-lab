import { type } from "arktype";
import { dashAuthType } from "./common.js";
export const DRAFT_OWNER_HANDLE = "~draft";
export function isDraftOwnerHandle(ownerHandle) {
    return ownerHandle === DRAFT_OWNER_HANDLE;
}
export function isReservedHandle(handle) {
    return handle.startsWith("~");
}
export const DraftChatState = type("'unowned' | 'turn-parked' | 'attached'");
export const ReqAttachDraftHandle = type({
    type: "'vibes.diy.req-attach-draft-handle'",
    auth: dashAuthType,
    chatId: "string",
    ownerHandle: "string",
});
export function isReqAttachDraftHandle(obj) {
    return !(ReqAttachDraftHandle(obj) instanceof type.errors);
}
export const ResAttachDraftHandle = type({
    type: "'vibes.diy.res-attach-draft-handle'",
    chatId: "string",
    ownerHandle: "string",
    appSlug: "string",
    persisted: "boolean",
});
export function isResAttachDraftHandle(obj) {
    return !(ResAttachDraftHandle(obj) instanceof type.errors);
}
export const DRAFT_NOT_FOUND_CODE = "draft-not-found";
export const HANDLE_TAKEN_CODE = "handle-taken";
//# sourceMappingURL=draft-chat.js.map