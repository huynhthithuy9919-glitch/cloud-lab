import { type } from "arktype";
export const evtRetentionStep = type({
    type: "'vibes.diy.evt-retention-step'",
    userId: "string",
    step: "string",
    "preview?": {
        requestedBy: "string",
    },
});
export function isEvtRetentionStep(obj) {
    return !(evtRetentionStep(obj) instanceof type.errors);
}
export const RETENTION_PREVIEW_DOMAIN = "@vibes.diy";
export function isRetentionPreviewRecipientAllowed(arg) {
    return arg.email.trim().toLowerCase().endsWith(RETENTION_PREVIEW_DOMAIN) || arg.targetUserId === arg.requestedBy;
}
//# sourceMappingURL=retention-msg.js.map