import { type } from "arktype";
import { dashAuthType } from "./common.js";
export const AudienceArmState = type("'public' | 'followers' | 'private'");
export const AudienceArmWriteState = type("'public' | 'followers' | 'private' | 'allow' | 'deny'");
export const AudiencePolicy = type("'public' | 'followers' | 'private' | 'ask' | 'always-allow' | 'never-allow'");
export const LegacyWireArmState = type("'allow' | 'deny'");
export const LegacyWireArmPolicy = type("'ask' | 'always-allow' | 'never-allow'");
export const AudiencePolicyWrite = AudiencePolicy;
export const ReqAudienceArmState = type({
    type: "'vibes.diy.req-audience-arm-state'",
    auth: dashAuthType,
    ownerHandle: "string",
    appSlug: "string",
    "actingHandle?": "string",
});
export function isReqAudienceArmState(obj) {
    return !(ReqAudienceArmState(obj) instanceof type.errors);
}
export const ResAudienceArmState = type({
    type: "'vibes.diy.res-audience-arm-state'",
    ownerHandle: "string",
    appSlug: "string",
    state: LegacyWireArmState.or("null"),
    policy: LegacyWireArmPolicy,
    visibility: AudienceArmState.or("null"),
    intent: AudienceArmState.or("null"),
    sensitive: "boolean",
});
export function isResAudienceArmState(obj) {
    return !(ResAudienceArmState(obj) instanceof type.errors);
}
export const ReqSetAudienceArm = type({
    type: "'vibes.diy.req-set-audience-arm'",
    auth: dashAuthType,
    ownerHandle: "string",
    appSlug: "string",
    state: AudienceArmWriteState.or("null"),
    "actingHandle?": "string",
});
export function isReqSetAudienceArm(obj) {
    return !(ReqSetAudienceArm(obj) instanceof type.errors);
}
export const ResSetAudienceArm = type({
    type: "'vibes.diy.res-set-audience-arm'",
    ownerHandle: "string",
    appSlug: "string",
    state: LegacyWireArmState.or("null"),
    visibility: AudienceArmState.or("null"),
});
export function isResSetAudienceArm(obj) {
    return !(ResSetAudienceArm(obj) instanceof type.errors);
}
export const ReqSetAudiencePolicy = type({
    type: "'vibes.diy.req-set-audience-policy'",
    auth: dashAuthType,
    policy: AudiencePolicyWrite,
    "actingHandle?": "string",
});
export function isReqSetAudiencePolicy(obj) {
    return !(ReqSetAudiencePolicy(obj) instanceof type.errors);
}
export const ResSetAudiencePolicy = type({
    type: "'vibes.diy.res-set-audience-policy'",
    policy: LegacyWireArmPolicy,
    intent: AudienceArmState.or("null"),
});
export function isResSetAudiencePolicy(obj) {
    return !(ResSetAudiencePolicy(obj) instanceof type.errors);
}
export const AudienceArmEntry = type({
    ownerHandle: "string",
    appSlug: "string",
    state: LegacyWireArmState,
    visibility: AudienceArmState,
});
export const ReqListAudienceArms = type({
    type: "'vibes.diy.req-list-audience-arms'",
    auth: dashAuthType,
    "actingHandle?": "string",
});
export function isReqListAudienceArms(obj) {
    return !(ReqListAudienceArms(obj) instanceof type.errors);
}
export const ResListAudienceArms = type({
    type: "'vibes.diy.res-list-audience-arms'",
    policy: LegacyWireArmPolicy,
    intent: AudienceArmState.or("null"),
    arms: AudienceArmEntry.array(),
});
export function isResListAudienceArms(obj) {
    return !(ResListAudienceArms(obj) instanceof type.errors);
}
export function normalizeArmState(state) {
    if (state === "allow")
        return "followers";
    if (state === "deny")
        return "private";
    return state;
}
export function normalizeHandleIntent(policy) {
    if (policy === "public" || policy === "followers" || policy === "private")
        return policy;
    if (policy === "never-allow")
        return "private";
    return undefined;
}
export function handleIntentForWire(policy) {
    return normalizeHandleIntent(policy) ?? null;
}
export function armStateToLegacyWire(state) {
    if (state === null)
        return null;
    return state === "private" ? "deny" : "allow";
}
export function handleIntentToLegacyWire(policy) {
    const wire = handleIntentForWire(policy);
    if (wire === "private")
        return "never-allow";
    if (wire === "followers" || wire === "public")
        return "always-allow";
    return "ask";
}
export function audienceVisibilityArgs(res) {
    return { state: res.visibility, sensitive: res.sensitive, policy: res.intent };
}
export function resolvePromptOutcome({ policy, sensitive, prior }) {
    if (prior === "public" || prior === "followers" || prior === "allow")
        return "auto-allow";
    if (prior === "private" || prior === "deny")
        return "silent-deny";
    if (policy === "private" || policy === "never-allow")
        return "silent-deny";
    if (!sensitive && (policy === "public" || policy === "followers" || policy === "always-allow"))
        return "auto-allow";
    return "prompt";
}
export function effectiveAudienceVisibility({ state, sensitive, policy }) {
    if (state !== null) {
        const normalized = normalizeArmState(state);
        return normalized === "public" && sensitive ? "followers" : normalized;
    }
    if (sensitive)
        return "private";
    const intent = normalizeHandleIntent(policy);
    if (intent !== undefined)
        return intent;
    return "followers";
}
export function effectiveFollowersEnabled(args) {
    return effectiveAudienceVisibility(args) !== "private";
}
export const AUDIENCE_VISIBILITY_RANK = { private: 0, followers: 1, public: 2 };
export function isDownwardAudienceTransition(before, after) {
    return AUDIENCE_VISIBILITY_RANK[after] < AUDIENCE_VISIBILITY_RANK[before];
}
//# sourceMappingURL=audience-arms-msg.js.map