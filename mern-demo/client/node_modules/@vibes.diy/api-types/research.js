import { exception2Result } from "@adviser/cement";
import { type } from "arktype";
import { CoercedDate, utf8ByteLength } from "@vibes.diy/call-ai-v2";
export const RESEARCH_BLOCK_SEQ = -1;
export const RESEARCH_MAX_SOURCES = 3;
export const RESEARCH_MAX_EXCERPTS = 8;
export const RESEARCH_MAX_API_ENDPOINTS = 8;
export const RESEARCH_MAX_SEED_ROWS = 24;
export const RESEARCH_MAX_BRIEF_CHARS = 4_000;
export const RESEARCH_MAX_SOURCE_URL_CHARS = 2_048;
export const RESEARCH_MAX_CONTENT_TYPE_CHARS = 256;
export const RESEARCH_MAX_EXCERPT_CHARS = 400;
export const RESEARCH_MAX_LOCATOR_CHARS = 200;
export const RESEARCH_MAX_METHOD_CHARS = 32;
export const RESEARCH_MAX_PATH_CHARS = 512;
export const RESEARCH_MAX_DESCRIPTION_CHARS = 400;
export const RESEARCH_MAX_FIELD_CHARS = 128;
export const RESEARCH_MAX_FIELDS = 16;
export const RESEARCH_MAX_SEED_NAME_CHARS = 128;
export const RESEARCH_MAX_ITEM_BYTES = 8 * 1024;
export const RESEARCH_MAX_SEED_BYTES = 64 * 1024;
export const RESEARCH_MAX_EVENT_BYTES = 48 * 1024;
export const RESEARCH_JOB_MAX_TEXT_CHARS = 12_000;
export const RESEARCH_JOB_MAX_ID_CHARS = 256;
export const RESEARCH_JOB_MAX_MODEL_CHARS = 256;
export const RESEARCH_JOB_MAX_HANDLE_CHARS = 256;
const RESEARCH_MAX_ID_CHARS = 256;
const RESEARCH_MAX_COMPLETED_AT_CHARS = 64;
const RESEARCH_MAX_ERROR_CODE_CHARS = 64;
const RESEARCH_ERROR_CODE_RE = /^[a-z0-9][a-z0-9_.:-]*$/;
const RESEARCH_MAX_SEED_DEPTH = 5;
const RESEARCH_SEED_NAME_RE = /^[A-Za-z0-9][A-Za-z0-9._-]*$/;
export function isValidIsoTimestamp(value) {
    if (typeof value !== "string")
        return false;
    const parsed = new Date(value);
    return Number.isFinite(parsed.getTime()) && parsed.toISOString() === value;
}
function isPlainObject(value) {
    return typeof value === "object" && value !== null && Array.isArray(value) === false;
}
function isBoundedJsonValue(value, depth) {
    if (depth > RESEARCH_MAX_SEED_DEPTH)
        return false;
    if (value === null || typeof value === "string" || typeof value === "boolean")
        return true;
    if (typeof value === "number")
        return Number.isFinite(value);
    if (Array.isArray(value))
        return value.every((item) => isBoundedJsonValue(item, depth + 1));
    if (isPlainObject(value))
        return Object.values(value).every((item) => isBoundedJsonValue(item, depth + 1));
    return false;
}
function jsonByteLength(value) {
    const serialized = exception2Result(() => JSON.stringify(value));
    if (serialized.isErr())
        return undefined;
    const json = serialized.Ok();
    return json === undefined ? undefined : utf8ByteLength(json);
}
function isSafeErrorCode(value) {
    return (typeof value === "string" &&
        value.length > 0 &&
        value.length <= RESEARCH_MAX_ERROR_CODE_CHARS &&
        RESEARCH_ERROR_CODE_RE.test(value));
}
const ResearchSource = type({
    url: "string",
    finalUrl: "string | null",
    contentType: "string | null",
    httpStatus: "number | null",
    backendFetchable: "boolean",
    fetched: "boolean",
    "errorCode?": "string",
    "+": "reject",
}).narrow((value, ctx) => {
    if (value.url.length === 0 || value.url.length > RESEARCH_MAX_SOURCE_URL_CHARS) {
        return ctx.mustBe("a bounded source URL");
    }
    if (value.finalUrl !== null && value.finalUrl.length > RESEARCH_MAX_SOURCE_URL_CHARS) {
        return ctx.mustBe("a bounded final URL");
    }
    if (value.contentType !== null && value.contentType.length > RESEARCH_MAX_CONTENT_TYPE_CHARS) {
        return ctx.mustBe("a bounded content type");
    }
    if (value.httpStatus !== null &&
        (Number.isInteger(value.httpStatus) === false || value.httpStatus < 100 || value.httpStatus > 599)) {
        return ctx.mustBe("a valid HTTP status");
    }
    if (value.errorCode !== undefined && isSafeErrorCode(value.errorCode) === false) {
        return ctx.mustBe("a safe error code");
    }
    return true;
});
const ResearchApiEndpoint = type({
    method: "string",
    path: "string",
    description: "string",
    fields: "string[]",
    "+": "reject",
}).narrow((value, ctx) => {
    if (value.method.length === 0 || value.method.length > RESEARCH_MAX_METHOD_CHARS) {
        return ctx.mustBe("a bounded method");
    }
    if (value.path.length === 0 || value.path.length > RESEARCH_MAX_PATH_CHARS) {
        return ctx.mustBe("a bounded API path");
    }
    if (value.description.length > RESEARCH_MAX_DESCRIPTION_CHARS) {
        return ctx.mustBe("a bounded API description");
    }
    if (value.fields.length > RESEARCH_MAX_FIELDS || value.fields.some((field) => field.length > RESEARCH_MAX_FIELD_CHARS)) {
        return ctx.mustBe("bounded API fields");
    }
    return true;
});
const ResearchExcerpt = type({
    sourceUrl: "string",
    text: "string",
    "locator?": "string",
    "+": "reject",
}).narrow((value, ctx) => {
    if (value.sourceUrl.length === 0 || value.sourceUrl.length > RESEARCH_MAX_SOURCE_URL_CHARS) {
        return ctx.mustBe("a bounded excerpt source URL");
    }
    if (value.text.length > RESEARCH_MAX_EXCERPT_CHARS) {
        return ctx.mustBe("a bounded excerpt");
    }
    if (value.locator !== undefined && value.locator.length > RESEARCH_MAX_LOCATOR_CHARS) {
        return ctx.mustBe("a bounded excerpt locator");
    }
    return true;
});
const ResearchSeedRow = type({
    sourceUrl: "string",
    dbName: "string",
    key: "string",
    item: "unknown",
    "+": "reject",
}).narrow((value, ctx) => {
    if (value.sourceUrl.length === 0 || value.sourceUrl.length > RESEARCH_MAX_SOURCE_URL_CHARS) {
        return ctx.mustBe("a bounded seed source URL");
    }
    if (value.dbName.length === 0 ||
        value.dbName.length > RESEARCH_MAX_SEED_NAME_CHARS ||
        RESEARCH_SEED_NAME_RE.test(value.dbName) === false) {
        return ctx.mustBe("a safe seed database name");
    }
    if (value.key.length === 0 ||
        value.key.length > RESEARCH_MAX_SEED_NAME_CHARS ||
        RESEARCH_SEED_NAME_RE.test(value.key) === false) {
        return ctx.mustBe("a safe seed key");
    }
    if (isBoundedJsonValue(value.item, 0) === false)
        return ctx.mustBe("bounded JSON seed data");
    const itemBytes = jsonByteLength(value.item);
    if (itemBytes === undefined || itemBytes > RESEARCH_MAX_ITEM_BYTES) {
        return ctx.mustBe("a bounded seed item");
    }
    return true;
});
export const PromptResearch = type({
    type: "'prompt.research'",
    version: "1",
    streamId: "string",
    chatId: "string",
    seq: "number",
    timestamp: CoercedDate,
    researchId: "string",
    status: "'complete'|'partial'|'failed'",
    brief: "string",
    sources: ResearchSource.array(),
    api: ResearchApiEndpoint.array(),
    excerpts: ResearchExcerpt.array(),
    seedRows: ResearchSeedRow.array(),
    completedAt: "string",
    "errorCode?": "string",
    "+": "reject",
}).narrow((value, ctx) => {
    if (value.researchId.length === 0 || value.researchId.length > RESEARCH_MAX_ID_CHARS) {
        return ctx.mustBe("a bounded research id");
    }
    if (value.brief.length > RESEARCH_MAX_BRIEF_CHARS)
        return ctx.mustBe("a bounded research brief");
    if (value.sources.length > RESEARCH_MAX_SOURCES)
        return ctx.mustBe("bounded research sources");
    if (value.api.length > RESEARCH_MAX_API_ENDPOINTS)
        return ctx.mustBe("bounded research API endpoints");
    if (value.excerpts.length > RESEARCH_MAX_EXCERPTS)
        return ctx.mustBe("bounded research excerpts");
    if (value.seedRows.length > RESEARCH_MAX_SEED_ROWS)
        return ctx.mustBe("bounded research seed rows");
    if (value.seq !== RESEARCH_BLOCK_SEQ)
        return ctx.mustBe("the hidden research section sequence");
    if (value.completedAt.length === 0 ||
        value.completedAt.length > RESEARCH_MAX_COMPLETED_AT_CHARS ||
        isValidIsoTimestamp(value.completedAt) === false) {
        return ctx.mustBe("a bounded completion timestamp");
    }
    if (value.timestamp instanceof Date === false || Number.isFinite(value.timestamp.getTime()) === false) {
        return ctx.mustBe("a valid server timestamp");
    }
    if (value.errorCode !== undefined && isSafeErrorCode(value.errorCode) === false) {
        return ctx.mustBe("a safe research error code");
    }
    const seedBytes = jsonByteLength(value.seedRows);
    if (seedBytes === undefined || seedBytes > RESEARCH_MAX_SEED_BYTES)
        return ctx.mustBe("bounded research seed data");
    const eventBytes = jsonByteLength(value);
    if (eventBytes === undefined || eventBytes > RESEARCH_MAX_EVENT_BYTES)
        return ctx.mustBe("a bounded research event");
    return true;
});
export function isPromptResearch(value) {
    return !(PromptResearch(value) instanceof type.errors);
}
export const evtUrlResearch = type({
    type: "'vibes.diy.evt-url-research'",
    userId: "string",
    chatId: "string",
    promptId: "string",
    ownerHandle: "string",
    appSlug: "string",
    currentUserText: "string",
    activatedUrls: "string[]",
    model: "string",
    "+": "reject",
}).narrow((value, ctx) => {
    if (value.userId.length === 0 ||
        value.chatId.length === 0 ||
        value.promptId.length === 0 ||
        value.userId.length > RESEARCH_JOB_MAX_ID_CHARS ||
        value.chatId.length > RESEARCH_JOB_MAX_ID_CHARS ||
        value.promptId.length > RESEARCH_JOB_MAX_ID_CHARS) {
        return ctx.mustBe("bounded research identifiers");
    }
    if (value.ownerHandle.length === 0 ||
        value.appSlug.length === 0 ||
        value.ownerHandle.length > RESEARCH_JOB_MAX_HANDLE_CHARS ||
        value.appSlug.length > RESEARCH_JOB_MAX_HANDLE_CHARS) {
        return ctx.mustBe("bounded research app scope");
    }
    if (value.currentUserText.length > RESEARCH_JOB_MAX_TEXT_CHARS) {
        return ctx.mustBe("bounded research user text");
    }
    if (value.model.length === 0 || value.model.length > RESEARCH_JOB_MAX_MODEL_CHARS) {
        return ctx.mustBe("a bounded research model");
    }
    if (value.activatedUrls.length > RESEARCH_MAX_SOURCES ||
        value.activatedUrls.some((url) => url.length === 0 || url.length > RESEARCH_MAX_SOURCE_URL_CHARS || /\s/.test(url) || url.startsWith("https://") === false)) {
        return ctx.mustBe("bounded HTTPS research candidates");
    }
    const eventBytes = jsonByteLength(value);
    if (eventBytes === undefined || eventBytes > RESEARCH_MAX_EVENT_BYTES) {
        return ctx.mustBe("a bounded research queue event");
    }
    return true;
});
export function isEvtUrlResearch(value) {
    return !(evtUrlResearch(value) instanceof type.errors);
}
export function buildUrlResearchJob(input) {
    if (input.mode !== "codegen" || input.isInitialTurn === false)
        return undefined;
    return {
        userId: input.userId,
        chatId: input.chatId,
        promptId: input.promptId,
        ownerHandle: input.ownerHandle,
        appSlug: input.appSlug,
        currentUserText: input.currentUserText.slice(0, RESEARCH_JOB_MAX_TEXT_CHARS),
        activatedUrls: [...input.activatedUrls],
        model: input.model,
    };
}
//# sourceMappingURL=research.js.map