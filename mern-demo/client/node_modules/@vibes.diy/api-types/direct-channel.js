const DM_PREFIX = "_d.";
export function directChannelUserSlug(slugA, slugB) {
    const [first, second] = slugA < slugB ? [slugA, slugB] : [slugB, slugA];
    return `${DM_PREFIX}${first}.${second}`;
}
export function isDirectChannel(ownerHandle) {
    return ownerHandle.startsWith(DM_PREFIX);
}
export function directChannelParticipants(channelUserSlug) {
    if (!isDirectChannel(channelUserSlug))
        return null;
    const body = channelUserSlug.slice(DM_PREFIX.length);
    const dotIdx = body.indexOf(".");
    if (dotIdx < 1 || dotIdx === body.length - 1)
        return null;
    return [body.slice(0, dotIdx), body.slice(dotIdx + 1)];
}
//# sourceMappingURL=direct-channel.js.map