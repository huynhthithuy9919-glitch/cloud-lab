import { type } from "arktype";
const nonBlank = type("string").narrow((s) => s.trim().length > 0);
export const createVibeToolInput = type({
    prompt: nonBlank,
    "skills?": "string[]",
    "pairs?": type({ title: "string", slug: "string" }).array(),
    "iconDescription?": "string",
    "theme?": "string",
    "enrichedPrompt?": "string",
    "textureNote?": "string",
    "privacySensitive?": "boolean",
    "deferred?": {
        "access?": { needed: "boolean", why: "string" },
        "backend?": { recommended: "boolean", why: "string" },
    },
}).onUndeclaredKey("delete");
export function isCreateVibeToolInput(obj) {
    return !(createVibeToolInput(obj) instanceof type.errors);
}
export const editVibeToolInput = type({
    instruction: nonBlank,
    "baseFsId?": "string",
    "focusPath?": "string",
    "skills?": "string[]",
    "selected?": type({ kind: "'version'", fsId: "string" }),
});
export const setAppStyleToolInput = type({
    instruction: nonBlank,
}).onUndeclaredKey("delete");
export function isSetAppStyleToolInput(obj) {
    return !(setAppStyleToolInput(obj) instanceof type.errors);
}
export function isEditVibeToolInput(obj) {
    return !(editVibeToolInput(obj) instanceof type.errors);
}
export const agentToolRefusal = type({
    ok: "false",
    reason: "'busy' | 'stale-base' | 'out-of-scope' | 'ambiguous-handle' | 'not-owner' | 'invalid-input' | 'failed' | 'no-app-in-scope' | 'unchanged' | 'already-created' | 'claim-unavailable'",
    "message?": "string",
    "inFlightPromptId?": "string",
    "currentFsId?": "string",
    "candidates?": "string[]",
    "scope?": { ownerHandle: "string", "appSlug?": "string" },
});
export function isAgentToolRefusal(obj) {
    return !(agentToolRefusal(obj) instanceof type.errors);
}
export const agentBuildDispatched = type({
    ok: "true",
    chatId: "string",
    ownerHandle: "string",
    appSlug: "string",
    "promptId?": "string",
});
export function isAgentBuildDispatched(obj) {
    return !(agentBuildDispatched(obj) instanceof type.errors);
}
export const agentStyleApplied = type({
    ok: "true",
    ownerHandle: "string",
    appSlug: "string",
    description: "string",
    "font?": "string",
});
export function isAgentStyleApplied(obj) {
    return !(agentStyleApplied(obj) instanceof type.errors);
}
//# sourceMappingURL=agent-tools.js.map