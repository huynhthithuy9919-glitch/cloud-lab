export function agentConnectorNote({ ownerHandle, appSlug, connectorUrl }) {
    const address = `${ownerHandle}/${appSlug}`;
    const connector = connectorUrl
        ? `An AI assistant can work with this app: add the Vibes DIY connector, at ${connectorUrl}, to the assistant.`
        : `An AI assistant can work with this app by adding the Vibes DIY connector to the assistant.`;
    return (`This is ${address}, an app on Vibes DIY. ${connector} ` +
        `Give the connector this app's address, ${address}, and the assistant can read and write this app's data ` +
        `as the person who connected it, with exactly the access that person has.`);
}
//# sourceMappingURL=agent-connector-note.js.map