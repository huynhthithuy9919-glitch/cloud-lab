export function mkTokenUnavailableError(message, reason) {
    return Object.assign(new Error(message), { tokenUnavailableReason: reason });
}
export function tokenUnavailableReason(err) {
    if (typeof err !== "object" || err === null)
        return undefined;
    const reason = err.tokenUnavailableReason;
    return typeof reason === "string" ? reason : undefined;
}
export function isIndeterminateTokenError(err) {
    return tokenUnavailableReason(err) === "unavailable";
}
export const AUTH_INDETERMINATE_CODE = "auth-indeterminate";
export function isAuthIndeterminateError(err) {
    if (typeof err !== "object" || err === null)
        return false;
    const error = err.error;
    return typeof error === "object" && error !== null && error.code === AUTH_INDETERMINATE_CODE;
}
//# sourceMappingURL=auth.js.map