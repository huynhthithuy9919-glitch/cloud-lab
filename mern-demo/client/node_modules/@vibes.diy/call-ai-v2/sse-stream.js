import { type } from "arktype";
import { CoercedDate } from "./types.js";
import { isDataBegin, isDataLine, isDataEnd, isDataStats } from "./data-stream.js";
import { isStatsCollect } from "./stats-stream.js";
import { passthrough } from "./passthrough.js";
import { ToolCallDelta } from "./prompt-type.js";
export const SseUsage = type({
    prompt_tokens: "number",
    completion_tokens: "number",
    total_tokens: "number",
});
export const SSeImage = type({
    type: "string",
    "index?": "number",
    image_url: type({
        url: "string",
    }),
});
export const SseChunk = type({
    id: "string",
    provider: "string",
    model: "string",
    object: "string",
    created: "number",
    choices: type({
        index: "number",
        delta: {
            "role?": "string",
            "content?": "string|null",
            "reasoning?": "string|null",
            "reasoning_details?": "unknown[]",
            "images?": SSeImage.array(),
            "tool_calls?": ToolCallDelta.array(),
            "+": "delete",
        },
        finish_reason: "string|null",
        native_finish_reason: "string|null",
        "logprobs?": "unknown",
    }).array(),
    "system_fingerprint?": "string",
    "usage?": SseUsage,
    "+": "delete",
});
export const SseBeginMsg = type({
    type: "'sse.begin'",
    streamId: "string",
    timestamp: CoercedDate,
});
export const SseLineMsg = type({
    type: "'sse.line'",
    streamId: "string",
    chunk: SseChunk,
    chunkNr: "number",
    timestamp: CoercedDate,
});
export const SseEndMsg = type({
    type: "'sse.end'",
    streamId: "string",
    usages: SseUsage.array(),
    totalChunks: "number",
    totalErrors: "number",
    timestamp: CoercedDate,
});
export const SseErrorMsg = type({
    type: "'sse.error'",
    streamId: "string",
    error: "string",
    json: "unknown",
    errorNr: "number",
    timestamp: CoercedDate,
});
export const SseStatsMsg = type({
    type: "'sse.stats'",
    streamId: "string",
    stats: {
        chunkNr: "number",
        errorNr: "number",
    },
    timestamp: CoercedDate,
});
export const SseStreamMsg = SseBeginMsg.or(SseLineMsg).or(SseErrorMsg).or(SseEndMsg).or(SseStatsMsg);
export const isSseBegin = (msg, streamId) => !(SseBeginMsg(msg) instanceof type.errors) && (!streamId || msg.streamId === streamId);
export const isSseLine = (msg, streamId) => !(SseLineMsg(msg) instanceof type.errors) && (!streamId || msg.streamId === streamId);
export const isSseError = (msg, streamId) => !(SseErrorMsg(msg) instanceof type.errors) && (!streamId || msg.streamId === streamId);
export const isSseEnd = (msg, streamId) => !(SseEndMsg(msg) instanceof type.errors) && (!streamId || msg.streamId === streamId);
export const isSseStats = (msg, streamId) => !(SseStatsMsg(msg) instanceof type.errors) && (!streamId || msg.streamId === streamId);
export const isSseMsg = (msg, streamId) => !(SseStreamMsg(msg) instanceof type.errors) && (!streamId || msg.streamId === streamId);
export function createSseStream(filterStreamId) {
    let chunkNr = 0;
    let errorNr = 0;
    const usages = [];
    let streamId = "";
    return new TransformStream({
        transform: passthrough((msg, controller) => {
            if (isStatsCollect(msg, filterStreamId)) {
                controller.enqueue({
                    type: "sse.stats",
                    streamId: filterStreamId,
                    stats: { chunkNr, errorNr },
                    timestamp: new Date(),
                });
                return;
            }
            if (isDataStats(msg, filterStreamId)) {
                return;
            }
            if (isDataBegin(msg, filterStreamId)) {
                streamId = msg.streamId;
                controller.enqueue({
                    type: "sse.begin",
                    streamId,
                    timestamp: new Date(),
                });
            }
            else if (isDataLine(msg, filterStreamId)) {
                const result = SseChunk(msg.json);
                if (result instanceof type.errors) {
                    errorNr++;
                    controller.enqueue({
                        type: "sse.error",
                        streamId,
                        error: result.summary,
                        json: msg.json,
                        errorNr,
                        timestamp: new Date(),
                    });
                    return;
                }
                chunkNr++;
                if (result.usage) {
                    usages.push(result.usage);
                }
                controller.enqueue({
                    type: "sse.line",
                    streamId,
                    chunk: result,
                    chunkNr,
                    timestamp: new Date(),
                });
            }
            else if (isDataEnd(msg, filterStreamId)) {
                controller.enqueue({
                    type: "sse.end",
                    streamId,
                    usages,
                    totalChunks: chunkNr,
                    totalErrors: errorNr,
                    timestamp: new Date(),
                });
            }
        }),
    });
}
//# sourceMappingURL=sse-stream.js.map