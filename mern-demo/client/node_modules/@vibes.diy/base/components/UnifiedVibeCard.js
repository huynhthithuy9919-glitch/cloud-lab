import React, { useCallback, useEffect, useRef, useState } from "react";
import { VibesSwitch } from "./VibesSwitch.js";
import { ovalColors } from "./VibesSwitch.styles.js";
import { EditorTabStrip } from "./EditorTabStrip.js";
import { ViewerStandingView } from "./ViewerStandingView.js";
import { OptionButtons } from "./OptionButtons.js";
import { ViewerTagView } from "./ViewerTagView.js";
import { SignedOutSavePrompt } from "./SignedOutSavePrompt.js";
import { HandlePickerMenu } from "./HandlePickerMenu.js";
import { CreditsRingView } from "./CreditsRingView.js";
import { CreditsDetailMenu } from "./CreditsDetailMenu.js";
import { CodingIndicator } from "./CodingIndicator.js";
import { AttachmentButton, AttachmentChips } from "./AttachmentRow.js";
import { useAttachments } from "../attachments/use-attachments.js";
import { PeopleIcon, PersonIcon } from "./icons/index.js";
import { HOME_BRAND_FONT } from "./homeBrandFont.js";
import { SAFE_AREA_TOP } from "../theme/safe-area.js";
const UNIFIED_VIBE_CARD_DARK_VARS = `
  --color-dark-primary: #e0e0e0;
  --color-dark-secondary: #aaaaaa;
  --color-dark-decorative-00: #333333;
  --color-dark-decorative-01: #444444;
  --color-dark-decorative-02: #e0e0e0;
  --color-dark-background-00: #1a1a1a;
  --color-dark-background-01: #222222;
  --color-dark-background-02: #222222;
  --uvc-card-shadow: rgba(255,255,255,0.75);
  --uvc-amber-text: #fbbf24;
  --uvc-blue-text: #93c5fd;
  --uvc-purple-text: #c4b5fd;
  --color-light-primary: var(--color-dark-primary, #e0e0e0);
  --color-light-secondary: var(--color-dark-secondary, #e0e0e0);
  --color-light-decorative-00: var(--color-dark-decorative-00, #333333);
  --color-light-decorative-01: var(--color-dark-decorative-01, #444444);
  --color-light-decorative-02: var(--color-dark-decorative-02, #e0e0e0);
  --color-light-background-00: var(--color-dark-background-00, #1a1a1a);
  --color-light-background-01: var(--color-dark-background-01, #222222);
  --color-light-background-02: var(--color-dark-background-02, #222222);`;
const unifiedVibeCardDarkCss = `
@media (prefers-color-scheme: dark) {
  [data-unified-vibe-card], [data-vibe-handle-menu], [data-vibe-version-menu], [data-vibe-credits-menu] {${UNIFIED_VIBE_CARD_DARK_VARS}
  }
}
.dark [data-unified-vibe-card], .dark [data-vibe-handle-menu], .dark [data-vibe-version-menu], .dark [data-vibe-credits-menu] {${UNIFIED_VIBE_CARD_DARK_VARS}
}
/* Composer submit button: a blue pulsating glow while the field holds unsent
   content, nudging the user to send it — the same "something's live, act on it"
   cue the homepage submit-ripple gives, tinted to the button's vibes-blue.
   Applied via className in OtherRow, gated to typed/staged-but-unsent + enabled.
   Each stop keeps the .vibes-edit-btn hard offset shadow as the first layer so
   the brutalist depth stays put while the ring pulses on top — a bare ring-only
   box-shadow would override the class shadow (animations beat normal
   declarations) and flicker the depth out exactly when the button wants notice. */
@keyframes vibesSubmitPulse {
  0% {
    box-shadow: 2px 2px 0 0 var(--vibes-card-border), 0 0 0 0 rgba(59, 130, 246, 0.5);
  }
  70% {
    box-shadow: 2px 2px 0 0 var(--vibes-card-border), 0 0 0 7px rgba(59, 130, 246, 0);
  }
  100% {
    box-shadow: 2px 2px 0 0 var(--vibes-card-border), 0 0 0 0 rgba(59, 130, 246, 0);
  }
}
.vibes-submit-pulse {
  animation: vibesSubmitPulse 1.5s ease-out infinite;
}
/* Reduced motion: hold one soft static ring instead of the loop so the "ready
   to send" cue still reads without any movement (mirrors .submit-ripple) — layered
   over the same hard shadow so the brutalist depth is preserved here too. */
@media (prefers-reduced-motion: reduce) {
  .vibes-submit-pulse {
    animation: none;
    box-shadow: 2px 2px 0 0 var(--vibes-card-border), 0 0 0 3px rgba(59, 130, 246, 0.35);
  }
}
/* Access-wizard held state (#4056): the wizard is a whole codegen turn — tens of
   seconds — and the banner spent all of it as a flat blue rectangle with a
   static "Securing…" stamp, which reads as hung rather than working. A slow
   indeterminate sweep across the banner plus a spinner in the held control give
   the wait a pulse, so "still going" is legible without a progress number we
   don't have. Self-contained keyframes (no global.css / Tailwind dependency) to
   match the rest of this card. Reduced motion holds both still — the banner
   keeps a soft static highlight so the state still reads as distinct. */
@keyframes vibesSecuringSweep {
  0% { background-position: 200% 0; }
  100% { background-position: -200% 0; }
}
.vibes-securing {
  background-image: linear-gradient(100deg, transparent 35%, rgba(59, 130, 246, 0.16) 50%, transparent 65%);
  background-size: 250% 100%;
  background-repeat: no-repeat;
  animation: vibesSecuringSweep 1.8s linear infinite;
}
@keyframes vibesSecuringSpin {
  to { transform: rotate(360deg); }
}
.vibes-securing-spinner {
  animation: vibesSecuringSpin 0.8s linear infinite;
}
@media (prefers-reduced-motion: reduce) {
  .vibes-securing {
    animation: none;
    background-image: linear-gradient(100deg, transparent 35%, rgba(59, 130, 246, 0.1) 50%, transparent 65%);
    background-size: 100% 100%;
  }
  .vibes-securing-spinner {
    animation: none;
  }
}
/* Bottom-nav circles on narrow phones: five fixed 38px circles + the 120px logo
   spacer + 6px gaps need 340px of row width, but a 375px viewport only leaves
   327px after the card insets and row padding — and flex's default shrink would
   squash the circles into ovals. The circles are shrink-proof (flexShrink 0 in
   NavIcon) and instead step DOWN via these vars so the row keeps fitting, clear
   of the floating logo, on common 360–400px phones (Codex P2 on #3037). */
@media (max-width: 400px) {
  [data-unified-vibe-card] { --uvc-nav-size: 34px; --uvc-nav-gap: 4px; }
}
@media (max-width: 360px) {
  [data-unified-vibe-card] { --uvc-nav-size: 30px; }
}
/* Composer input row — a FIELD, not a button (Storybook audition C2, #4578).
   It used to wear a hard 2px/3px offset shadow over the card's own grey, which
   put it one pixel of shadow away from .vibes-edit-btn and made the row read
   as three buttons instead of a field with two controls. So: no shadow at all
   (the send/work buttons are now the only raised things in the row — that IS
   the cue), and a distinct field fill (--vibes-field-bg: white on the pale
   card, one step below near-black on the dark card) so the surface you type
   into is visibly a different kind of surface from the card. The 2px border and
   8px corners stay — the brutalist language is the border, not the shadow.
   Focus swaps the border to brand blue (--vibes-blue, #3b82f6 both modes) in
   place of the default focus ring. The caret is the real one: it appears on
   focus, because a painted caret on an unfocused input lies about focus state. */
.vibes-edit-field {
  border: 2px solid var(--vibes-card-border);
  border-radius: 8px;
  background: var(--vibes-field-bg, #ffffff);
  box-shadow: none;
  transition: border-color 0.12s ease;
}
.vibes-edit-field:focus-within {
  border-color: var(--vibes-blue);
}
/* Suggestion-chip well (audition D, #4578). The chips and their label become
   ONE panel — a light recess holding a labelled group of suggestions — and the
   chips inside flip to the field fill with a 1px border and a 1px raise: the
   panel says "these are one thing", the raise says "these are still buttons".
   Chips self-size to their text so they read as suggestions rather than as rows
   of a menu.

   This is CSS scoped to the well, not a change to OptionButtons: that component
   is ALSO the in-transcript option list in MessageList (a different surface,
   deliberately untouched). It only marks its option buttons with
   data-chip-role="option"; the styling lives here and reaches nothing outside
   .vibes-chip-well. Gold upgrade/jump chips (data-chip-tone="gold") keep
   their amber treatment — they're inside the well but not of the neutral set. */
.vibes-chip-well {
  background: rgba(128, 128, 128, 0.13);
  border-radius: 10px;
  padding: 8px 10px 10px;
  gap: 6px;
}
.vibes-chip-well [data-testid="option-buttons-explainer"] {
  opacity: 0.65;
}
.vibes-chip-well [data-message-role="brainstorm-options"] {
  gap: 6px;
}
.vibes-chip-well [data-chip-role="option"]:not([data-chip-tone="gold"]) {
  background: var(--vibes-field-bg, #ffffff);
  border: 1px solid var(--vibes-card-border);
  box-shadow: 1px 1px 0 0 var(--vibes-card-border);
}
/* Self-sized, not full-width. flex: 0 0 auto (not just width: auto) because
   a chip decorated with an aside is a flex child of its own row wrapper and
   would otherwise still stretch. The vertical padding stays at the shipped 8px:
   the chip loses width and type size, never tap target — 38px of button plus the
   6px gap keeps the effective target at 44px. */
.vibes-chip-well [data-chip-role="option"] {
  width: auto;
  max-width: 100%;
  flex: 0 0 auto;
  align-self: flex-start;
  border-radius: 8px;
  font-size: 13px;
  /* Pinned rather than inherited from text-sm: dropping to 13px type shrank the
     line box and with it the tap target. 20px of line + 16px of padding + 2px
     of border = a 38px button, which with the 6px gap holds the effective
     target at the 44px it had before the restyle. */
  line-height: 20px;
  padding: 8px 10px;
}
/* The field's :focus-within (blue border) is the SINGLE focus cue. The inner
   <input> would otherwise draw the app-wide global :focus-visible outline
   (2px solid var(--vibes-blue), offset 2px — see base/theme/global-styles.ts),
   which paints a second blue rectangle nested inside the field. Suppress it
   here; keyboard focus stays visible via the field border, so a11y holds. */
.vibes-edit-field input:focus,
.vibes-edit-field input:focus-visible,
.vibes-edit-field textarea:focus,
.vibes-edit-field textarea:focus-visible {
  outline: none;
}
/* Submit / stop buttons get the brutalist treatment: 2px solid border, a hard
   mini offset shadow, and the global press effect (translate into the shadow,
   which drops — same feel as the neo-brutalist <select>:active). Rounded-square
   footprint (borderRadius 8 from OTHER_ROW_BUTTON_STYLE, matching the field's
   corners) — the audition winner over the old circle. */
.vibes-edit-btn {
  border: 2px solid var(--vibes-card-border);
  box-shadow: 2px 2px 0 0 var(--vibes-card-border);
  transition: transform 0.12s ease, box-shadow 0.12s ease;
}
.vibes-edit-btn:active:not(:disabled) {
  transform: translate(2px, 2px);
  box-shadow: none;
}`;
const CARD_VIEWPORT_HEIGHT = "58%";
const CARD_FULL_VIEWPORT_HEIGHT = "94%";
export function UnifiedVibeCard(props) {
    const { appTitle, appSlug, isTwinkling, isTeasing, open: controlledOpen, onOpenChange } = props;
    const [internalOpen, setInternalOpen] = useState(false);
    const open = controlledOpen ?? internalOpen;
    const setOpen = (next) => {
        if (controlledOpen === undefined)
            setInternalOpen(next);
        onOpenChange?.(next);
    };
    const [hidden, setHidden] = useState(false);
    const LONG_PRESS_HIDE_MS = 500;
    const HIDE_MOVE_TOLERANCE_PX = 10;
    const hidePressTimer = useRef(undefined);
    const hideOrigin = useRef(undefined);
    const restorePressed = useRef(false);
    useEffect(() => () => clearTimeout(hidePressTimer.current), []);
    const cancelHidePress = () => {
        clearTimeout(hidePressTimer.current);
        hidePressTimer.current = undefined;
        hideOrigin.current = undefined;
    };
    const onSwitchPointerDown = (e) => {
        if (e.pointerType === "mouse" && e.button !== 0)
            return;
        hideOrigin.current = { x: e.clientX, y: e.clientY };
        clearTimeout(hidePressTimer.current);
        hidePressTimer.current = setTimeout(() => {
            hidePressTimer.current = undefined;
            hideOrigin.current = undefined;
            setOpen(false);
            setHidden(true);
        }, LONG_PRESS_HIDE_MS);
    };
    const onSwitchPointerMove = (e) => {
        if (!hideOrigin.current)
            return;
        if (Math.hypot(e.clientX - hideOrigin.current.x, e.clientY - hideOrigin.current.y) > HIDE_MOVE_TOLERANCE_PX) {
            cancelHidePress();
        }
    };
    const pickerInteractive = props.handles !== undefined;
    const [internalPickerOpen, setInternalPickerOpen] = useState(false);
    const pickerOpen = props.handlePickerOpen ?? internalPickerOpen;
    const setPickerOpen = (next) => {
        if (props.handlePickerOpen === undefined)
            setInternalPickerOpen(next);
        props.onHandlePickerOpenChange?.(next);
        if (next)
            setCreditsOpen(false);
    };
    const pickerWrapRef = useRef(null);
    const menuRef = useRef(null);
    useEffect(() => {
        if (!pickerOpen)
            return;
        const onDown = (e) => {
            const target = e.target;
            if (!pickerWrapRef.current?.contains(target) && !menuRef.current?.contains(target))
                setPickerOpen(false);
        };
        document.addEventListener("mousedown", onDown);
        return () => document.removeEventListener("mousedown", onDown);
    }, [pickerOpen]);
    useEffect(() => {
        if (open === false && pickerOpen === true)
            setPickerOpen(false);
    }, [open, pickerOpen]);
    const creditsEnabled = props.credits?.enabled === true;
    const [internalCreditsOpen, setInternalCreditsOpen] = useState(false);
    const creditsOpen = props.creditsMenuOpen ?? internalCreditsOpen;
    const setCreditsOpen = (next) => {
        if (props.creditsMenuOpen === undefined)
            setInternalCreditsOpen(next);
        props.onCreditsMenuOpenChange?.(next);
        if (next)
            setPickerOpen(false);
    };
    const creditsWrapRef = useRef(null);
    const creditsMenuRef = useRef(null);
    useEffect(() => {
        if (!creditsOpen)
            return;
        const onDown = (e) => {
            const target = e.target;
            if (!creditsWrapRef.current?.contains(target) && !creditsMenuRef.current?.contains(target))
                setCreditsOpen(false);
        };
        document.addEventListener("mousedown", onDown);
        return () => document.removeEventListener("mousedown", onDown);
    }, [creditsOpen]);
    useEffect(() => {
        if (open === false && creditsOpen === true)
            setCreditsOpen(false);
    }, [open, creditsOpen]);
    const [draft, setDraft] = useState("");
    const [queued, setQueued] = useState([]);
    const attachmentsCtl = useAttachments();
    const prevAppSlugRef = useRef(props.appSlug);
    if (prevAppSlugRef.current !== props.appSlug) {
        prevAppSlugRef.current = props.appSlug;
        setDraft("");
        setQueued([]);
        attachmentsCtl.clear();
    }
    const restore = props.restoreComposer;
    const stageAttachments = attachmentsCtl.stage;
    const prevRestoreSeqRef = useRef(undefined);
    useEffect(() => {
        if (restore === undefined || restore.seq === prevRestoreSeqRef.current)
            return;
        prevRestoreSeqRef.current = restore.seq;
        setDraft((d) => (d === "" ? restore.text : d));
        stageAttachments(restore.attachments);
    }, [restore, stageAttachments]);
    const seed = props.seedComposer;
    const prevSeedSeqRef = useRef(undefined);
    useEffect(() => {
        if (seed === undefined || seed.seq === prevSeedSeqRef.current)
            return;
        prevSeedSeqRef.current = seed.seq;
        setDraft((d) => (d === "" ? seed.text : d));
    }, [seed]);
    const { generating, onSubmitOther } = props;
    const liveDuringBuild = props.liveDuringBuild;
    const canSendNow = props.canSendNow;
    const [flushAttempt, setFlushAttempt] = useState(0);
    useEffect(() => {
        const flushable = liveDuringBuild === true && canSendNow === true ? true : !generating;
        if (!flushable || queued.length === 0)
            return;
        const batch = queued.join("\n\n");
        if (onSubmitOther?.(batch) !== false) {
            setQueued([]);
            setFlushAttempt(0);
            return;
        }
        if (flushAttempt >= QUEUE_FLUSH_MAX_ATTEMPTS) {
            setQueued([]);
            setFlushAttempt(0);
            setDraft((d) => (d === "" ? batch : `${d}\n${batch}`));
            return;
        }
        const timer = setTimeout(() => setFlushAttempt((n) => n + 1), QUEUE_FLUSH_RETRY_MS);
        return () => clearTimeout(timer);
    }, [generating, queued, onSubmitOther, flushAttempt, liveDuringBuild, canSendNow]);
    const [mounted, setMounted] = useState(open);
    const [grown, setGrown] = useState(open);
    useEffect(() => {
        let t;
        if (open) {
            setMounted(true);
            t = setTimeout(() => setGrown(true), 10);
        }
        else {
            setGrown(false);
            t = setTimeout(() => setMounted(false), 240);
        }
        return () => clearTimeout(t);
    }, [open]);
    const [everOpened, setEverOpened] = useState(open);
    useEffect(() => {
        if (open)
            setEverOpened(true);
    }, [open]);
    const retained = Boolean(props.keepMounted) && everOpened;
    const scrollRegionRef = useRef(null);
    const chipsRegionRef = useRef(null);
    const stickToBottomRef = useRef(true);
    const hasHistory = props.historyBody != null && props.body == null;
    const streaming = props.streamBody != null;
    useEffect(() => {
        if (!streaming)
            return;
        stickToBottomRef.current = true;
        const region = scrollRegionRef.current;
        if (region)
            region.scrollTop = region.scrollHeight;
    }, [streaming]);
    useEffect(() => {
        const region = scrollRegionRef.current;
        const content = chipsRegionRef.current;
        if (!hasHistory || !region || !content)
            return;
        const pin = () => {
            if (stickToBottomRef.current)
                region.scrollTop = region.scrollHeight;
        };
        const onScroll = () => {
            stickToBottomRef.current = region.scrollHeight - region.scrollTop - region.clientHeight < 48;
        };
        region.addEventListener("scroll", onScroll);
        pin();
        const ro = typeof ResizeObserver !== "undefined" ? new ResizeObserver(pin) : undefined;
        ro?.observe(content);
        return () => {
            region.removeEventListener("scroll", onScroll);
            ro?.disconnect();
        };
    }, [hasHistory, mounted]);
    const [userHeight, setUserHeight] = useState(null);
    const cardRef = useRef(null);
    const dragStateRef = useRef(null);
    const [dragging, setDragging] = useState(false);
    const RESIZE_MIN = 220;
    const FLICK_VELOCITY = 0.6;
    const FLICK_MIN_SAMPLE_MS = 8;
    const FLICK_MAX_IDLE_MS = 120;
    function resizeMaxHeight() {
        const parentEl = cardRef.current?.offsetParent ?? null;
        const viewportH = typeof window === "undefined" ? RESIZE_MIN : window.innerHeight;
        const parentH = parentEl instanceof HTMLElement && parentEl.clientHeight > 0 ? parentEl.clientHeight : viewportH;
        return Math.max(RESIZE_MIN, parentH - 24);
    }
    function clampHeight(px) {
        return Math.min(Math.max(px, RESIZE_MIN), resizeMaxHeight());
    }
    const heightFractionRef = useRef(null);
    function setForcedHeight(px) {
        const clamped = clampHeight(px);
        const max = resizeMaxHeight();
        heightFractionRef.current = max > 0 ? clamped / max : null;
        setUserHeight(clamped);
    }
    function clearForcedHeight() {
        heightFractionRef.current = null;
        setUserHeight(null);
    }
    useEffect(() => {
        if (typeof window === "undefined")
            return;
        const onResize = () => {
            const frac = heightFractionRef.current;
            if (frac === null)
                return;
            setUserHeight(clampHeight(frac * resizeMaxHeight()));
        };
        window.addEventListener("resize", onResize);
        return () => window.removeEventListener("resize", onResize);
    }, []);
    function currentHeight() {
        return userHeight ?? cardRef.current?.offsetHeight ?? 0;
    }
    function onHandlePointerDown(e) {
        e.preventDefault();
        setDragging(true);
        const now = performance.now();
        dragStateRef.current = { startY: e.clientY, startHeight: currentHeight(), lastY: e.clientY, lastT: now, velocity: 0 };
        if (typeof e.currentTarget.setPointerCapture === "function")
            e.currentTarget.setPointerCapture(e.pointerId);
    }
    function onHandlePointerMove(e) {
        const drag = dragStateRef.current;
        if (drag === null)
            return;
        const now = performance.now();
        const dt = now - drag.lastT;
        if (dt >= FLICK_MIN_SAMPLE_MS) {
            drag.velocity = (drag.lastY - e.clientY) / dt;
            drag.lastY = e.clientY;
            drag.lastT = now;
        }
        setForcedHeight(drag.startHeight + (drag.startY - e.clientY));
    }
    function cancelHandleDrag(e) {
        dragStateRef.current = null;
        setDragging(false);
        if (typeof e.currentTarget.releasePointerCapture === "function")
            e.currentTarget.releasePointerCapture(e.pointerId);
    }
    function endHandleDrag(e) {
        const drag = dragStateRef.current;
        dragStateRef.current = null;
        setDragging(false);
        if (typeof e.currentTarget.releasePointerCapture === "function")
            e.currentTarget.releasePointerCapture(e.pointerId);
        if (drag === null)
            return;
        const settled = performance.now() - drag.lastT > FLICK_MAX_IDLE_MS;
        const velocity = settled ? 0 : drag.velocity;
        if (velocity > FLICK_VELOCITY) {
            setForcedHeight(resizeMaxHeight());
        }
        else if (velocity < -FLICK_VELOCITY) {
            clearForcedHeight();
            setOpen(false);
        }
    }
    function onHandleKeyDown(e) {
        if (e.key !== "ArrowUp" && e.key !== "ArrowDown")
            return;
        e.preventDefault();
        setForcedHeight(currentHeight() + (e.key === "ArrowUp" ? 24 : -24));
    }
    const fullHeightActive = Boolean(props.fullHeight) && userHeight === null;
    const cardHeight = userHeight ?? (props.fullHeight ? CARD_FULL_VIEWPORT_HEIGHT : CARD_VIEWPORT_HEIGHT);
    const heightForced = userHeight !== null;
    const streamFillActive = Boolean((props.streamFill || heightForced) && props.streamBody && props.body == null);
    const agentPanelActive = props.agentPanel != null;
    const stretch = heightForced || fullHeightActive || Boolean(props.bodyFill && props.body) || streamFillActive || agentPanelActive;
    const resizeValueMax = resizeMaxHeight();
    const resizeValueNow = Math.round(Math.min(Math.max(userHeight ?? cardRef.current?.offsetHeight ?? 0, RESIZE_MIN), resizeValueMax));
    return (React.createElement(React.Fragment, null,
        React.createElement("style", null, unifiedVibeCardDarkCss),
        open && (React.createElement("div", { "aria-hidden": true, "data-testid": "vibe-menu-backdrop", onClick: () => setOpen(false), style: { position: "fixed", inset: 0, zIndex: 1, pointerEvents: "auto" } })),
        !hidden && (mounted || retained) && (React.createElement("div", { ref: cardRef, role: "dialog", "aria-label": "Vibe menu", "data-unified-vibe-card": true, className: props.className, "aria-hidden": mounted ? undefined : true, inert: mounted ? undefined : true, style: {
                position: "absolute",
                left: 6,
                right: 6,
                bottom: 6,
                zIndex: 2,
                borderRadius: 16,
                maxHeight: cardHeight,
                ...(stretch ? { height: cardHeight } : null),
                overflow: "hidden",
                display: "flex",
                flexDirection: "column",
                background: "var(--color-light-background-00, #fff)",
                border: "1px solid var(--vibes-near-black, #1a1a1a)",
                boxShadow: "0 8px 40px var(--uvc-card-shadow, rgba(0,0,0,0.75))",
                transformOrigin: "bottom right",
                transform: grown ? "scale(1)" : "scale(0)",
                transition: dragging
                    ? "transform 0.24s cubic-bezier(0.34, 1.56, 0.64, 1)"
                    : "transform 0.24s cubic-bezier(0.34, 1.56, 0.64, 1), max-height 0.24s ease, height 0.24s ease",
                pointerEvents: grown ? "auto" : "none",
                ...(mounted ? null : { visibility: "hidden" }),
            } },
            React.createElement("div", { style: {
                    display: "flex",
                    flexDirection: "column",
                    minHeight: 0,
                    ...(stretch ? { flex: "1 1 auto" } : null),
                    opacity: grown ? 1 : 0,
                    transition: grown ? "opacity 0.18s ease 0.14s" : "opacity 0.1s ease",
                }, className: "text-light-primary dark:text-dark-primary" },
                React.createElement("div", { style: {
                        display: "flex",
                        alignItems: "center",
                        gap: 10,
                        padding: "10px 14px 6px",
                        flexShrink: 0,
                        position: "relative",
                        userSelect: "none",
                    } },
                    React.createElement("div", { "data-testid": "vibe-card-resize-handle", role: "separator", "aria-orientation": "horizontal", "aria-label": "Resize chat height", "aria-valuemin": RESIZE_MIN, "aria-valuemax": resizeValueMax, "aria-valuenow": resizeValueNow, tabIndex: 0, onPointerDown: onHandlePointerDown, onPointerMove: onHandlePointerMove, onPointerUp: endHandleDrag, onPointerCancel: cancelHandleDrag, onKeyDown: onHandleKeyDown, onDoubleClick: () => clearForcedHeight(), style: {
                            position: "absolute",
                            top: 0,
                            left: 0,
                            right: 0,
                            bottom: 0,
                            zIndex: 0,
                            cursor: "ns-resize",
                            touchAction: "none",
                        } },
                        React.createElement("span", { "aria-hidden": true, style: {
                                position: "absolute",
                                top: 4,
                                left: "50%",
                                transform: "translateX(-50%)",
                                width: 44,
                                height: 4,
                                borderRadius: 999,
                                background: "var(--color-light-decorative-01, #ddd)",
                            } })),
                    React.createElement("div", { style: {
                            position: "relative",
                            width: 30,
                            height: 30,
                            borderRadius: 8,
                            flexShrink: 0,
                            overflow: "hidden",
                            background: "linear-gradient(160deg,#312e81,#4c1d95)",
                            border: "1px solid rgba(0,0,0,0.15)",
                            pointerEvents: "none",
                        } },
                        props.appIconUrl && (React.createElement("img", { src: props.appIconUrl, alt: "", style: { width: "100%", height: "100%", objectFit: "cover", transform: "scale(2)" } })),
                        React.createElement(CodingIndicator, { active: props.coding === true })),
                    React.createElement("div", { style: { lineHeight: 1.2 } },
                        React.createElement("strong", { className: "text-sm", style: { fontFamily: HOME_BRAND_FONT } }, appTitle),
                        appSlug && (React.createElement("div", { className: "text-light-secondary dark:text-dark-secondary", style: { fontSize: 12 } }, appSlug))),
                    (() => {
                        const badges = [
                            props.publishState === "old" ? React.createElement(DraftBadge, { key: "old", variant: "old" }) : null,
                            props.publishState === "draft" && !props.versionLocked ? React.createElement(DraftBadge, { key: "draft", variant: "draft" }) : null,
                            props.versionLocked ? React.createElement(LockedBadge, { key: "locked" }) : null,
                            !props.publishState && props.onViewDraft && !props.versionLocked ? (React.createElement(ViewDraftBadge, { key: "view-draft", onClick: props.onViewDraft })) : null,
                        ].filter(Boolean);
                        return badges.length === 0 ? null : (React.createElement("div", { "data-vibe-card-header-controls": "true", style: { position: "relative", zIndex: 1, display: "flex", alignItems: "center", gap: 10, flexShrink: 0 } }, badges));
                    })(),
                    React.createElement("div", { "data-vibe-card-header-controls": "true", style: {
                            position: "relative",
                            zIndex: 1,
                            marginLeft: "auto",
                            display: "flex",
                            alignItems: "center",
                            gap: 10,
                            flexShrink: 0,
                        } },
                        creditsEnabled && (React.createElement("div", { ref: creditsWrapRef, style: { position: "relative", flexShrink: 0, display: "flex" } },
                            React.createElement(CreditsRingView, { remaining: props.credits?.remaining, grantIncluded: props.credits?.grantIncluded, loading: props.credits?.loading, expanded: creditsOpen, onClick: () => setCreditsOpen(!creditsOpen) }))),
                        props.hideViewerTag ? null : props.handleSlug ? (React.createElement("div", { ref: pickerWrapRef, style: { position: "relative", flexShrink: 0 } },
                            React.createElement(ViewerTagView, { slug: props.handleSlug, displayName: `@${props.handleSlug}`, avatarUrl: props.handleAvatarUrl, editable: Boolean(props.onPickAvatar), onPickFile: props.onPickAvatar, onTagClick: pickerInteractive ? () => setPickerOpen(!pickerOpen) : undefined, tagExpanded: pickerOpen, style: {
                                    background: "var(--color-light-background-01, #eee)",
                                    border: "1px solid var(--color-light-decorative-01, #ddd)",
                                    color: "var(--color-light-primary, #333)",
                                    fontSize: 13,
                                    padding: "3px 8px 3px 4px",
                                    flexShrink: 0,
                                } }))) : props.viewerPending ? (React.createElement(ViewerTagView, { slug: "", pending: true })) : props.viewerNeedsHandle ? (React.createElement(ViewerTagView, { slug: "", needsHandle: true, onChooseHandle: props.onChooseHandle })) : (React.createElement(ViewerTagView, { slug: "?", anonymous: true, onSignIn: props.onSignIn })))),
                (() => {
                    const wizardBanner = (props.publishWizardActive === true || props.publishWizardError === true) && props.onPublish;
                    if (wizardBanner && props.selectedNav !== "share") {
                        return (React.createElement("div", { style: { padding: "0 14px", flexShrink: 0 } },
                            React.createElement(PublishBanner, { variant: props.publishState === "draft" || props.publishState === "old" ? props.publishState : undefined, access: props.publishAccess, onPublish: props.onPublish, publishing: props.publishing, wizardActive: props.publishWizardActive, wizardError: props.publishWizardError, escapeRevealed: props.publishEscapeRevealed, onPublishWithoutAccess: props.onPublishWithoutAccess, onRetryWizard: props.onRetryWizard })));
                    }
                    if (props.autoPublishError === true && props.selectedNav !== "share") {
                        return (React.createElement("div", { style: { padding: "0 14px", flexShrink: 0 } },
                            React.createElement(PublishBanner, { variant: undefined, autoPublishError: true, onRetryAutoPublish: props.onRetryAutoPublish })));
                    }
                    if (props.statusBanner && props.selectedNav !== "share") {
                        return React.createElement("div", { style: { padding: "0 14px", flexShrink: 0 } }, props.statusBanner);
                    }
                    if (props.versionLocked === true && props.selectedNav !== "share") {
                        return (React.createElement("div", { style: { padding: "0 14px", flexShrink: 0 } },
                            React.createElement(PublishBanner, { variant: undefined, locked: true, onUnlock: props.onUnlock })));
                    }
                    if (props.signedOutSavePrompt && props.selectedNav !== "share") {
                        return (React.createElement("div", { style: { padding: "0 14px", flexShrink: 0 } },
                            React.createElement(SignedOutSavePrompt, { onSignIn: props.onSignIn })));
                    }
                    const hasPendingVersion = props.publishState === "draft" || props.publishState === "old";
                    const accessPending = Boolean(props.publishAccessPending) && !hasPendingVersion && props.publishAccess === "private";
                    const showActionBanner = (hasPendingVersion || props.publishAccess === "private") && Boolean(props.onPublish);
                    return ((accessPending || showActionBanner) &&
                        props.selectedNav !== "share" && (React.createElement("div", { style: { padding: "0 14px", flexShrink: 0 } },
                        React.createElement(PublishBanner, { variant: props.publishState === "draft" || props.publishState === "old" ? props.publishState : undefined, access: props.publishAccess, onPublish: props.onPublish, publishing: props.publishing, pending: accessPending }))));
                })(),
                props.editorTab != null && props.selectedNav !== "share" && !agentPanelActive && (React.createElement("div", { style: { padding: "0 14px 6px", flexShrink: 0 } },
                    React.createElement(EditorTabStrip, { tab: props.editorTab, onTab: (t) => props.onEditorTab?.(t), standing: {
                            viewerMode: props.viewerMode,
                            memberReadOnly: props.memberReadOnly,
                            adminMode: props.adminMode,
                            versionLocked: props.versionLocked,
                            publishState: props.publishState,
                            publishAccess: props.publishAccess,
                        } }))),
                props.body == null && props.composerControls && !agentPanelActive && (React.createElement("div", { "data-testid": "vibe-card-composer-controls", "aria-hidden": props.streamBody ? true : undefined, inert: props.streamBody ? true : undefined, style: {
                        padding: "0 14px",
                        marginBottom: 6,
                        flexShrink: 0,
                        ...(props.streamBody ? { visibility: "hidden" } : null),
                    } }, props.composerControls)),
                React.createElement("div", { ref: scrollRegionRef, "data-testid": "vibe-card-scroll", style: {
                        padding: "0 14px 4px",
                        overflowY: "auto",
                        overflowX: "hidden",
                        flex: "1 1 auto",
                        minHeight: 0,
                        ...((props.bodyFill && props.body) || agentPanelActive || ((heightForced || streamFillActive) && props.body == null)
                            ? { display: "flex", flexDirection: "column" }
                            : null),
                    } }, agentPanelActive ? (React.createElement("div", { style: { flex: "1 1 auto", minHeight: 0 } }, props.agentPanel)) : props.editorTab === "standing" ? (React.createElement(ViewerStandingView, { viewerMode: props.viewerMode, memberReadOnly: props.memberReadOnly, adminMode: props.adminMode, versionLocked: props.versionLocked, publishState: props.publishState, publishAccess: props.publishAccess, myGrant: props.myGrant, onToggleAdmin: props.onToggleAdmin, onUnlock: props.onUnlock })) : props.body != null ? (props.bodyFill ? (React.createElement("div", { style: { flex: "1 1 auto", minHeight: 0 } }, props.body)) : (props.body)) : (React.createElement(React.Fragment, null,
                    React.createElement("div", { ref: chipsRegionRef, style: streamFillActive
                            ? { flex: "1 1 auto", minHeight: 0, display: "flex", flexDirection: "column" }
                            : { flex: "1 0 auto", minHeight: 0 } },
                        props.historyBody && (React.createElement("div", { "data-testid": "vibe-card-history", "aria-hidden": props.streamBody ? true : undefined, inert: props.streamBody ? true : undefined, style: { marginBottom: 8, ...(props.streamBody ? { display: "none" } : null) } }, props.historyBody)),
                        React.createElement("div", { "data-testid": "vibe-card-stream-region", style: {
                                position: "relative",
                                ...(streamFillActive
                                    ? { flex: "1 1 auto", minHeight: 0 }
                                    :
                                        props.streamBody
                                            ? { minHeight: props.streamMinHeight ?? 128 }
                                            : null),
                            } },
                            React.createElement("div", { "aria-hidden": props.streamBody ? true : undefined, inert: props.streamBody ? true : undefined, style: props.streamBody ? { visibility: "hidden" } : undefined }, (props.seedOffer || props.upgradeChips || (props.chips && props.chips.length > 0)) && (React.createElement("div", { className: "vibes-chip-well mx-3 mt-2 flex flex-col", "data-testid": "vibe-chip-set" },
                                props.seedOffer,
                                props.upgradeChips,
                                props.chips && props.chips.length > 0 && (React.createElement(OptionButtons, { options: props.chips.slice(0, 3), inSet: true, isFirst: true, firstMessage: props.isOwner
                                        ? "Describe a change to edit this app live:"
                                        : "Describe a change to remix your own copy of this app:", onSelect: (o) => {
                                        const r = props.onSelectChip?.(o);
                                        return r && typeof r === "object" && "then" in r ? r.then((ok) => ok ?? false) : false;
                                    }, decorate: (option) => decorateChip(option, props) }))))),
                            props.streamBody && (React.createElement("div", { style: {
                                    position: "absolute",
                                    inset: 0,
                                    display: "flex",
                                    flexDirection: "column",
                                    minHeight: 0,
                                } },
                                React.createElement("div", { style: { flex: "1 1 auto", minHeight: 0, overflowY: "auto" } }, props.streamBody),
                                props.liveDuringBuild === true && React.createElement(PendingMessages, { messages: queued })))))))),
                props.body == null && !agentPanelActive && (React.createElement("div", { style: { flexShrink: 0, padding: "0 14px 10px" } },
                    React.createElement(OtherRow, { value: draft, onValueChange: setDraft, queued: queued, onQueuedChange: setQueued, onSubmitOther: props.onSubmitOther, onSubmitWork: props.onSubmitWork, generating: props.generating, liveDuringBuild: props.liveDuringBuild, canSendNow: props.canSendNow, onStop: props.onStop, attachmentsCtl: attachmentsCtl }))),
                agentPanelActive ? (React.createElement("div", { "aria-hidden": true, style: { display: "flex", flexShrink: 0, padding: "0 12px 4px" } },
                    React.createElement("div", { style: { width: 120, height: 50, marginLeft: "auto" } }))) : (React.createElement("div", { style: {
                        display: "flex",
                        alignItems: "center",
                        gap: "var(--uvc-nav-gap, 6px)",
                        padding: "0 12px 4px",
                        borderTop: "1px solid var(--color-light-decorative-00, #e5e5e5)",
                        flexShrink: 0,
                    } },
                    React.createElement(NavIcon, { label: "Home", color: "#2563eb", onClick: props.onHome },
                        React.createElement("svg", { width: "17", height: "17", viewBox: "0 0 24 24", fill: "none", stroke: "currentColor", strokeWidth: 2.2, strokeLinecap: "round", strokeLinejoin: "round", "aria-hidden": "true" },
                            React.createElement("path", { d: "M3 12l9-9 9 9" }),
                            React.createElement("path", { d: "M5 10v10a1 1 0 001 1h4v-6h4v6h4a1 1 0 001-1V10" }))),
                    React.createElement(NavIcon, { label: "Account", color: "#7c3aed", selected: props.selectedNav === "recommended", onClick: props.onRecommended },
                        React.createElement(PersonIcon, null)),
                    React.createElement(NavIcon, { label: "Edit", color: "#ea580c", selected: ["edit", "edit-chat", "standing", "code", "data", "style", "settings"].includes(props.selectedNav ?? "edit"), onClick: props.onEdit },
                        React.createElement("svg", { width: "17", height: "17", viewBox: "0 0 24 24", fill: "none", stroke: "currentColor", strokeWidth: 2.2, strokeLinecap: "round", strokeLinejoin: "round", "aria-hidden": "true" },
                            React.createElement("path", { d: "M12 20h9" }),
                            React.createElement("path", { d: "M16.5 3.5a2.121 2.121 0 0 1 3 3L7 19l-4 1 1-4 12.5-12.5z" }))),
                    React.createElement(NavIcon, { label: "Share", color: "#16a34a", selected: props.selectedNav === "share", onClick: props.onShare, buttonRef: props.shareButtonRef, badgeCount: (props.shareCommentCount ?? 0) + (props.pendingRequestCount ?? 0) },
                        React.createElement(PeopleIcon, null)),
                    React.createElement(NavIcon, { label: "FAQ", color: "#4b5563", selected: props.selectedNav === "faq", onClick: props.onAbout }, "?"),
                    React.createElement("div", { "aria-hidden": true, style: { width: 120, height: 50, flexShrink: 0, marginLeft: "auto" } })))))),
        open && pickerInteractive && pickerOpen && props.handleSlug && (React.createElement("div", { ref: menuRef, "data-vibe-handle-menu": true, className: "text-light-primary dark:text-dark-primary", style: {
                position: "fixed",
                top: `calc(${SAFE_AREA_TOP} + 16px)`,
                right: 3,
                zIndex: 4,
                pointerEvents: "auto",
            } },
            React.createElement(HandlePickerMenu, { handles: props.handles ?? [], activeSlug: props.handleSlug, busy: props.handlePickerBusy, style: { position: "static", right: "auto", top: "auto", bottom: "auto" }, onSelect: (slug) => {
                    props.onSelectHandle?.(slug);
                    setPickerOpen(false);
                }, onNewHandle: (handle) => {
                    props.onNewHandle?.(handle);
                    setPickerOpen(false);
                }, onLogout: props.onLogout
                    ? () => {
                        props.onLogout?.();
                        setPickerOpen(false);
                    }
                    : undefined }))),
        open && creditsEnabled && creditsOpen && (React.createElement("div", { ref: creditsMenuRef, "data-vibe-credits-menu": true, className: "text-light-primary dark:text-dark-primary", style: {
                position: "fixed",
                top: `calc(${SAFE_AREA_TOP} + 16px)`,
                right: 3,
                zIndex: 4,
                pointerEvents: "auto",
            } },
            React.createElement(CreditsDetailMenu, { plan: props.credits?.plan, remaining: props.credits?.remaining, grantRemaining: props.credits?.grantRemaining, grantIncluded: props.credits?.grantIncluded, grantResetsAt: props.credits?.grantResetsAt, purchasedRemaining: props.credits?.purchasedRemaining, loading: props.credits?.loading, style: { position: "static", right: "auto", top: "auto", bottom: "auto" }, onManage: props.onManageCredits
                    ? () => {
                        props.onManageCredits?.();
                        setCreditsOpen(false);
                    }
                    : undefined }))),
        !hidden && (React.createElement("div", { style: {
                position: "absolute",
                right: 16,
                bottom: 15,
                zIndex: 3,
                pointerEvents: "auto",
                display: "flex",
                flexDirection: "column",
                alignItems: "center",
                gap: 22,
            } },
            !open && props.versionLocked && (React.createElement("button", { type: "button", "data-testid": "locked-chiclet", "aria-label": "Version locked \u2014 new edits aren't live. Open the vibe menu to unlock.", title: "A version is locked as the live release \u2014 open the menu to unlock", onClick: () => setOpen(true), style: {
                    display: "inline-flex",
                    alignItems: "center",
                    gap: 4,
                    borderRadius: 999,
                    padding: "2px 9px",
                    fontSize: 11,
                    fontWeight: 600,
                    lineHeight: 1.4,
                    color: "#92400e",
                    background: "#fef5e7",
                    border: "1px solid rgba(245,158,11,0.4)",
                    boxShadow: "0 1px 4px rgba(0,0,0,0.25)",
                    whiteSpace: "nowrap",
                    cursor: "pointer",
                } },
                React.createElement(LockGlyphSmall, null),
                "Locked")),
            !open && !props.versionLocked && (props.publishState === "draft" || props.onViewDraft !== undefined) && (React.createElement("button", { type: "button", "data-testid": "draft-chiclet", "aria-label": "Draft \u2014 not live yet. Open the vibe menu to update.", title: "Your latest changes aren't live yet \u2014 open the menu to update", onClick: () => setOpen(true), style: {
                    display: "inline-flex",
                    alignItems: "center",
                    gap: 4,
                    borderRadius: 999,
                    padding: "2px 9px",
                    fontSize: 11,
                    fontWeight: 600,
                    lineHeight: 1.4,
                    color: "#92400e",
                    background: "#fef5e7",
                    border: "1px solid rgba(245,158,11,0.4)",
                    boxShadow: "0 1px 4px rgba(0,0,0,0.25)",
                    whiteSpace: "nowrap",
                    cursor: "pointer",
                } },
                React.createElement("span", { "aria-hidden": true, style: { width: 6, height: 6, borderRadius: "50%", background: "#f59e0b" } }),
                "draft")),
            React.createElement("button", { type: "button", "aria-label": open ? "Close vibe menu" : "Open vibe menu", onClick: () => setOpen(!open), onPointerDown: onSwitchPointerDown, onPointerMove: onSwitchPointerMove, onPointerUp: cancelHidePress, onPointerLeave: cancelHidePress, onPointerCancel: cancelHidePress, title: "Hold to hide the vibe controls", style: {
                    display: "block",
                    background: "none",
                    border: "none",
                    padding: 0,
                    cursor: "pointer",
                    width: 120,
                    height: 44,
                    position: "relative",
                    overflow: "visible",
                } },
                React.createElement(VibesSwitch, { size: 60, style: { position: "absolute", right: 0, bottom: 0, pointerEvents: "none" }, isActive: !open, isTwinkling: !open && isTwinkling, isTeasing: !open && isTeasing, ovalColor: props.versionLocked || props.publishState === "draft" || props.onViewDraft !== undefined
                        ? ovalColors.yellow
                        : undefined })))),
        hidden && (React.createElement("button", { type: "button", "data-testid": "vibe-controls-restore", "aria-label": "Show vibe controls", title: "Show vibe controls", onPointerDown: () => {
                restorePressed.current = true;
            }, onClick: (e) => {
                const keyboard = e.detail === 0;
                if (!restorePressed.current && !keyboard)
                    return;
                restorePressed.current = false;
                setHidden(false);
            }, style: {
                position: "absolute",
                right: 16,
                bottom: 24,
                zIndex: 3,
                pointerEvents: "auto",
                width: 24,
                height: 24,
                borderRadius: "50%",
                display: "flex",
                alignItems: "center",
                justifyContent: "center",
                padding: 0,
                cursor: "pointer",
                opacity: 0.35,
                background: "rgba(0,0,0,0.28)",
                border: "1px solid rgba(255,255,255,0.35)",
                boxShadow: "0 1px 3px rgba(0,0,0,0.25)",
            } },
            React.createElement("span", { "aria-hidden": true, style: { width: 5, height: 5, borderRadius: "50%", background: "rgba(255,255,255,0.9)" } })))));
}
function LockGlyphSmall() {
    return (React.createElement("svg", { "aria-hidden": "true", viewBox: "0 0 24 24", width: 11, height: 11, fill: "none", stroke: "currentColor", strokeWidth: 2.4, strokeLinecap: "round", strokeLinejoin: "round", style: { display: "block" } },
        React.createElement("rect", { x: "5", y: "11", width: "14", height: "10", rx: "2" }),
        React.createElement("path", { d: "M8 11V7a4 4 0 0 1 8 0v4" })));
}
function LockedBadge() {
    return (React.createElement("span", { role: "status", "aria-label": "Version locked \u2014 new edits aren't live", title: "A version is locked as the live release \u2014 new edits won't go live until you unlock", style: {
            flexShrink: 0,
            display: "inline-flex",
            alignItems: "center",
            gap: 4,
            borderRadius: 999,
            padding: "2px 8px",
            fontSize: 11,
            fontWeight: 600,
            lineHeight: 1.4,
            color: "var(--uvc-amber-text, #92400e)",
            background: "rgba(245,158,11,0.14)",
            border: "1px solid rgba(245,158,11,0.45)",
            whiteSpace: "nowrap",
        } },
        React.createElement(LockGlyphSmall, null),
        "Locked"));
}
function DraftBadge({ variant }) {
    const draft = variant === "draft";
    return (React.createElement("span", { role: "status", "aria-label": draft ? "Draft — not live yet" : "Older version — not the published one", title: draft ? "You're viewing a draft that isn't live yet" : "You're viewing a version that isn't the published one", style: {
            flexShrink: 0,
            display: "inline-flex",
            alignItems: "center",
            gap: 4,
            borderRadius: 999,
            padding: "2px 8px",
            fontSize: 11,
            fontWeight: 600,
            lineHeight: 1.4,
            color: "var(--uvc-amber-text, #92400e)",
            background: "rgba(245,158,11,0.14)",
            border: "1px solid rgba(245,158,11,0.45)",
            whiteSpace: "nowrap",
        } },
        React.createElement("span", { "aria-hidden": true, style: { width: 6, height: 6, borderRadius: "50%", background: "#f59e0b" } }),
        draft ? "Draft" : "Older version"));
}
function ViewDraftBadge({ onClick }) {
    return (React.createElement("button", { type: "button", "aria-label": "Newer draft available \u2014 view it", title: "You have a draft that isn't live yet \u2014 click to view it", onClick: onClick, style: {
            flexShrink: 0,
            display: "inline-flex",
            alignItems: "center",
            gap: 4,
            borderRadius: 999,
            padding: "2px 8px",
            fontSize: 11,
            fontWeight: 600,
            lineHeight: 1.4,
            color: "var(--uvc-amber-text, #92400e)",
            background: "rgba(245,158,11,0.14)",
            border: "1px solid rgba(245,158,11,0.45)",
            whiteSpace: "nowrap",
            cursor: "pointer",
        } },
        React.createElement("span", { "aria-hidden": true, style: { width: 6, height: 6, borderRadius: "50%", background: "#f59e0b" } }),
        "Draft \u00B7 view"));
}
function PublishBanner({ variant, access, onPublish, publishing, pending, locked, onUnlock, autoPublishError, onRetryAutoPublish, wizardActive, wizardError, escapeRevealed, onPublishWithoutAccess, onRetryWizard, }) {
    if (autoPublishError === true) {
        return (React.createElement("div", { className: "rounded-md", "data-testid": "auto-publish-error-banner", style: {
                background: "rgba(245,158,11,0.10)",
                border: "1px solid rgba(245,158,11,0.4)",
                padding: "8px 10px",
                marginBottom: 10,
                display: "flex",
                alignItems: "center",
                justifyContent: "space-between",
                gap: 8,
            } },
            React.createElement("span", { className: "text-sm", style: { color: "var(--uvc-amber-text, #92400e)", lineHeight: 1.3 } },
                React.createElement("strong", null, "Couldn\u2019t publish your latest edit."),
                " Your changes are saved but not live yet."),
            React.createElement("button", { type: "button", onClick: onRetryAutoPublish, className: "rounded-md px-3 py-1.5 text-sm font-semibold", style: {
                    flexShrink: 0,
                    background: "var(--vibes-near-black, #1a1a1a)",
                    color: "#fff",
                    border: "none",
                    cursor: "pointer",
                } }, "Retry")));
    }
    if (locked === true) {
        return (React.createElement("div", { className: "rounded-md", "data-testid": "version-locked-banner", style: {
                background: "rgba(245,158,11,0.10)",
                border: "1px solid rgba(245,158,11,0.4)",
                padding: "8px 10px",
                marginBottom: 10,
                display: "flex",
                alignItems: "center",
                justifyContent: "space-between",
                gap: 8,
            } },
            React.createElement("span", { className: "text-sm", style: { color: "var(--uvc-amber-text, #92400e)", lineHeight: 1.3 } },
                React.createElement("strong", null, "Version locked."),
                " New edits won\u2019t go live until you unlock."),
            onUnlock && (React.createElement("button", { type: "button", onClick: onUnlock, className: "rounded-md px-3 py-1.5 text-sm font-semibold", style: {
                    flexShrink: 0,
                    background: "var(--vibes-near-black, #1a1a1a)",
                    color: "#fff",
                    border: "none",
                    cursor: "pointer",
                } }, "Unlock"))));
    }
    if (wizardActive === true || wizardError === true) {
        const showEscape = (wizardActive === true && escapeRevealed === true) || wizardError === true;
        return (React.createElement("div", { className: wizardError === true ? "rounded-md" : "rounded-md vibes-securing", role: wizardError === true ? "alert" : "status", "data-testid": "access-wizard-banner", "data-wizard-state": wizardError === true ? "error" : "running", style: {
                backgroundColor: wizardError === true ? "rgba(245,158,11,0.10)" : "rgba(59,130,246,0.08)",
                border: wizardError === true ? "1px solid rgba(245,158,11,0.4)" : "1px solid rgba(59,130,246,0.35)",
                padding: "8px 10px",
                marginBottom: 10,
                display: "flex",
                alignItems: "center",
                justifyContent: "space-between",
                gap: 8,
            } },
            React.createElement("span", { className: "text-sm", style: {
                    color: wizardError === true ? "var(--uvc-amber-text, #92400e)" : "var(--uvc-blue-text, #1d4ed8)",
                    lineHeight: 1.3,
                } }, wizardError === true ? (React.createElement(React.Fragment, null,
                React.createElement("strong", null, "Couldn\u2019t set up access rules."),
                " Your app is still live for people you approve \u2014 it didn\u2019t go public.")) : (React.createElement(React.Fragment, null,
                React.createElement("strong", null, "Securing\u2026"),
                " Setting up data access control rules before opening this app to everyone with the link."))),
            React.createElement("div", { style: { display: "flex", flexDirection: "column", gap: 6, flexShrink: 0, alignItems: "stretch" } },
                wizardError === true ? (React.createElement("button", { type: "button", onClick: onRetryWizard, className: "rounded-md px-3 py-1.5 text-sm font-semibold", style: { background: "var(--vibes-near-black, #1a1a1a)", color: "#fff", border: "none", cursor: "pointer" } }, "Retry")) : (React.createElement("button", { type: "button", onClick: onPublish, className: "rounded-md px-3 py-1.5 text-sm font-semibold", style: {
                        display: "flex",
                        alignItems: "center",
                        gap: 6,
                        background: "var(--vibes-blue, #3b82f6)",
                        color: "#fff",
                        border: "none",
                        cursor: "default",
                        opacity: 0.7,
                    } },
                    React.createElement("span", { "aria-hidden": "true", className: "vibes-securing-spinner", style: {
                            width: 12,
                            height: 12,
                            flexShrink: 0,
                            borderRadius: "50%",
                            border: "2px solid currentColor",
                            borderTopColor: "transparent",
                        } }),
                    "Securing\u2026")),
                showEscape && (React.createElement("button", { type: "button", onClick: onPublishWithoutAccess, "data-testid": "publish-without-access-controls", className: "rounded-md px-3 py-1 text-xs font-medium", style: {
                        background: "transparent",
                        color: wizardError === true ? "var(--uvc-amber-text, #92400e)" : "var(--uvc-blue-text, #1d4ed8)",
                        border: "1px solid currentColor",
                        cursor: "pointer",
                        whiteSpace: "nowrap",
                    } }, "Go public without access controls")))));
    }
    const worldwide = !pending && access === "private" && variant === undefined;
    const informational = worldwide || pending;
    const bannerStyle = informational
        ? {
            background: "rgba(59,130,246,0.08)",
            border: "1px solid rgba(59,130,246,0.35)",
            padding: "8px 10px",
            marginBottom: 10,
        }
        : {
            background: "rgba(245,158,11,0.10)",
            border: "1px solid rgba(245,158,11,0.4)",
            padding: "8px 10px",
            marginBottom: 10,
        };
    return (React.createElement("div", { className: "rounded-md", style: {
            ...bannerStyle,
            display: "flex",
            alignItems: "center",
            justifyContent: "space-between",
            gap: 8,
        } },
        React.createElement("span", { className: "text-sm", style: { color: informational ? "var(--uvc-blue-text, #1d4ed8)" : "var(--uvc-amber-text, #92400e)", lineHeight: 1.3 } }, pending ? (React.createElement("span", null, "Checking who can open this vibe\u2026")) : worldwide ? (React.createElement(React.Fragment, null,
            React.createElement("strong", null, "Private."),
            " Only people you approve can open it.")) : variant === "draft" ? (React.createElement(React.Fragment, null,
            React.createElement("strong", null, "Draft."),
            " Your latest changes aren\u2019t live yet.")) : (React.createElement(React.Fragment, null,
            React.createElement("strong", null, "Older version."),
            " This isn\u2019t what visitors currently see."))),
        !pending && onPublish && (React.createElement("button", { type: "button", onClick: onPublish, disabled: publishing, className: "rounded-md px-3 py-1.5 text-sm font-semibold", style: {
                flexShrink: 0,
                background: worldwide ? "var(--vibes-blue, #3b82f6)" : "var(--vibes-near-black, #1a1a1a)",
                color: "#fff",
                border: "none",
                cursor: publishing ? "default" : "pointer",
                opacity: publishing ? 0.6 : 1,
            } }, publishing ? "Publishing…" : worldwide ? "Publish worldwide" : variant === "draft" ? "Update" : "Publish this version"))));
}
function decorateChip(option, props) {
    const fp = props.chipFastPaths?.[option];
    if (!fp)
        return undefined;
    if (fp.jump)
        return { tone: "gold", leading: React.createElement(ChipJumpBadge, null) };
    const badge = fp.shielded ? React.createElement(ChipShieldBadge, null) : undefined;
    const aside = fp.canUnbless && props.onUnblessChip ? (React.createElement(FastPathToggle, { blessed: true, onClick: () => props.onUnblessChip?.(option) })) : fp.canBless && props.onBlessChip ? (React.createElement(FastPathToggle, { onClick: () => props.onBlessChip?.(option) })) : undefined;
    return badge || aside ? { badge, aside } : undefined;
}
function ChipJumpBadge() {
    return (React.createElement("span", { role: "img", "aria-label": "Opens another app", title: "Instant curated jump \u2014 opens another app (no sign-in)", className: "inline-flex shrink-0 text-amber-500 dark:text-amber-300" },
        React.createElement("svg", { width: "14", height: "14", viewBox: "0 0 24 24", fill: "none", stroke: "currentColor", strokeWidth: 2, strokeLinecap: "round", strokeLinejoin: "round", "aria-hidden": "true" },
            React.createElement("path", { d: "M5 12h14" }),
            React.createElement("path", { d: "M13 6l6 6-6 6" }))));
}
function ChipShieldBadge() {
    return (React.createElement("span", { role: "img", "aria-label": "Stays here", title: "Featured fast path \u2014 clicking stays here, on the same data", style: { display: "inline-flex", flexShrink: 0, color: "#16a34a" } },
        React.createElement("svg", { width: "14", height: "14", viewBox: "0 0 24 24", fill: "currentColor", stroke: "currentColor", strokeWidth: 1.5, strokeLinecap: "round", strokeLinejoin: "round", "aria-hidden": "true" },
            React.createElement("path", { d: "M12 3l7 3v5c0 4.5-3 7.5-7 9-4-1.5-7-4.5-7-9V6l7-3z" }))));
}
function FastPathToggle({ blessed, onClick }) {
    const label = blessed ? "Remove fast path (unbless)" : "Feature as fast path (bless)";
    return (React.createElement("button", { type: "button", "aria-label": label, title: label, "aria-pressed": Boolean(blessed), onClick: onClick, className: "shrink-0 rounded-md border px-2 py-1.5 text-xs font-semibold transition-colors", style: {
            display: "inline-flex",
            alignItems: "center",
            gap: 4,
            cursor: "pointer",
            whiteSpace: "nowrap",
            ...(blessed
                ? { color: "#15803d", background: "rgba(22,163,74,0.12)", borderColor: "rgba(22,163,74,0.5)" }
                : {
                    color: "var(--color-light-primary, #333)",
                    background: "var(--color-light-background-01, #eee)",
                    borderColor: "var(--color-light-decorative-01, #ddd)",
                }),
        } },
        React.createElement("svg", { width: "13", height: "13", viewBox: "0 0 24 24", fill: blessed ? "currentColor" : "none", stroke: "currentColor", strokeWidth: 2, strokeLinecap: "round", strokeLinejoin: "round", "aria-hidden": "true" },
            React.createElement("path", { d: "M12 3l7 3v5c0 4.5-3 7.5-7 9-4-1.5-7-4.5-7-9V6l7-3z" })),
        React.createElement("span", null, blessed ? "Featured" : "Feature")));
}
function NavIcon({ label, color, selected, onClick, buttonRef, badgeCount, children, }) {
    const showBadge = typeof badgeCount === "number" && badgeCount > 0;
    const badgeText = showBadge ? (badgeCount > 99 ? "99+" : String(badgeCount)) : "";
    return (React.createElement("button", { ref: buttonRef, type: "button", "aria-label": showBadge ? `${label} (${badgeCount})` : label, title: label, onClick: onClick, style: {
            position: "relative",
            width: "var(--uvc-nav-size, 38px)",
            height: "var(--uvc-nav-size, 38px)",
            flexShrink: 0,
            boxSizing: "border-box",
            borderRadius: "50%",
            background: "transparent",
            display: "flex",
            alignItems: "center",
            justifyContent: "center",
            color,
            fontSize: 16,
            cursor: "pointer",
            border: `1px solid ${color}`,
            boxShadow: selected ? `0 0 0 3px ${color}` : "none",
        } },
        children,
        showBadge ? (React.createElement("span", { "aria-hidden": "true", style: {
                position: "absolute",
                top: -4,
                right: -4,
                minWidth: 16,
                height: 16,
                padding: "0 4px",
                boxSizing: "border-box",
                borderRadius: 9999,
                background: "var(--vibes-near-black, #1a1a1a)",
                color: "#fff",
                border: "1px solid #fff",
                fontSize: 10,
                lineHeight: "14px",
                fontWeight: 700,
                display: "flex",
                alignItems: "center",
                justifyContent: "center",
            } }, badgeText)) : null));
}
function PendingMessages({ messages }) {
    if (messages.length === 0)
        return null;
    return (React.createElement("div", { role: "status", "data-testid": "pending-messages", style: { flexShrink: 0, padding: "6px 0 2px", display: "flex", flexDirection: "column", gap: 6 } },
        messages.map((message, i) => (React.createElement("div", { key: `${i}-${message}`, style: { display: "flex", justifyContent: "flex-end" } },
            React.createElement("div", { className: "text-light-primary dark:text-dark-primary", style: {
                    maxWidth: "85%",
                    borderRadius: 10,
                    background: "rgba(139,92,246,0.10)",
                    border: "1px solid rgba(139,92,246,0.28)",
                    padding: "6px 9px",
                    fontSize: 13,
                    lineHeight: 1.35,
                    overflowWrap: "anywhere",
                } }, message)))),
        React.createElement("div", { className: "text-light-secondary dark:text-dark-secondary", style: { fontSize: 11, textAlign: "right" } }, "sends when this build finishes")));
}
const QUEUE_FLUSH_RETRY_MS = 400;
const QUEUE_FLUSH_MAX_ATTEMPTS = 25;
const OTHER_ROW_BUTTON_STYLE = {
    width: 30,
    height: 30,
    flexShrink: 0,
    borderRadius: 8,
    color: "#fff",
    fontSize: 17,
    fontWeight: "bold",
    lineHeight: 1,
    cursor: "pointer",
    display: "flex",
    alignItems: "center",
    justifyContent: "center",
};
function WorkGlyph() {
    return (React.createElement("svg", { width: "17", height: "17", viewBox: "0 0 24 24", fill: "#fff", "aria-hidden": "true" },
        React.createElement("path", { d: "M21.7 5.6l-3.6 3.6-3.3-3.3 3.6-3.6a6 6 0 0 0-7.7 7.3l-8 8a2.1 2.1 0 0 0 3 3l8-8a6 6 0 0 0 7.3-7.7z" })));
}
function AutoGrowTextarea({ value, onValueChange, ariaLabel, }) {
    const ref = useRef(null);
    const measure = useCallback(() => {
        const ta = ref.current;
        if (ta === null)
            return;
        ta.style.height = "auto";
        ta.style.height = `${Math.min(ta.scrollHeight, 120)}px`;
    }, []);
    useEffect(() => {
        measure();
    }, [value, measure]);
    useEffect(() => {
        const ta = ref.current;
        if (ta === null || typeof ResizeObserver === "undefined")
            return;
        let lastWidth = ta.clientWidth;
        const ro = new ResizeObserver(() => {
            if (ta.clientWidth !== lastWidth) {
                lastWidth = ta.clientWidth;
                measure();
            }
        });
        ro.observe(ta);
        return () => ro.disconnect();
    }, [measure]);
    return (React.createElement("textarea", { ref: ref, value: value, rows: 1, onChange: (e) => onValueChange(e.target.value), onKeyDown: (e) => {
            if (e.key === "Enter" && !e.shiftKey && !e.nativeEvent.isComposing && e.nativeEvent.keyCode !== 229) {
                e.preventDefault();
                e.currentTarget.form?.requestSubmit();
            }
        }, "aria-label": ariaLabel, className: "w-full bg-transparent text-sm text-light-primary dark:text-dark-primary outline-none", style: { resize: "none", overflowY: "auto", maxHeight: 120, lineHeight: "20px" } }));
}
function OtherRow({ value, onValueChange, queued, onQueuedChange, onSubmitOther, onSubmitWork, generating, liveDuringBuild, canSendNow, onStop, attachmentsCtl, }) {
    const showStop = Boolean(generating) && value.trim() === "";
    const submitDisabled = attachmentsCtl.busy || (Boolean(generating) && attachmentsCtl.readyAttachments.length > 0);
    const sendNow = liveDuringBuild === true && canSendNow === true && queued.length === 0 && attachmentsCtl.readyAttachments.length === 0;
    const hasUnsentContent = value.trim() !== "" || attachmentsCtl.readyAttachments.length > 0;
    return (React.createElement(React.Fragment, null,
        React.createElement(AttachmentChips, { chips: attachmentsCtl.attachments, onRemove: attachmentsCtl.remove }),
        React.createElement("form", { onSubmit: (e) => {
                e.preventDefault();
                const text = value.trim();
                const files = attachmentsCtl.readyAttachments;
                if (!text && files.length === 0)
                    return;
                if (attachmentsCtl.busy)
                    return;
                if (generating && !sendNow) {
                    if (files.length > 0)
                        return;
                    if (!text)
                        return;
                    onQueuedChange([...queued, text]);
                }
                else {
                    const sent = onSubmitOther?.(text || (files.length === 1 ? "Use the attached file." : "Use the attached files."), files.length > 0 ? files : undefined);
                    if (sent === false)
                        return;
                    attachmentsCtl.clear();
                }
                onValueChange("");
            }, style: { display: "flex", flexDirection: "column", gap: 6, marginTop: 6 }, className: "vibes-edit-field py-2 pl-3 pr-2" },
            React.createElement("div", { style: { position: "relative", minWidth: 0, display: "flex" } },
                React.createElement(AutoGrowTextarea, { value: value, onValueChange: onValueChange, ariaLabel: "Change the app" }),
                value === "" && (React.createElement("span", { "aria-hidden": true, className: "text-sm text-light-secondary dark:text-dark-secondary", style: {
                        position: "absolute",
                        left: 0,
                        top: 0,
                        bottom: 0,
                        display: "flex",
                        alignItems: "center",
                        pointerEvents: "none",
                        whiteSpace: "nowrap",
                    } },
                    "Change the app with ",
                    React.createElement("s", { style: { margin: "0 0.25em" } }, "magic"),
                    " words\u2026"))),
            React.createElement("div", { style: { display: "flex", alignItems: "center", gap: 8 } },
                React.createElement(AttachmentButton, { onFiles: attachmentsCtl.add }),
                !generating && onSubmitWork !== undefined && (React.createElement("button", { type: "button", "aria-label": "Work on it", title: "Takes a few minutes \u2014 reads your app's files and makes one improvement", className: "vibes-edit-btn", disabled: value.trim() === "" || attachmentsCtl.busy, onClick: () => {
                        const text = value.trim();
                        if (text === "" || attachmentsCtl.busy)
                            return;
                        const files = attachmentsCtl.readyAttachments;
                        if (onSubmitWork(text, files.length > 0 ? files : undefined) === false)
                            return;
                        attachmentsCtl.clear();
                        onValueChange("");
                    }, style: {
                        ...OTHER_ROW_BUTTON_STYLE,
                        background: "var(--vibes-amber, #f59e0b)",
                        opacity: value.trim() === "" || attachmentsCtl.busy ? 0.5 : 1,
                        cursor: value.trim() === "" || attachmentsCtl.busy ? "default" : "pointer",
                    } },
                    React.createElement(WorkGlyph, null))),
                React.createElement("div", { style: { flex: 1 } }),
                showStop ? (React.createElement("button", { type: "button", "aria-label": "Stop generating", className: "vibes-edit-btn", onClick: () => {
                        if (queued.length > 0) {
                            onValueChange(queued.join("\n"));
                            onQueuedChange([]);
                        }
                        onStop?.();
                    }, style: { ...OTHER_ROW_BUTTON_STYLE, background: "var(--vibes-near-black, #1a1a1a)" } },
                    React.createElement("span", { "aria-hidden": "true", style: { width: 10, height: 10, borderRadius: 2, background: "#fff", display: "block" } }))) : (React.createElement("button", { type: "submit", "aria-label": generating && !sendNow ? "Queue message" : "Submit change", disabled: submitDisabled, className: `vibes-edit-btn${hasUnsentContent && !submitDisabled ? " vibes-submit-pulse" : ""}`, title: Boolean(generating) && attachmentsCtl.readyAttachments.length > 0
                        ? "Files send with the next change — wait for this one to finish"
                        : generating && !sendNow
                            ? "Sends when the current change finishes"
                            : undefined, style: {
                        ...OTHER_ROW_BUTTON_STYLE,
                        background: "var(--vibes-blue, #3b82f6)",
                        opacity: submitDisabled ? 0.5 : 1,
                        cursor: submitDisabled ? "default" : "pointer",
                    } },
                    React.createElement("span", { "aria-hidden": "true" }, "\u2191"))))),
        liveDuringBuild !== true && Boolean(generating) && queued.length > 0 && (React.createElement("div", { role: "status", className: "text-xs text-light-secondary dark:text-dark-secondary", style: { marginTop: 6, lineHeight: 1.4 } },
            React.createElement("div", null,
                queued.length === 1 ? "1 message queued" : `${queued.length} messages queued`,
                " \u2014 sends when this one finishes (Stop returns it to the box)."),
            React.createElement("ol", { style: {
                    margin: "3px 0 0",
                    padding: 0,
                    listStyle: "none",
                    maxHeight: 68,
                    overflowY: "auto",
                    overscrollBehavior: "contain",
                } }, queued.map((message, i) => (React.createElement("li", { key: `${i}-${message}`, style: { display: "flex", gap: 5, marginTop: 1 } },
                React.createElement("span", { "aria-hidden": "true", style: { opacity: 0.6 } },
                    i + 1,
                    "."),
                React.createElement("span", { style: { minWidth: 0, overflowWrap: "anywhere" } }, message)))))))));
}
//# sourceMappingURL=UnifiedVibeCard.js.map