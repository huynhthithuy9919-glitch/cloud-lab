import React from "react";
import { BrutalistCardStyles } from "./BrutalistCard.styles.js";
export const BrutalistCard = React.forwardRef(({ children, variant = "default", size = "md", messageType, style, className, ...divProps }, ref) => {
    const platform = messageType === "platform";
    const user = messageType === "user";
    const cardStyle = {
        ...BrutalistCardStyles.getCardStyle(variant, size, messageType),
        background: platform
            ? "linear-gradient(rgba(59, 130, 246, 0.14), rgba(59, 130, 246, 0.14)), var(--vibes-card-bg)"
            : user
                ? "linear-gradient(rgba(0, 0, 0, 0.08), rgba(0, 0, 0, 0.08)), var(--vibes-card-bg)"
                : "var(--vibes-card-bg)",
        color: "var(--vibes-card-text)",
        border: platform ? "3px solid rgba(59, 130, 246, 0.55)" : "3px solid var(--vibes-card-border)",
        ...style,
    };
    return (React.createElement("div", { ref: ref, style: cardStyle, className: className, ...divProps }, children));
});
BrutalistCard.displayName = "BrutalistCard";
//# sourceMappingURL=BrutalistCard.js.map