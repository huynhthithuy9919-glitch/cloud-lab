import React, { useState } from "react";
import { HOME_BRAND_FONT } from "./homeBrandFont.js";
function faqItems(ownerHandle) {
    const owner = ownerHandle ? `@${ownerHandle}` : "the maker";
    const OwnerCap = ownerHandle ? `@${ownerHandle}` : "The maker";
    return [
        {
            q: "What is a vibe?",
            a: "A vibe is a small web app someone made by describing it in plain words on Vibes DIY. It's real software — a live app with real data and a shareable link, not a mockup.",
        },
        {
            q: "Who made this vibe?",
            a: `${OwnerCap} built it and runs it — the app, its sharing rules, and its updates all come from them. Vibes DIY is the platform that hosts it.`,
        },
        {
            q: "Who can see what I type into it?",
            a: `That depends on how ${owner} built it — some apps share everything with every user, others keep what you enter private to you or people you pick. The maker can't casually browse your data, but they can switch into an admin mode to inspect their own app (normally for debugging), so the honest question is whether you trust them. If you want certainty, the app's code is open to read — or remix it and take the ball home: your copy, your data, you're the owner.`,
        },
        {
            q: "Do I need an account?",
            a: "Not to use an open vibe like this one. You'll need a free account to save things under your own name, join a private vibe, or make your own copy.",
        },
        {
            q: "Can I change it myself?",
            a: "Yes — that's called remixing. You get your own copy under your own name and can reshape it however you like, by describing changes or editing the code directly.",
        },
        {
            q: "Will remixing affect the original?",
            a: "No. Your copy starts with fresh, empty data, and everything you change belongs to you. The original app and its data are untouched.",
        },
        {
            q: "Does it cost anything?",
            a: "Using vibes is free. Building and editing with AI uses credits — every account gets free credits each month, and you don't need a credit card to start.",
        },
        {
            q: "Can I use this app with friends?",
            a: "Yes — send them this link. On an open vibe they're in instantly; on a more private one they may need to sign in and request or accept an invite first, depending on how it's shared. Data syncs live between users automatically — but which data each user can see is up to the app.",
        },
    ];
}
export function FaqView(props) {
    const items = faqItems(props.ownerHandle);
    const [open, setOpen] = useState(() => items.map(() => false));
    function toggle(i) {
        setOpen((prev) => prev.map((v, idx) => (idx === i ? !v : v)));
    }
    return (React.createElement("div", { "data-testid": "faq-view", style: { padding: "4px 0 8px" } },
        React.createElement("div", { className: "text-light-secondary dark:text-dark-secondary", style: { fontSize: 12, fontWeight: 600, margin: "0 2px 8px" } }, "Questions & answers"),
        React.createElement("div", { style: { display: "flex", flexDirection: "column" } }, items.map((item, i) => {
            const isOpen = open[i];
            const panelId = `faq-answer-${i}`;
            return (React.createElement("div", { key: item.q, style: { borderTop: i === 0 ? "none" : "1px solid var(--color-light-decorative-00, #e5e5e5)" } },
                React.createElement("button", { type: "button", "aria-expanded": isOpen, "aria-controls": panelId, onClick: () => toggle(i), className: "text-light-primary dark:text-dark-primary", style: {
                        display: "flex",
                        alignItems: "center",
                        gap: 8,
                        width: "100%",
                        background: "none",
                        border: "none",
                        padding: "10px 2px",
                        cursor: "pointer",
                        textAlign: "left",
                        font: "inherit",
                    } },
                    React.createElement("span", { style: { flex: 1, fontSize: 14, fontWeight: 600, lineHeight: 1.3, fontFamily: HOME_BRAND_FONT } }, item.q),
                    React.createElement("span", { "aria-hidden": true, className: "text-light-secondary dark:text-dark-secondary", style: {
                            flexShrink: 0,
                            display: "inline-flex",
                            transition: "transform 0.15s ease",
                            transform: isOpen ? "rotate(180deg)" : "none",
                        } },
                        React.createElement("svg", { width: "16", height: "16", viewBox: "0 0 24 24", fill: "none", stroke: "currentColor", strokeWidth: 2.2, strokeLinecap: "round", strokeLinejoin: "round" },
                            React.createElement("path", { d: "M6 9l6 6 6-6" })))),
                isOpen && (React.createElement("p", { id: panelId, className: "text-light-primary dark:text-dark-primary", style: { margin: 0, padding: "0 2px 12px", fontSize: 13, lineHeight: 1.5, opacity: 0.85 } }, item.a))));
        })),
        props.onAbout && (React.createElement("div", { style: { marginTop: 12, padding: "0 2px" } },
            React.createElement("button", { type: "button", onClick: props.onAbout, style: {
                    display: "inline-flex",
                    alignItems: "center",
                    gap: 4,
                    background: "none",
                    border: "none",
                    padding: 0,
                    cursor: "pointer",
                    fontSize: 13,
                    fontWeight: 600,
                    color: "var(--vibes-blue, #3b82f6)",
                } },
                "More about Vibes DIY",
                React.createElement("span", { "aria-hidden": true }, "\u2192"))))));
}
//# sourceMappingURL=FaqView.js.map