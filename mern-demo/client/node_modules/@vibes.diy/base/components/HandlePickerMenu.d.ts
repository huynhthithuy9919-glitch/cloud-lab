import React from "react";
import { toRFC2822_32ByteLength } from "@vibes.diy/vibe-types";
export interface HandleOption {
    readonly slug: string;
    readonly displayName?: string;
    readonly avatarUrl?: string;
}
export interface HandlePickerMenuProps {
    readonly handles: readonly HandleOption[];
    readonly activeSlug?: string;
    readonly onSelect?: (slug: string) => void;
    readonly onNewHandle?: (handle?: string) => void;
    readonly busy?: boolean;
    readonly placement?: "up" | "down";
    readonly onResetToDefault?: () => void;
    readonly resetToDefaultLabel?: string;
    readonly onLogout?: () => void;
    readonly style?: React.CSSProperties;
}
export declare const sanitizeHandle: typeof toRFC2822_32ByteLength;
export declare function HandlePickerMenu({ handles, activeSlug, onSelect, onNewHandle, busy, placement, onResetToDefault, resetToDefaultLabel, onLogout, style, }: HandlePickerMenuProps): React.ReactElement;
