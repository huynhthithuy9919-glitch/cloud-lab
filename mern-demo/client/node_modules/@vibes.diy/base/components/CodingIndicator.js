import * as React from "react";
const SWEEP_CLASS = "vibes-coding-indicator-sweep";
const RING = "var(--uvc-purple-text, var(--accent, light-dark(#7c3aed, #c4b5fd)))";
const CSS = `
.${SWEEP_CLASS} {
  position: absolute;
  inset: 0;
  border-radius: inherit;
  pointer-events: none;
}
.${SWEEP_CLASS}-move {
  position: absolute;
  inset: 0;
  background-image: linear-gradient(100deg, transparent 35%, rgba(255,255,255,0.85) 50%, transparent 65%);
  background-size: 260% 100%;
  animation: vibesCodingIndicatorSweep 1.5s linear infinite;
}
@keyframes vibesCodingIndicatorSweep {
  0% { background-position: 180% 0; }
  100% { background-position: -180% 0; }
}
@media (prefers-reduced-motion: reduce) {
  /* No sweep at all — and the ring underneath is then the ONLY thing telling a
     building header from an idle one, which is why it is not decoration. */
  .${SWEEP_CLASS}-move { animation: none; background-position: 50% 0; opacity: 0.55; }
}
`;
export function CodingIndicator({ active, label = "coding" }) {
    if (!active)
        return null;
    return (React.createElement("span", { "data-testid": "coding-indicator", className: SWEEP_CLASS, "aria-hidden": false },
        React.createElement("style", null, CSS),
        React.createElement("span", { "aria-hidden": true, style: {
                position: "absolute",
                inset: 0,
                borderRadius: "inherit",
                boxShadow: `inset 0 0 0 2px ${RING}`,
            } }),
        React.createElement("span", { "aria-hidden": true, className: `${SWEEP_CLASS}-move` }),
        React.createElement("span", { role: "status", style: {
                position: "absolute",
                width: 1,
                height: 1,
                margin: -1,
                padding: 0,
                overflow: "hidden",
                clip: "rect(0 0 0 0)",
                clipPath: "inset(50%)",
                whiteSpace: "nowrap",
                border: 0,
            } }, label)));
}
//# sourceMappingURL=CodingIndicator.js.map