import * as React from "react";
import { combineClasses, defaultClasses } from "../utils/style-utils.js";
import { imgVibesStyles } from "../utils/styles.js";
export function PromptBar({ promptText, editedPrompt, setEditedPrompt, handlePromptEdit, classes = defaultClasses, }) {
    return (React.createElement("div", { style: imgVibesStyles.topLine },
        React.createElement("div", { className: combineClasses(classes.prompt), style: imgVibesStyles.prompt }, editedPrompt !== null ? (React.createElement("input", { type: "text", value: editedPrompt, onChange: (e) => setEditedPrompt(e.target.value), onKeyDown: (e) => {
                if (e.key === "Enter") {
                    e.preventDefault();
                    handlePromptEdit(editedPrompt);
                }
                else if (e.key === "Escape") {
                    setEditedPrompt(null);
                }
            }, autoFocus: true, style: {
                ...imgVibesStyles.promptInput,
                ...imgVibesStyles.promptInputEditMode,
            }, "aria-label": "Edit prompt" })) : (React.createElement("div", { className: "imggen-prompt-text", onClick: () => {
                setEditedPrompt(promptText);
            }, style: {
                ...imgVibesStyles.promptText,
                ...imgVibesStyles.truncate,
            }, title: "Click to edit prompt" }, promptText)))));
}
//# sourceMappingURL=PromptBar.js.map