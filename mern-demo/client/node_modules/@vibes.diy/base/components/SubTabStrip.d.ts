import * as React from "react";
export declare function SubTabStrip({ tabs, active, onSelect, ariaLabel, }: {
    tabs: readonly {
        key: string;
        label: string;
        icon?: React.ReactNode;
    }[];
    active: string;
    onSelect: (key: string) => void;
    ariaLabel: string;
}): React.JSX.Element;
