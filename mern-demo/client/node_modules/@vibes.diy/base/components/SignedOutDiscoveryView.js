import * as React from "react";
import { useState } from "react";
import { SubTabStrip } from "./SubTabStrip.js";
import { RecommendedView } from "./RecommendedView.js";
import { AccountSignInView } from "./AccountSignInView.js";
export function SignedOutDiscoveryView(props) {
    const [tab, setTab] = useState(props.initialTab ?? "account");
    return (React.createElement("div", { "data-testid": "signed-out-discovery-view" },
        React.createElement(SubTabStrip, { tabs: [
                { key: "recommended", label: "Recommended" },
                { key: "account", label: "Your account" },
            ], active: tab, onSelect: (k) => setTab(k), ariaLabel: "Sign in or browse recommended vibes" }),
        tab === "account" ? (React.createElement(AccountSignInView, { onSignIn: props.onSignIn })) : (React.createElement(RecommendedView, { items: props.items, loading: props.loading, label: "", onOpenVibe: props.onOpenVibe }))));
}
//# sourceMappingURL=SignedOutDiscoveryView.js.map