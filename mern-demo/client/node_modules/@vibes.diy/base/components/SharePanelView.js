import React from "react";
import { ViewerTagView } from "./ViewerTagView.js";
import { QrCode } from "./QrCode.js";
import { SubTabStrip } from "./SubTabStrip.js";
export const POSTURE_NOTICE = {
    open: "Reachable by anyone; documents governed by your app's rules.",
    gated: "Anyone can open it; the people you approve can write.",
    private: "Only approved members can open this vibe.",
};
export const ANON_WRITE_NOTICE = {
    explicit: "Anonymous submissions: on — set when this app was published for anonymous posting. People who aren’t signed in can submit into the shared app; your app’s rules still govern each document.",
    legacy: "Anonymous submissions: on — grandfathered from before this setting existed. People who aren’t signed in can submit into the shared app; your app’s rules still govern each document.",
};
const ANON_WRITE_REVOKE_NOTE = "Turning it off stops new anonymous submissions; entries already submitted stay.";
function subjectsLabel(subjects) {
    if (subjects === undefined)
        return "the app's posture";
    if (subjects.length === 0)
        return "you only";
    return subjects.join(", ");
}
function relativeTime(iso) {
    const d = new Date(iso);
    if (Number.isNaN(d.getTime()))
        return "";
    const diff = Date.now() - d.getTime();
    const min = 60 * 1000;
    const hour = 60 * min;
    const day = 24 * hour;
    if (diff < min)
        return "just now";
    if (diff < hour)
        return `${Math.floor(diff / min)}m ago`;
    if (diff < day)
        return `${Math.floor(diff / hour)}h ago`;
    if (diff < 2 * day)
        return "yesterday";
    if (diff < 7 * day)
        return `${Math.floor(diff / day)} days ago`;
    return d.toLocaleDateString(undefined, { month: "short", day: "numeric" });
}
function roleLabel(role) {
    switch (role) {
        case "viewer":
            return "reader";
        case "submitter":
            return "submitter";
        default:
            return role;
    }
}
const TAB_PANE_MIN_HEIGHT = 220;
const TAB_PANE_MAX_HEIGHT = "min(420px, 44vh)";
const ACCESS_NOTICE_MIN_HEIGHT = 62;
const LINK_RADIUS = 8;
const LINK_GAP = 12;
const ROSTER_TAG_STYLE = {
    background: "var(--color-light-background-01, #eee)",
    border: "1px solid var(--color-light-decorative-01, #ddd)",
    color: "var(--color-light-primary, #333)",
    fontSize: 13,
    padding: "3px 10px 3px 4px",
};
export function SharePanelView({ url, copied, onCopy, viewer, members = [], access = "public", onChangeAccess, accessPending, posture, envelopeRows, anonWrite, onRevokeAnonWrite, anonWritePending, onSelectMember, requestsSlot, notifications, notificationsLoading, notificationsError, onSelectNotification, commentsSlot, developersSlot, className, }) {
    const isAuthor = viewer === "author";
    const roster = [...members].sort((a, b) => Number(b.role === "owner") - Number(a.role === "owner"));
    const tabs = [];
    tabs.push({ key: "qr", label: "Link" });
    if (commentsSlot !== undefined)
        tabs.push({ key: "comments", label: "Comments" });
    if (notifications !== undefined)
        tabs.push({ key: "notifications", label: "Notifications" });
    tabs.push({ key: "who", label: "Members" });
    const [picked, setPicked] = React.useState(undefined);
    const defaultKey = "qr";
    const activeTab = picked !== undefined && tabs.some((t) => t.key === picked) ? picked : defaultKey;
    return (React.createElement("div", { className: `text-light-primary dark:text-dark-primary ${className ?? ""}`, style: { display: "flex", flexDirection: "column" } },
        React.createElement(SubTabStrip, { ariaLabel: "Share", tabs: tabs, active: activeTab, onSelect: setPicked }),
        React.createElement("div", { "data-testid": "share-tab-content", style: {
                minHeight: TAB_PANE_MIN_HEIGHT,
                maxHeight: TAB_PANE_MAX_HEIGHT,
                overflowY: "auto",
                paddingTop: 14,
                display: "flex",
                flexDirection: "column",
                gap: 14,
            } },
            activeTab === "qr" && (React.createElement("div", { style: { display: "flex", flexDirection: "column", gap: LINK_GAP, alignItems: "center" } },
                React.createElement("div", { className: "border border-light-decorative-01 dark:border-dark-decorative-01", style: { background: "#ffffff", borderRadius: LINK_RADIUS, padding: 8, lineHeight: 0 } },
                    React.createElement(QrCode, { value: url, size: 132, title: "QR code \u2014 scan to open this vibe" })),
                React.createElement("div", { style: { display: "flex", flexDirection: "column", gap: 4, alignItems: "center", width: "100%" } },
                    React.createElement("p", { className: "text-light-primary dark:text-dark-primary", style: { fontSize: 12, fontWeight: 600, lineHeight: 1.3, textAlign: "center", margin: 0 } }, "Send this link to a friend"),
                    React.createElement("span", { className: "text-xs text-light-secondary dark:text-dark-secondary", style: { textAlign: "center", overflowWrap: "anywhere", wordBreak: "break-word" } }, url)),
                React.createElement("button", { type: "button", onClick: onCopy, "aria-label": "Copy link", className: "w-full bg-blue-500 text-sm font-bold text-white transition-colors hover:bg-blue-600", style: { borderRadius: LINK_RADIUS, padding: "10px 12px", border: "none" } }, copied ? "Copied" : "Copy link"))),
            activeTab === "comments" && commentsSlot !== undefined ? (commentsSlot) : activeTab === "who" ? (React.createElement(React.Fragment, null,
                isAuthor ? (React.createElement("div", { "data-testid": "share-access-notice", style: { minHeight: ACCESS_NOTICE_MIN_HEIGHT } },
                    React.createElement("p", { className: "text-xs text-light-secondary dark:text-dark-secondary", style: { marginBottom: 6 } }, "Who can open it"),
                    React.createElement("div", { role: "radiogroup", "aria-label": "Who can open it", style: { display: "flex", gap: 6 } }, [
                        { value: "request", label: "People you approve" },
                        { value: "public", label: "Anyone with the link" },
                    ].map((opt) => {
                        const active = !accessPending && opt.value === access;
                        const publicAccent = opt.value === "public"
                            ? active
                                ? { background: "var(--vibes-blue, #3b82f6)", borderColor: "var(--vibes-blue, #3b82f6)", color: "#fff" }
                                : { borderColor: "rgba(59,130,246,0.55)", color: "var(--vibes-blue, #3b82f6)" }
                            : undefined;
                        return (React.createElement("button", { key: opt.value, type: "button", role: "radio", "aria-checked": active, disabled: accessPending, onClick: () => onChangeAccess?.(opt.value), style: publicAccent, className: "flex-1 rounded-md border px-3 py-2 text-center text-xs font-medium transition-colors " +
                                (accessPending ? "cursor-default opacity-60 " : "") +
                                (active
                                    ? "border-[#1a1a1a] bg-light-background-01 dark:bg-dark-background-01"
                                    : "border-light-decorative-01 dark:border-dark-decorative-01 hover:bg-light-background-01 dark:hover:bg-dark-background-01") }, opt.label));
                    })),
                    posture !== undefined && accessPending !== true && (React.createElement("p", { "data-testid": "share-posture-notice", className: "text-xs text-light-secondary dark:text-dark-secondary", style: { marginTop: 8 } }, POSTURE_NOTICE[posture])),
                    (anonWrite === "explicit" || anonWrite === "legacy") && accessPending !== true && (React.createElement("div", { "data-testid": "share-anon-write-notice", style: { marginTop: 8, display: "flex", flexDirection: "column", gap: 6 } },
                        React.createElement("p", { className: "text-xs text-light-secondary dark:text-dark-secondary", style: { margin: 0 } }, ANON_WRITE_NOTICE[anonWrite]),
                        onRevokeAnonWrite !== undefined && (React.createElement(React.Fragment, null,
                            React.createElement("p", { className: "text-xs text-light-secondary dark:text-dark-secondary", style: { margin: 0, opacity: 0.8 } }, ANON_WRITE_REVOKE_NOTE),
                            React.createElement("button", { type: "button", onClick: onRevokeAnonWrite, "aria-label": "Turn off anonymous submissions", disabled: anonWritePending === true, className: "self-start rounded-md border px-3 py-1.5 text-xs font-medium transition-colors " +
                                    "border-light-decorative-01 dark:border-dark-decorative-01 " +
                                    (anonWritePending === true
                                        ? "cursor-default opacity-60"
                                        : "hover:bg-light-background-01 dark:hover:bg-dark-background-01") }, anonWritePending === true ? "Turning off…" : "Turn off anonymous submissions"))))),
                    envelopeRows !== undefined && envelopeRows.length > 0 && (React.createElement("ul", { "data-testid": "share-envelope-rows", style: { marginTop: 8, listStyle: "none", padding: 0 } }, envelopeRows.map((row) => (React.createElement("li", { key: row.dbName, className: "text-xs text-light-secondary dark:text-dark-secondary" },
                        React.createElement("span", { style: { fontWeight: 600 } }, row.dbName),
                        " — reads: ",
                        subjectsLabel(row.effective.read),
                        ", writes: ",
                        subjectsLabel(row.effective.write),
                        " (",
                        row.provenance === "platform-default" ? "platform default" : "your pin",
                        row.absorbed === true && row.provenance === "your-pin" ? "; not restricting anything right now" : "",
                        ")"))))))) : null,
                React.createElement("div", null,
                    React.createElement("p", { className: "text-xs text-light-secondary dark:text-dark-secondary", style: { marginBottom: 6 } }, isAuthor ? "In this vibe" : access === "public" ? "People who’ve written here" : "People with access"),
                    roster.length === 0 ? (React.createElement("p", { className: "text-xs text-light-secondary dark:text-dark-secondary" }, access === "public" ? "No one’s written here yet." : "No one else has access yet.")) : (React.createElement("div", { style: { display: "flex", flexWrap: "wrap", gap: 6 } }, roster.map((m) => {
                        const tag = (React.createElement(ViewerTagView, { slug: m.handle, displayName: `@${m.handle}`, avatarUrl: m.avatarUrl, trailing: m.role ? (React.createElement("span", { style: { fontSize: 11, opacity: 0.6, marginLeft: 2 } }, roleLabel(m.role))) : undefined, style: ROSTER_TAG_STYLE }));
                        return isAuthor ? (React.createElement("button", { key: m.handle, type: "button", "aria-label": `Manage @${m.handle}`, onClick: () => onSelectMember?.(m), style: { background: "none", border: "none", padding: 0, cursor: "pointer" } }, tag)) : (React.createElement("span", { key: m.handle }, tag));
                    })))),
                isAuthor && requestsSlot !== undefined ? requestsSlot : null,
                isAuthor && developersSlot !== undefined ? developersSlot : null)) : activeTab === "notifications" && notifications !== undefined ? (React.createElement("div", null, notificationsError === true ? (React.createElement("p", { className: "text-xs text-light-secondary dark:text-dark-secondary" }, "Couldn\u2019t load notifications for this vibe.")) : notificationsLoading === true && notifications.length === 0 ? (React.createElement("p", { className: "text-xs text-light-secondary dark:text-dark-secondary" }, "Loading notifications\u2026")) : notifications.length === 0 ? (React.createElement("p", { className: "text-xs text-light-secondary dark:text-dark-secondary" }, "No notifications for this vibe yet.")) : (React.createElement("div", { className: "rounded-md border border-light-decorative-01 dark:border-dark-decorative-01", style: { display: "flex", flexDirection: "column" } }, notifications.map((n) => {
                const isUnread = n.unread === true;
                const interactive = onSelectNotification !== undefined;
                const inner = (React.createElement(React.Fragment, null,
                    React.createElement("span", { "aria-hidden": "true", className: `mt-1.5 h-2 w-2 shrink-0 rounded-full ${isUnread ? "bg-blue-500" : "bg-transparent"}` }),
                    React.createElement("span", { className: "min-w-0 flex-1" },
                        React.createElement("span", { className: `block break-words text-sm ${isUnread ? "font-semibold" : ""}` }, n.body),
                        React.createElement("span", { className: "mt-0.5 block text-xs text-light-secondary dark:text-dark-secondary" }, relativeTime(n.created)))));
                const rowClass = "flex w-full items-start gap-3 px-3 py-2.5 text-left border-b border-light-decorative-01 dark:border-dark-decorative-01 last:border-b-0";
                if (n.actionSlot !== undefined) {
                    return (React.createElement("div", { key: n.id, className: `${rowClass} items-center` },
                        interactive ? (React.createElement("button", { type: "button", onClick: () => onSelectNotification?.(n.id), className: "flex min-w-0 flex-1 items-start gap-3 text-left" }, inner)) : (React.createElement("span", { className: "flex min-w-0 flex-1 items-start gap-3" }, inner)),
                        React.createElement("span", { className: "shrink-0" }, n.actionSlot)));
                }
                return interactive ? (React.createElement("button", { key: n.id, type: "button", onClick: () => onSelectNotification?.(n.id), className: `${rowClass} transition-colors hover:bg-light-background-01 dark:hover:bg-dark-background-01` }, inner)) : (React.createElement("div", { key: n.id, className: rowClass }, inner));
            }))))) : null)));
}
//# sourceMappingURL=SharePanelView.js.map