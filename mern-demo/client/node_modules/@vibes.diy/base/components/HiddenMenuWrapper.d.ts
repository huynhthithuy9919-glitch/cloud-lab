import React from "react";
export interface HiddenMenuWrapperProps {
    children: React.ReactNode;
    menuContent: React.ReactNode;
    triggerBounce?: boolean;
    showVibesSwitch?: boolean;
}
export declare function HiddenMenuWrapper({ children, menuContent, triggerBounce, showVibesSwitch }: HiddenMenuWrapperProps): React.JSX.Element;
