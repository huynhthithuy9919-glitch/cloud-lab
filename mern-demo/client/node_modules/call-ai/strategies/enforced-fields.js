const TOOL_MODE_PRESERVED_KEYS = new Set(["type", "properties", "required", "additionalProperties"]);
const HARMLESS_METADATA_KEYS = new Set(["name", "title", "description"]);
const COMPOSITION_KEYS = ["oneOf", "anyOf", "allOf", "not"];
function hasNonEmptyProperties(schema) {
    const properties = schema.properties;
    return properties !== null && typeof properties === "object" && Object.keys(properties).length > 0;
}
function isRepresentable(strategy, schema) {
    const topLevelType = schema.type;
    if (topLevelType !== undefined && topLevelType !== "object")
        return false;
    if (strategy.strategy === "json_schema") {
        return !(COMPOSITION_KEYS.some((key) => schema[key] !== undefined) && hasNonEmptyProperties(schema) === false);
    }
    return Object.keys(schema).every((key) => TOOL_MODE_PRESERVED_KEYS.has(key) || HARMLESS_METADATA_KEYS.has(key));
}
function namedFunction(v) {
    if (v === null || typeof v !== "object")
        return undefined;
    const fn = v.function;
    if (fn === null || typeof fn !== "object")
        return undefined;
    return typeof fn.name === "string" && fn.name.length > 0
        ? fn
        : undefined;
}
function isNamedToolChoice(v) {
    return namedFunction(v) !== undefined;
}
function isNamedToolDefinition(v) {
    const fn = namedFunction(v);
    return fn !== undefined && fn.parameters !== null && typeof fn.parameters === "object";
}
function validatePrepared(prepared) {
    if (prepared === null || typeof prepared !== "object" || Array.isArray(prepared))
        return undefined;
    const record = prepared;
    const { response_format, tools, tool_choice } = record;
    if (response_format !== undefined && (response_format === null || typeof response_format !== "object"))
        return undefined;
    if (tools !== undefined && (Array.isArray(tools) === false || tools.length === 0 || tools.every(isNamedToolDefinition) === false))
        return undefined;
    if (tool_choice !== undefined && isNamedToolChoice(tool_choice) === false)
        return undefined;
    return {
        ...(response_format !== undefined ? { response_format: response_format } : {}),
        ...(tools !== undefined ? { tools: tools } : {}),
        ...(tool_choice !== undefined ? { tool_choice } : {}),
    };
}
export function enforcedSchemaFields(strategy, schema) {
    if (strategy.strategy !== "json_schema" && strategy.strategy !== "tool_mode") {
        return undefined;
    }
    if (isRepresentable(strategy, schema) === false) {
        return undefined;
    }
    const fields = validatePrepared(strategy.prepareRequest(schema, []));
    if (fields === undefined)
        return undefined;
    return Object.keys(fields).length === 0 ? undefined : fields;
}
//# sourceMappingURL=enforced-fields.js.map