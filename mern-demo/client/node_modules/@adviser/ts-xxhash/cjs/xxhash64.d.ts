import { Uint64, XXHInput, XXHSeed } from "./types.js";
export declare class XXH64 {
    #private;
    private readonly seed;
    private readonly v1;
    private readonly v2;
    private readonly v3;
    private readonly v4;
    private total_len;
    private memsize;
    private readonly memory;
    constructor(seed?: XXHSeed);
    update(input: XXHInput): this;
    digest(): Uint64;
    toString(radix?: number): string;
    static h64(inputOrSeed?: XXHInput | XXHSeed, seed?: XXHSeed): XXH64;
    static hash(input: XXHInput, seed?: XXHSeed): Uint64;
}
//# sourceMappingURL=xxhash64.d.ts.map