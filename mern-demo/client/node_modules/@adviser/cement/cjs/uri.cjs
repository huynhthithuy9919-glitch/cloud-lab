"use strict";
Object.defineProperty(exports, "__esModule", { value: true });
exports.URI = exports.BuildURI = void 0;
exports.isURL = isURL;
exports.isRequestLike = isRequestLike;
exports.isCoerceURI = isCoerceURI;
const result_js_1 = require("./result.cjs");
const mutable_url_js_1 = require("./mutable-url.cjs");
const resolve_once_js_1 = require("./resolve-once.cjs");
const index_js_1 = require("./index.cjs");
const relative_path_js_1 = require("./utils/relative-path.cjs");
const stripper_js_1 = require("./utils/stripper.cjs");
function match(iref, ioth) {
    const mr = {
        score: 0,
        protocol: false,
        hostname: false,
        port: false,
        pathname: false,
        pathParts: [],
        params: {},
    };
    const ref = URI.from(iref);
    const oth = URI.from(ioth);
    if (ref.protocol === oth.protocol) {
        mr.score += 1;
        mr.protocol = true;
    }
    try {
        const refH = ref.hostname;
        const refP = ref.port;
        if (refH === oth.hostname) {
            mr.score += 1;
            mr.hostname = true;
        }
        if (refP.length && refP === oth.port) {
            mr.score += 1;
            mr.port = true;
        }
    }
    catch (_e) {
    }
    if (ref.pathname.length && ref.pathname !== "/") {
        const pref = ref.pathname.split("/").filter((p) => p.length);
        const poth = oth.pathname.split("/").filter((p) => p.length);
        for (let i = 0; i < pref.length && i < poth.length; i++) {
            if (poth[i] === pref[i]) {
                mr.score += 1;
                mr.pathname = true;
                mr.pathParts.push(pref[i]);
            }
        }
    }
    for (const [key, value] of ref.getParams) {
        if (oth.getParam(key) === value) {
            mr.score += 1;
            mr.params[key] = value;
        }
    }
    return mr;
}
function coerceKey(key, def) {
    if (typeof key === "object") {
        const keys = Object.keys(key);
        if (keys.length !== 1) {
            throw new Error(`Invalid key: ${JSON.stringify(key)}`);
        }
        return { key: keys[0], def: key[keys[0]] };
    }
    return { key, def: def };
}
function resolveHash(hash) {
    const searchParams = new URLSearchParams(hash.replace(/^#/, ""));
    return {
        getParam: (k) => {
            const ret = searchParams.get(k);
            return ret === null ? undefined : ret;
        },
    };
}
function falsy2undef(value) {
    return value === undefined || value === null ? undefined : value;
}
function ensureURLWithDefaultProto(url, defaultProtocol, action) {
    if (!url) {
        return action.fromThrow(`${defaultProtocol}//`);
    }
    if (typeof url === "string") {
        try {
            return action.fromThrow(url);
        }
        catch (_e) {
            return action.fromThrow(`${defaultProtocol}//${url}`);
        }
    }
    else {
        return action.fromThrow(url.toString());
    }
}
function isURL(value) {
    return (value instanceof URL ||
        (!!value &&
            typeof value.searchParams === "object" &&
            typeof value.searchParams.sort === "function" &&
            typeof value.hash === "string"));
}
function isRequestLike(value) {
    return !!value && (typeof value.url === "string" || isURL(value.url));
}
function from(fac, strURLUri, defaultProtocol, action) {
    switch (typeof falsy2undef(strURLUri)) {
        case "undefined":
            return fac(action.fromThrow(`${defaultProtocol}///`));
        case "string":
            return fac(ensureURLWithDefaultProto(strURLUri, defaultProtocol, action));
        case "object":
            if (BuildURI.is(strURLUri)) {
                return fac(action.fromThrow(strURLUri._url.toString()));
            }
            else if (URI.is(strURLUri)) {
                return fac(action.fromThrow(strURLUri._url.toString()));
            }
            else if (isURL(strURLUri)) {
                return fac(action.fromThrow(strURLUri.toString()));
            }
            else if (isRequestLike(strURLUri)) {
                return fac(action.fromThrow(strURLUri.url.toString()));
            }
            throw new Error(`unknown object type: ${strURLUri}`);
        default:
            throw new Error(`Invalid argument: ${typeof strURLUri}`);
    }
}
function getParamResult(key, val, msgFn = (key) => {
    return `missing parameter: ${key}`;
}) {
    if (val === undefined) {
        return result_js_1.Result.Err(msgFn(key));
    }
    return result_js_1.Result.Ok(val);
}
function setParams(src, val, mode = "reset", out = new URLSearchParams("")) {
    let preset;
    switch (mode) {
        case "reset":
            preset = {};
            break;
        case "merge":
        default:
            preset = Object.fromEntries((0, mutable_url_js_1.URLSearchParamsEntries)(new URLSearchParams(src)));
            break;
    }
    for (const [key, value] of Object.entries({ ...preset, ...val }).sort((a, b) => a[0].localeCompare(b[0]))) {
        switch (typeof value) {
            case "string":
                out.set(key, value);
                break;
            case "number":
                out.set(key, value.toString());
                break;
            case "boolean":
                out.set(key, value ? "true" : "false");
                break;
            default:
                if (value instanceof Date) {
                    out.set(key, value.toISOString());
                }
                else {
                    console.error(`unsupported type: ${typeof value} ignore key: ${key}`);
                }
                break;
        }
    }
    return out.toString();
}
class BuildURI {
    constructor(url) {
        this._url = url;
    }
    static is(value) {
        return (value instanceof BuildURI ||
            (!!value && typeof value.delParam === "function" && typeof value.setParam === "function"));
    }
    static from(strURLUri, defaultProtocol = "file:") {
        return from((url) => new BuildURI(url), strURLUri, defaultProtocol, { fromThrow: mutable_url_js_1.WritableURL.fromThrow });
    }
    match(other) {
        return match(this.URI(), URI.from(other));
    }
    port(p) {
        this._url.port = p;
        return this;
    }
    host(h) {
        this._url.host = h;
        return this;
    }
    hostname(h) {
        this._url.hostname = h;
        return this;
    }
    protocol(p) {
        if (!p.endsWith(":")) {
            p = `${p}:`;
        }
        this._url.protocol = p;
        return this;
    }
    pathname(p) {
        this._url.pathname = p;
        return this;
    }
    hash(h) {
        this._url.hash = h;
        return this;
    }
    resolve(p) {
        if (!p) {
            return this;
        }
        if (typeof p === "string") {
            if (!p.match(/^[a-zA-Z0-9]+:/)) {
                if (p.startsWith("/")) {
                    this.pathname(p);
                    return this;
                }
                return this.appendRelative(p);
            }
        }
        if (isRequestLike(p)) {
            return this.resolve(p.url);
        }
        this._url = mutable_url_js_1.WritableURL.fromThrow(p.toString());
        return this;
    }
    appendRelative(p) {
        const appendUrl = URI.from(p);
        const pathname = "./" + appendUrl.pathname;
        const basePath = this._url.pathname;
        this.pathname((0, relative_path_js_1.relativePath)(basePath, pathname));
        for (const [key, value] of appendUrl.getParams) {
            this.setParam(key, value);
        }
        return this;
    }
    cleanParams(...remove) {
        const keys = new Set(remove.flat());
        for (const [key] of Array.from((0, mutable_url_js_1.URLSearchParamsEntries)(this._url.searchParams))) {
            if (keys.size === 0 || keys.has(key)) {
                this._url.searchParams.delete(key);
            }
        }
        return this;
    }
    searchParams(val, mode = "reset") {
        setParams(this._url.search, val, mode, this._url.searchParams);
        return this;
    }
    hashParams(val, mode = "reset") {
        this._url.hash = setParams(this._url.hash.replace(/^#/, ""), val, mode);
        return this;
    }
    delParam(key) {
        this._url.searchParams.delete(key);
        return this;
    }
    defParam(key, str) {
        if (!this._url.searchParams.has(key)) {
            this._url.searchParams.set(key, str);
        }
        return this;
    }
    setParam(key, str) {
        this._url.searchParams.set(key, str);
        return this;
    }
    hasParam(key) {
        return this._url.searchParams.has(key);
    }
    get getParams() {
        return (0, mutable_url_js_1.URLSearchParamsEntries)(this._url.searchParams);
    }
    getParam(key, def) {
        const { key: k, def: d } = coerceKey(key, def);
        let val = this._url.searchParams.get(k);
        if (!falsy2undef(val) && d) {
            val = d;
        }
        return falsy2undef(val);
    }
    getParamResult(key, msgFn) {
        return getParamResult(key, this.getParam(key), msgFn);
    }
    getParamsResult(...keys) {
        return (0, index_js_1.getParamsResult)(keys, this);
    }
    getHashParams(...keys) {
        return (0, index_js_1.getParamsResult)(keys, resolveHash(this._url.hash));
    }
    toString() {
        this._url.searchParams.sort();
        return this._url.toString();
    }
    toJSON() {
        return this.toString();
    }
    asURL() {
        return this.URI().asURL();
    }
    asObj(...strips) {
        return this.URI().asObj(...strips);
    }
    clone() {
        return BuildURI.from(this.toString());
    }
    get onlyHostAndSchema() {
        return this.clone().pathname("").cleanParams().hash("").toString();
    }
    get withoutHostAndSchema() {
        return this._url.pathname + this._url.search + this._url.hash;
    }
    URI() {
        return URI.from(this._url);
    }
}
exports.BuildURI = BuildURI;
function isCoerceURI(value) {
    if (!value) {
        return false;
    }
    if (isURL(value)) {
        return true;
    }
    if (URI.is(value)) {
        return true;
    }
    if (BuildURI.is(value)) {
        return true;
    }
    if (typeof value === "string") {
        return true;
    }
    return false;
}
const uriInstances = new resolve_once_js_1.KeyedResolvOnce({
    lru: { maxEntries: 1000 },
});
class URI {
    static protocolHasHostpart(protocol) {
        protocol = protocol.replace(/:$/, "");
        index_js_1.hasHostPartProtocols.add(protocol);
        return () => {
            index_js_1.hasHostPartProtocols.delete(protocol);
        };
    }
    match(other) {
        return match(this, other);
    }
    static merge(into, from, defaultProtocol = "file:") {
        const intoUrl = BuildURI.from(into, defaultProtocol);
        const fromUrl = URI.from(from, defaultProtocol);
        intoUrl.protocol(fromUrl.protocol);
        const fPath = fromUrl.pathname;
        if (!(fPath.length === 0 || fPath === "/" || fPath === "./")) {
            intoUrl.pathname(fromUrl.pathname);
        }
        for (const [key, value] of fromUrl.getParams) {
            intoUrl.setParam(key, value);
        }
        return intoUrl.URI();
    }
    static is(value) {
        return (value instanceof URI ||
            (!!value &&
                typeof value.asURL === "function" &&
                typeof value.getParam === "function" &&
                typeof value.hasParam === "function"));
    }
    static lazyFrom(strURLUri, defaultProtocol = "file:") {
        return from((url) => uriInstances.get(`lazy+${url.toString()}`).once(() => new URI(url)), strURLUri, defaultProtocol, {
            fromThrow: (urlStr) => {
                return mutable_url_js_1.ReadonlyURL.fromThrow(urlStr, true);
            },
        });
    }
    static from(strURLUri, defaultProtocol = "file:") {
        return from((url) => uriInstances.get(url.toString()).once(() => new URI(url)), strURLUri, defaultProtocol, {
            fromThrow: mutable_url_js_1.ReadonlyURL.fromThrow,
        });
    }
    static fromResult(strURLUri, defaultProtocol = "file:") {
        return (0, result_js_1.exception2Result)(() => URI.from(strURLUri, defaultProtocol));
    }
    constructor(url) {
        this._url = url.clone();
    }
    build() {
        return BuildURI.from(this._url);
    }
    get hostname() {
        return this._url.hostname;
    }
    get onlyHostAndSchema() {
        return this.build().pathname("").cleanParams().hash("").toString();
    }
    get withoutHostAndSchema() {
        return this._url.pathname + this._url.search + this._url.hash;
    }
    get port() {
        return this._url.port;
    }
    get host() {
        return this._url.host;
    }
    get search() {
        return this._url.search;
    }
    get protocol() {
        return this._url.protocol;
    }
    get pathname() {
        return this._url.pathname;
    }
    get hash() {
        return this._url.hash;
    }
    get getParams() {
        return (0, mutable_url_js_1.URLSearchParamsEntries)(this._url.searchParams);
    }
    get getHashes() {
        return (0, mutable_url_js_1.URLSearchParamsEntries)(new URLSearchParams(this._url.hash.slice("#".length)));
    }
    hasParam(key) {
        return this._url.searchParams.has(key);
    }
    getParam(key, def) {
        const { key: k, def: d } = coerceKey(key, def);
        let val = this._url.searchParams.get(k);
        if (!falsy2undef(val) && d) {
            val = d;
        }
        return falsy2undef(val);
    }
    getParamResult(key, msgFn) {
        return getParamResult(key, this.getParam(key), msgFn);
    }
    getParamsResult(...keys) {
        return (0, index_js_1.getParamsResult)(keys, this);
    }
    getHashParams(...keys) {
        return (0, index_js_1.getParamsResult)(keys, resolveHash(this._url.hash));
    }
    clone() {
        return new URI(this._url);
    }
    asURL() {
        return this._url.clone();
    }
    toString() {
        return this._url.toString();
    }
    toJSON() {
        return this.toString();
    }
    asObj(...strips) {
        const pathURI = {
            style: "path",
            protocol: this.protocol,
            pathname: this.pathname,
            searchParams: Object.fromEntries(this.getParams),
        };
        if (index_js_1.hasHostPartProtocols.has(this.protocol.replace(/:$/, ""))) {
            return (0, stripper_js_1.stripper)(strips, {
                ...pathURI,
                style: "host",
                hostname: this.hostname,
                port: this.port,
            });
        }
        return (0, stripper_js_1.stripper)(strips, pathURI);
    }
}
exports.URI = URI;
//# sourceMappingURL=uri.js.map