"use strict";
var __classPrivateFieldSet = (this && this.__classPrivateFieldSet) || function (receiver, state, value, kind, f) {
    if (kind === "m") throw new TypeError("Private method is not writable");
    if (kind === "a" && !f) throw new TypeError("Private accessor was defined without a setter");
    if (typeof state === "function" ? receiver !== state || !f : !state.has(receiver)) throw new TypeError("Cannot write private member to an object whose class did not declare it");
    return (kind === "a" ? f.call(receiver, value) : f ? f.value = value : state.set(receiver, value)), value;
};
var __classPrivateFieldGet = (this && this.__classPrivateFieldGet) || function (receiver, state, kind, f) {
    if (kind === "a" && !f) throw new TypeError("Private accessor was defined without a getter");
    if (typeof state === "function" ? receiver !== state || !f : !state.has(receiver)) throw new TypeError("Cannot read private member from an object whose class did not declare it");
    return kind === "m" ? f : kind === "a" ? f.call(receiver) : f ? f.value : state.get(receiver);
};
var _ClientFetch_server;
Object.defineProperty(exports, "__esModule", { value: true });
exports.TestFetchPair = exports.ServerFetch = exports.ClientFetch = void 0;
class ClientFetch {
    constructor() {
        _ClientFetch_server.set(this, void 0);
        this.calls = [];
        this.fetch = async (url, options) => {
            const req = new Request(url, options);
            const callsReq = req.clone();
            const res = await __classPrivateFieldGet(this, _ClientFetch_server, "f").serve(req);
            this.calls.push({ request: callsReq, response: res.clone() });
            return res;
        };
    }
    connect(server) {
        __classPrivateFieldSet(this, _ClientFetch_server, server, "f");
    }
}
exports.ClientFetch = ClientFetch;
_ClientFetch_server = new WeakMap();
class ServerFetch {
    onServe(serve) {
        this.serve = serve;
    }
}
exports.ServerFetch = ServerFetch;
class TestFetchPair {
    static create() {
        const tfp = new TestFetchPair();
        tfp.client.connect(tfp.server);
        return tfp;
    }
    constructor() {
        this.client = new ClientFetch();
        this.server = new ServerFetch();
    }
}
exports.TestFetchPair = TestFetchPair;
//# sourceMappingURL=test-fetch-pair.js.map