"use strict";
Object.defineProperty(exports, "__esModule", { value: true });
exports.ResultError = exports.ResultOK = exports.Result = void 0;
exports.exception2Result = exception2Result;
const is_promise_js_1 = require("./is-promise.cjs");
class Result {
    static Ok(...args) {
        return args.length >= 1 ? new ResultOK(args[0]) : new ResultOK(undefined);
    }
    static Err(t) {
        if (typeof t === "string") {
            return new ResultError(new Error(t));
        }
        if (Result.Is(t)) {
            if (t.is_ok()) {
                return new ResultError(new Error("Result Error is Ok"));
            }
            return t;
        }
        return new ResultError(t);
    }
    static Is(t) {
        if (!t) {
            return false;
        }
        if (t instanceof Result) {
            return true;
        }
        const rt = t;
        if ([typeof rt.is_ok, typeof rt.is_err, typeof rt.unwrap, typeof rt.unwrap_err].every((x) => x === "function")) {
            return true;
        }
        return false;
    }
    static AsyncOk(...args) {
        return Promise.resolve(Result.Ok(...args));
    }
    static AsyncErr(t) {
        return Promise.resolve(Result.Err(t));
    }
    isOk() {
        return this.is_ok();
    }
    isErr() {
        return this.is_err();
    }
    Ok() {
        return this.unwrap();
    }
    Err() {
        return this.unwrap_err();
    }
}
exports.Result = Result;
class ResultOK extends Result {
    constructor(t) {
        super();
        this._t = t;
    }
    is_ok() {
        return true;
    }
    is_err() {
        return false;
    }
    unwrap_err() {
        throw new Error("Result is Ok");
    }
    unwrap() {
        return this._t;
    }
}
exports.ResultOK = ResultOK;
class ResultError extends Result {
    constructor(t) {
        super();
        this._error = t;
    }
    is_ok() {
        return false;
    }
    is_err() {
        return true;
    }
    unwrap() {
        throw new Error(`Result is Err: ${this._error}`);
    }
    unwrap_err() {
        return this._error;
    }
}
exports.ResultError = ResultError;
function exception2Result(fn) {
    try {
        const res = fn();
        if ((0, is_promise_js_1.isPromise)(res)) {
            return res
                .then((value) => {
                return Result.Is(value) ? value : Result.Ok(value);
            })
                .catch((e) => Result.Err(e));
        }
        return (Result.Is(res) ? res : Result.Ok(res));
    }
    catch (e) {
        return Result.Err(e);
    }
}
//# sourceMappingURL=result.js.map