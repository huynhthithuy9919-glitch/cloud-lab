"use strict";
var __importDefault = (this && this.__importDefault) || function (mod) {
    return (mod && mod.__esModule) ? mod : { "default": mod };
};
Object.defineProperty(exports, "__esModule", { value: true });
exports.NodeSystemService = exports.NodeExitServiceImpl = void 0;
exports.NodeSysAbstraction = NodeSysAbstraction;
const node_file_service_js_1 = require("./node-file-service.cjs");
const node_process_1 = __importDefault(require("node:process"));
class NodeExitServiceImpl {
    constructor() {
        this._exitHandlers = [];
        this.invoked = false;
        this._handleExit = async () => {
            if (this.invoked) {
                return;
            }
            this.invoked = true;
            for (const h of this._exitHandlers) {
                try {
                    const ret = h.hdl();
                    if (typeof ret.then === "function") {
                        await ret;
                    }
                }
                finally {
                }
            }
        };
        node_process_1.default.on("unhandledRejection", (reason, p) => {
            this.exit(19);
        });
        node_process_1.default.on("uncaughtException", (error) => {
            this.exit(18);
        });
        node_process_1.default.on("close", () => {
            this.exit(0);
        });
        node_process_1.default.on("exit", () => {
            this.exit(0);
        });
        node_process_1.default.on("SIGQUIT", () => {
            this.exit(3);
        });
        node_process_1.default.on("SIGINT", () => {
            this.exit(2);
        });
        node_process_1.default.on("SIGTERM", () => {
            this.exit(9);
        });
    }
    injectExitHandlers(hdls) {
        this._exitHandlers = hdls;
    }
    exit(code) {
        this._handleExit()
            .then(() => {
            node_process_1.default.exit(code);
        })
            .catch((err) => {
            console.error("ExitService: failed to handle exit", err);
            node_process_1.default.exit(code);
        });
    }
}
exports.NodeExitServiceImpl = NodeExitServiceImpl;
class NodeSystemService {
    constructor() {
        this._exitService = new NodeExitServiceImpl();
        this._exitService.injectExitHandlers(NodeSystemService._exitHandlers);
    }
    OnExit(hdl) {
        const id = crypto.randomUUID();
        NodeSystemService._exitHandlers.push({ hdl, id });
        return () => {
            const idx = NodeSystemService._exitHandlers.findIndex((h) => h.id === id);
            if (idx >= 0) {
                NodeSystemService._exitHandlers.splice(idx, 1);
            }
        };
    }
    Exit(code) {
        this._exitService.exit(code);
    }
}
exports.NodeSystemService = NodeSystemService;
NodeSystemService._exitHandlers = [];
let baseSysAbstraction = undefined;
function NodeSysAbstraction(param) {
    const ende = param?.TxtEnDecoder || param.cement.TxtEnDecoderSingleton();
    baseSysAbstraction =
        baseSysAbstraction ??
            new param.cement.BaseSysAbstraction({
                TxtEnDecoder: ende,
                FileSystem: new node_file_service_js_1.NodeFileService(ende),
                SystemService: new NodeSystemService(),
            });
    return new param.cement.WrapperRuntimeSysAbstraction(baseSysAbstraction, {
        basicRuntimeService: param.cement.NodeBasicSysAbstraction(param),
        ...param,
    });
}
//# sourceMappingURL=node-sys-abstraction.js.map