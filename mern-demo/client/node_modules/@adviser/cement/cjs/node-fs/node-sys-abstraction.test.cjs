"use strict";
var __createBinding = (this && this.__createBinding) || (Object.create ? (function(o, m, k, k2) {
    if (k2 === undefined) k2 = k;
    var desc = Object.getOwnPropertyDescriptor(m, k);
    if (!desc || ("get" in desc ? !m.__esModule : desc.writable || desc.configurable)) {
      desc = { enumerable: true, get: function() { return m[k]; } };
    }
    Object.defineProperty(o, k2, desc);
}) : (function(o, m, k, k2) {
    if (k2 === undefined) k2 = k;
    o[k2] = m[k];
}));
var __setModuleDefault = (this && this.__setModuleDefault) || (Object.create ? (function(o, v) {
    Object.defineProperty(o, "default", { enumerable: true, value: v });
}) : function(o, v) {
    o["default"] = v;
});
var __importStar = (this && this.__importStar) || (function () {
    var ownKeys = function(o) {
        ownKeys = Object.getOwnPropertyNames || function (o) {
            var ar = [];
            for (var k in o) if (Object.prototype.hasOwnProperty.call(o, k)) ar[ar.length] = k;
            return ar;
        };
        return ownKeys(o);
    };
    return function (mod) {
        if (mod && mod.__esModule) return mod;
        var result = {};
        if (mod != null) for (var k = ownKeys(mod), i = 0; i < k.length; i++) if (k[i] !== "default") __createBinding(result, mod, k[i]);
        __setModuleDefault(result, mod);
        return result;
    };
})();
Object.defineProperty(exports, "__esModule", { value: true });
const cement_1 = require("@adviser/cement");
const vitest_1 = require("vitest");
function exitHandler(errCode, larg, done) {
    return (err, stdout, stderr) => {
        if (err) {
            (0, vitest_1.expect)(err.code).toBe(errCode);
        }
        if (stdout) {
            const res = stdout
                .toString()
                .split("\n")
                .filter((line) => line.trim())
                .map((line) => {
                const out = JSON.parse(line);
                return out;
            })
                .map((obj) => {
                delete obj.pid;
                return obj;
            });
            (0, vitest_1.expect)(res).toEqual([
                {
                    larg: larg,
                },
                {
                    larg: larg,
                    msg: "Called OnExit 1",
                },
                {
                    larg: larg,
                    msg: "Called OnExit 2",
                },
            ]);
            done();
        }
        if (stderr) {
            (0, vitest_1.expect)(stderr).toEqual({});
        }
    };
}
(0, vitest_1.describe)(`${(0, cement_1.runtimeFn)().isDeno ? "deno" : "node"}_sys`, () => {
    if ((0, cement_1.runtimeFn)().isNodeIsh || (0, cement_1.runtimeFn)().isDeno) {
        let fnExec;
        let execHandler = "tsx src/test/test-exit-handler.ts";
        (0, vitest_1.beforeAll)(async () => {
            const { exec } = await Promise.resolve().then(() => __importStar(require("node:child_process")));
            fnExec = exec;
            if ((0, cement_1.runtimeFn)().isDeno) {
                execHandler =
                    "deno run --quiet --allow-net --allow-read --allow-run --unstable-sloppy-imports src/test/test-exit-handler.ts";
            }
        });
        (0, vitest_1.it)("just-exit", () => {
            return new Promise((done) => {
                fnExec(`${execHandler}  exit24`, exitHandler(24, "exit24", done));
            });
        });
        (0, vitest_1.it)("throw", () => {
            return new Promise((done) => {
                fnExec(`${execHandler}  throw`, exitHandler(19, "throw", done));
            });
        });
        (0, vitest_1.it)("via sigint", () => {
            return new Promise((done) => {
                fnExec(`${execHandler}  sigint`, exitHandler(2, "sigint", done));
            });
        });
        (0, vitest_1.it)("via sigterm", () => {
            return new Promise((done) => {
                fnExec(`${execHandler}  sigterm`, exitHandler(9, "sigterm", done));
            });
        });
        (0, vitest_1.it)("via sigquit", () => {
            return new Promise((done) => {
                fnExec(`${execHandler}  sigquit`, exitHandler(3, "sigquit", done));
            });
        });
    }
    else {
        vitest_1.it.skip("nothing in browser", () => {
            (0, vitest_1.expect)(true).toBe(true);
        });
    }
});
//# sourceMappingURL=node-sys-abstraction.test.js.map