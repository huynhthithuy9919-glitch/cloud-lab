"use strict";
Object.defineProperty(exports, "__esModule", { value: true });
const vitest_1 = require("vitest");
const cement_1 = require("@adviser/cement");
const stream_test_helper_js_1 = require("./stream-test-helper.cjs");
(0, vitest_1.it)("array2stream", async () => {
    const as = (0, cement_1.array2stream)([1, 2, 3]);
    let i = 0;
    const reader = as.getReader();
    while (true) {
        const { done, value } = await reader.read();
        if (done) {
            break;
        }
        (0, vitest_1.expect)(1 + i++).toBe(value);
    }
});
(0, vitest_1.it)("stream2array", async () => {
    const as = await (0, cement_1.stream2array)(new ReadableStream({
        start(controller) {
            controller.enqueue(1);
            controller.enqueue(2);
            controller.enqueue(3);
            controller.close();
        },
    }));
    (0, vitest_1.expect)(as).toEqual([1, 2, 3]);
});
(0, vitest_1.it)("devnull", async () => {
    const cnt = await (0, cement_1.devnull)((0, cement_1.streamMap)((0, cement_1.array2stream)([1, 2, 3]), {
        Map: (i, idx) => (idx + 1) * 10 + i + 1,
    }));
    (0, vitest_1.expect)(cnt).toBe(3);
});
(0, vitest_1.it)("stream_map", async () => {
    const closeFn = vitest_1.vitest.fn();
    const s = await (0, cement_1.stream2array)((0, cement_1.streamMap)((0, cement_1.array2stream)([1, 2, 3]), {
        Map: (i, idx) => (idx + 1) * 10 + i + 1,
        Close: closeFn,
    }));
    (0, vitest_1.expect)(closeFn).toHaveBeenCalledTimes(1);
    (0, vitest_1.expect)(s).toEqual([12, 23, 34]);
});
(0, vitest_1.it)("stream_map async", async () => {
    const closeFn = vitest_1.vitest.fn();
    const s = await (0, cement_1.stream2array)((0, cement_1.streamMap)((0, cement_1.array2stream)([1, 2, 3]), {
        Map: (i, idx) => Promise.resolve((idx + 1) * 10 + i + 1),
        Close: closeFn,
    }));
    (0, vitest_1.expect)(closeFn).toHaveBeenCalledTimes(1);
    (0, vitest_1.expect)(s).toEqual([12, 23, 34]);
});
(0, vitest_1.it)("map types", async () => {
    const oo = await (0, cement_1.stream2array)((0, cement_1.streamMap)((0, cement_1.array2stream)([1, 2, 3]), {
        Map: (chunk, idx) => {
            return "[" + chunk + "/" + idx + "]";
        },
    }));
    (0, vitest_1.expect)(oo).toEqual(["[1/0]", "[2/1]", "[3/2]"]);
});
(0, vitest_1.describe)("test streaming through streamMap", () => {
    const state = {
        sendChunks: 10000,
        sendChunkSize: 3,
        fillCalls: 0,
        CollectorFn: vitest_1.vitest.fn(),
    };
    (0, vitest_1.it)("does streamMap respect backpressure", async () => {
        const ts = new TransformStream(undefined, undefined, { highWaterMark: 2 });
        const reb = (0, cement_1.streamMap)(ts.readable, {
            Map: (chunk) => {
                for (let i = 0; i < chunk.length; i++) {
                    chunk[i] = (chunk[i] + 1) % 256;
                }
                return chunk;
            },
        });
        await Promise.all([(0, stream_test_helper_js_1.receiveFromStream)(reb, state), (0, stream_test_helper_js_1.sendToStream)(ts.writable, state)]);
        (0, vitest_1.expect)(state.CollectorFn).toHaveBeenCalledTimes(state.sendChunks + 1);
        (0, vitest_1.expect)(state.CollectorFn.mock.calls.slice(-1)[0][0].done).toBeTruthy();
        let lastfillCalls = 0;
        for (let i = 0; i < state.CollectorFn.mock.calls.length - 1; i++) {
            const { fillCalls, reBufferCalls, value } = state.CollectorFn.mock.calls[i][0];
            (0, vitest_1.expect)(value?.[0]).toBe((i + 1) % 256);
            (0, vitest_1.expect)(fillCalls * state.sendChunkSize).toBeGreaterThanOrEqual((fillCalls - lastfillCalls) * state.sendChunkSize + reBufferCalls * state.sendChunkSize);
            lastfillCalls = fillCalls;
        }
    });
});
(0, vitest_1.describe)("coerceStreamUint8", () => {
    (0, vitest_1.it)("default: encodes string chunks to Uint8Array", async () => {
        const result = await (0, cement_1.stream2array)((0, cement_1.coerceStreamUint8)((0, cement_1.array2stream)(["Hello", " ", "World"])));
        (0, vitest_1.expect)(new TextDecoder().decode(new Uint8Array(result.flatMap((c) => [...c])))).toBe("Hello World");
    });
    (0, vitest_1.it)("default: passes Uint8Array chunks through unchanged", async () => {
        const chunks = ["foo", "bar"].map((s) => new TextEncoder().encode(s));
        const result = await (0, cement_1.stream2array)((0, cement_1.coerceStreamUint8)((0, cement_1.array2stream)(chunks)));
        (0, vitest_1.expect)(result).toEqual(chunks);
    });
    (0, vitest_1.it)("default: handles mixed string and Uint8Array chunks", async () => {
        const stream = (0, cement_1.array2stream)(["Hello", new TextEncoder().encode(" World")]);
        const result = await (0, cement_1.stream2array)((0, cement_1.coerceStreamUint8)(stream));
        (0, vitest_1.expect)(new TextDecoder().decode(new Uint8Array(result.flatMap((c) => [...c])))).toBe("Hello World");
    });
    (0, vitest_1.it)("custom encoder: is called for string chunks", async () => {
        const encodeSpy = vitest_1.vitest.fn((x) => {
            return new TextEncoder().encode(x);
        });
        await (0, cement_1.stream2array)((0, cement_1.coerceStreamUint8)((0, cement_1.array2stream)(["a", "b", new Uint8Array([99])]), encodeSpy));
        (0, vitest_1.expect)(encodeSpy).toHaveBeenCalledTimes(2);
    });
});
(0, vitest_1.describe)("coerceStreamString", () => {
    (0, vitest_1.it)("default: decodes Uint8Array chunks to string", async () => {
        const enc = new TextEncoder();
        const result = await (0, cement_1.stream2array)((0, cement_1.coerceStreamString)((0, cement_1.array2stream)([enc.encode("Hello"), enc.encode(" World")])));
        (0, vitest_1.expect)(result.join("")).toBe("Hello World");
    });
    (0, vitest_1.it)("default: passes string chunks through unchanged", async () => {
        const result = await (0, cement_1.stream2array)((0, cement_1.coerceStreamString)((0, cement_1.array2stream)(["foo", "bar"])));
        (0, vitest_1.expect)(result).toEqual(["foo", "bar"]);
    });
    (0, vitest_1.it)("default: handles mixed Uint8Array and string chunks", async () => {
        const stream = (0, cement_1.array2stream)([new TextEncoder().encode("Hello"), " World"]);
        const result = await (0, cement_1.stream2array)((0, cement_1.coerceStreamString)(stream));
        (0, vitest_1.expect)(result.join("")).toBe("Hello World");
    });
    (0, vitest_1.it)("custom decoder: is called for Uint8Array chunks", async () => {
        const decodeSpy = vitest_1.vitest.fn((x) => {
            if (x instanceof Uint8Array) {
                return new TextDecoder().decode(x);
            }
            return x;
        });
        const enc = new TextEncoder();
        await (0, cement_1.stream2array)((0, cement_1.coerceStreamString)((0, cement_1.array2stream)([enc.encode("a"), enc.encode("b"), "c"]), decodeSpy));
        (0, vitest_1.expect)(decodeSpy).toHaveBeenCalledTimes(3);
    });
});
//# sourceMappingURL=stream-map.test.js.map