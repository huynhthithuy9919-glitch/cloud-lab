"use strict";
Object.defineProperty(exports, "__esModule", { value: true });
const vitest_1 = require("vitest");
const tee_writer_js_1 = require("./tee-writer.cjs");
const result_js_1 = require("../result.cjs");
const string2stream_js_1 = require("./string2stream.cjs");
function makePeerStream(overrides = {}) {
    return {
        write: vitest_1.vi.fn().mockResolvedValue(undefined),
        cancel: vitest_1.vi.fn(),
        close: vitest_1.vi.fn(),
        ...overrides,
    };
}
function makePeer(peerStream, overrides = {}) {
    return { begin: vitest_1.vi.fn().mockResolvedValue(result_js_1.Result.Ok(peerStream)), ...overrides };
}
(0, vitest_1.describe)("teeWriter", () => {
    const chunk1 = new Uint8Array([1, 2, 3]);
    const chunk2 = new Uint8Array([4, 5, 6]);
    (0, vitest_1.it)("writes all chunks to a single peer and commits", async () => {
        const ps = makePeerStream();
        const peer = makePeer(ps);
        const result = await (0, tee_writer_js_1.teeWriter)([peer], (0, string2stream_js_1.uint8array2stream)(chunk1, chunk2));
        (0, vitest_1.expect)(result.isOk()).toBe(true);
        const { peer: winnerPeer } = result.Ok();
        (0, vitest_1.expect)(winnerPeer).toBe(ps);
        (0, vitest_1.expect)(ps.write).toHaveBeenCalledTimes(2);
        (0, vitest_1.expect)(ps.write).toHaveBeenNthCalledWith(1, chunk1);
        (0, vitest_1.expect)(ps.write).toHaveBeenNthCalledWith(2, chunk2);
        (0, vitest_1.expect)(ps.close).toHaveBeenCalledOnce();
    });
    (0, vitest_1.it)("returns the first peer and cancels the rest when all succeed", async () => {
        const ps1 = makePeerStream();
        const ps2 = makePeerStream();
        const ps3 = makePeerStream();
        const result = await (0, tee_writer_js_1.teeWriter)([makePeer(ps1), makePeer(ps2), makePeer(ps3)], (0, string2stream_js_1.uint8array2stream)(chunk1));
        (0, vitest_1.expect)(result.isOk()).toBe(true);
        const { peer: winnerPeer } = result.Ok();
        (0, vitest_1.expect)(winnerPeer).toBe(ps1);
        (0, vitest_1.expect)(ps1.close).toHaveBeenCalledOnce();
        (0, vitest_1.expect)(ps1.cancel).not.toHaveBeenCalled();
        (0, vitest_1.expect)(ps2.cancel).toHaveBeenCalled();
        (0, vitest_1.expect)(ps3.cancel).toHaveBeenCalled();
        (0, vitest_1.expect)(ps1.write).toHaveBeenCalledWith(chunk1);
        (0, vitest_1.expect)(ps2.write).toHaveBeenCalledWith(chunk1);
        (0, vitest_1.expect)(ps3.write).toHaveBeenCalledWith(chunk1);
    });
    (0, vitest_1.it)("removes a peer that fails mid-stream and continues with the rest", async () => {
        const ps1 = makePeerStream({
            write: vitest_1.vi
                .fn()
                .mockResolvedValueOnce(undefined)
                .mockRejectedValueOnce(new Error("write failed")),
        });
        const ps2 = makePeerStream();
        const result = await (0, tee_writer_js_1.teeWriter)([makePeer(ps1), makePeer(ps2)], (0, string2stream_js_1.uint8array2stream)(chunk1, chunk2));
        (0, vitest_1.expect)(result.isOk()).toBe(true);
        const { peer: winnerPeer } = result.Ok();
        (0, vitest_1.expect)(winnerPeer).toBe(ps2);
        (0, vitest_1.expect)(ps1.cancel).toHaveBeenCalled();
        (0, vitest_1.expect)(ps2.write).toHaveBeenCalledTimes(2);
    });
    (0, vitest_1.it)("returns Err when all peers fail during the stream", async () => {
        const ps1 = makePeerStream({
            write: vitest_1.vi.fn().mockRejectedValue(new Error("fail")),
        });
        const ps2 = makePeerStream({
            write: vitest_1.vi.fn().mockRejectedValue(new Error("fail")),
        });
        const result = await (0, tee_writer_js_1.teeWriter)([makePeer(ps1), makePeer(ps2)], (0, string2stream_js_1.uint8array2stream)(chunk1));
        (0, vitest_1.expect)(result.isErr()).toBe(true);
        (0, vitest_1.expect)(result.Err().message).toMatch(/all peers failed/);
    });
    (0, vitest_1.it)("returns the first peer on an empty stream", async () => {
        const ps1 = makePeerStream();
        const ps2 = makePeerStream();
        const result = await (0, tee_writer_js_1.teeWriter)([makePeer(ps1), makePeer(ps2)], (0, string2stream_js_1.uint8array2stream)());
        (0, vitest_1.expect)(result.isOk()).toBe(true);
        const { peer: winnerPeer } = result.Ok();
        (0, vitest_1.expect)(winnerPeer).toBe(ps1);
        (0, vitest_1.expect)(ps1.write).not.toHaveBeenCalled();
        (0, vitest_1.expect)(ps2.write).not.toHaveBeenCalled();
        (0, vitest_1.expect)(ps2.cancel).toHaveBeenCalled();
    });
    (0, vitest_1.it)("returns Err when all peers fail on begin", async () => {
        const failPeer = { begin: vitest_1.vi.fn().mockRejectedValue(new Error("begin failed")) };
        const result = await (0, tee_writer_js_1.teeWriter)([failPeer, failPeer], (0, string2stream_js_1.uint8array2stream)(chunk1));
        (0, vitest_1.expect)(result.isErr()).toBe(true);
        (0, vitest_1.expect)(result.Err().message).toMatch(/all peers failed/);
    });
    (0, vitest_1.it)("survives when some peers fail on begin", async () => {
        const ps = makePeerStream();
        const goodPeer = makePeer(ps);
        const badPeer = { begin: vitest_1.vi.fn().mockRejectedValue(new Error("begin failed")) };
        const result = await (0, tee_writer_js_1.teeWriter)([badPeer, goodPeer], (0, string2stream_js_1.uint8array2stream)(chunk1));
        (0, vitest_1.expect)(result.isOk()).toBe(true);
        const { peer: winnerPeer } = result.Ok();
        (0, vitest_1.expect)(winnerPeer).toBe(ps);
        (0, vitest_1.expect)(ps.write).toHaveBeenCalledWith(chunk1);
    });
    (0, vitest_1.it)("commit delegates name argument to the winning peer", async () => {
        const ps = makePeerStream();
        const result = await (0, tee_writer_js_1.teeWriter)([makePeer(ps)], (0, string2stream_js_1.uint8array2stream)());
        (0, vitest_1.expect)(result.isOk()).toBe(true);
        (0, vitest_1.expect)(ps.cancel).not.toHaveBeenCalled();
        (0, vitest_1.expect)(ps.close).toHaveBeenCalledOnce();
    });
});
(0, vitest_1.describe)("teeWriter resilience", () => {
    (0, vitest_1.it)("every active peer receives every chunk", async () => {
        const chunks = [new Uint8Array([10]), new Uint8Array([20]), new Uint8Array([30])];
        const received = new Map();
        function trackingPeerStream(name) {
            received.set(name, []);
            return makePeerStream({
                write: vitest_1.vi.fn().mockImplementation((chunk) => {
                    received.get(name)?.push(chunk);
                    return Promise.resolve();
                }),
            });
        }
        const ps1 = trackingPeerStream("a");
        const ps2 = trackingPeerStream("b");
        const ps3 = trackingPeerStream("c");
        const stream = new ReadableStream({
            start(controller) {
                for (const chunk of chunks) {
                    controller.enqueue(chunk);
                }
                controller.close();
            },
        });
        await (0, tee_writer_js_1.teeWriter)([makePeer(ps1), makePeer(ps2), makePeer(ps3)], stream);
        for (const [, data] of received) {
            (0, vitest_1.expect)(data).toEqual(chunks);
        }
    });
    (0, vitest_1.it)("returns Err when input stream errors", async () => {
        const ps = makePeerStream();
        const stream = new ReadableStream({
            start(controller) {
                controller.enqueue(new Uint8Array([1]));
                controller.error(new Error("source died"));
            },
        });
        const result = await (0, tee_writer_js_1.teeWriter)([makePeer(ps)], stream);
        (0, vitest_1.expect)(result.isErr()).toBe(true);
        (0, vitest_1.expect)(result.Err().message).toMatch(/source died/);
    });
    (0, vitest_1.it)("peers drop out at staggered points and survivor gets all chunks", async () => {
        const chunks = [new Uint8Array([1]), new Uint8Array([2]), new Uint8Array([3]), new Uint8Array([4])];
        const survivorChunks = [];
        const ps1 = makePeerStream({
            write: vitest_1.vi
                .fn()
                .mockResolvedValueOnce(undefined)
                .mockRejectedValueOnce(new Error("ps1 fail")),
        });
        const ps2 = makePeerStream({
            write: vitest_1.vi
                .fn()
                .mockResolvedValueOnce(undefined)
                .mockResolvedValueOnce(undefined)
                .mockRejectedValueOnce(new Error("ps2 fail")),
        });
        const ps3 = makePeerStream({
            write: vitest_1.vi.fn().mockImplementation((chunk) => {
                survivorChunks.push(chunk);
                return Promise.resolve();
            }),
        });
        const stream = new ReadableStream({
            start(controller) {
                for (const chunk of chunks) {
                    controller.enqueue(chunk);
                }
                controller.close();
            },
        });
        const result = await (0, tee_writer_js_1.teeWriter)([makePeer(ps1), makePeer(ps2), makePeer(ps3)], stream);
        (0, vitest_1.expect)(result.isOk()).toBe(true);
        (0, vitest_1.expect)(result.Ok().peer).toBe(ps3);
        (0, vitest_1.expect)(survivorChunks).toEqual(chunks);
        (0, vitest_1.expect)(ps1.cancel).toHaveBeenCalled();
        (0, vitest_1.expect)(ps2.cancel).toHaveBeenCalled();
    });
});
(0, vitest_1.describe)("teeWriter sync-throw", () => {
    (0, vitest_1.it)("sync throw in begin() bypasses Promise.allSettled", async () => {
        const survivor = makePeer(makePeerStream());
        const throwing = makePeer(makePeerStream(), {
            begin() {
                throw new Error("sync throw in begin");
            },
        });
        const result = await (0, tee_writer_js_1.teeWriter)([throwing, survivor], (0, string2stream_js_1.uint8array2stream)(new Uint8Array([1, 2, 3])));
        (0, vitest_1.expect)(result.isOk()).toBe(true);
        (0, vitest_1.expect)(result.Ok().peer.write).toHaveBeenCalledWith(new Uint8Array([1, 2, 3]));
    });
    (0, vitest_1.it)("sync throw in write() bypasses Promise.allSettled", async () => {
        const throwing = makePeerStream({
            write() {
                throw new Error("sync throw in write");
            },
        });
        const survivor = makePeerStream();
        const result = await (0, tee_writer_js_1.teeWriter)([makePeer(throwing), makePeer(survivor)], (0, string2stream_js_1.uint8array2stream)(new Uint8Array([1, 2, 3])));
        (0, vitest_1.expect)(result.isOk()).toBe(true);
        (0, vitest_1.expect)(result.Ok().peer.write).toHaveBeenCalledWith(new Uint8Array([1, 2, 3]));
    });
    (0, vitest_1.it)("sync throw in cancel() after a write failure bypasses Promise.allSettled", async () => {
        const failThenThrowCancel = makePeerStream({
            write() {
                return Promise.reject(new Error("write failed"));
            },
            cancel() {
                throw new Error("sync throw in cancel");
            },
        });
        const survivor = makePeerStream();
        const result = await (0, tee_writer_js_1.teeWriter)([makePeer(failThenThrowCancel), makePeer(survivor)], (0, string2stream_js_1.uint8array2stream)(new Uint8Array([1, 2, 3])));
        (0, vitest_1.expect)(result.isOk()).toBe(true);
        (0, vitest_1.expect)(result.Ok().peer.write).toHaveBeenCalledWith(new Uint8Array([1, 2, 3]));
    });
    (0, vitest_1.it)("sync throw in close() on the winner bypasses Promise.allSettled", async () => {
        const survivor = makePeerStream();
        const throwOnClose = makePeerStream({
            close() {
                throw new Error("sync throw in close");
            },
        });
        const result = await (0, tee_writer_js_1.teeWriter)([makePeer(throwOnClose), makePeer(survivor)], (0, string2stream_js_1.uint8array2stream)(new Uint8Array([1, 2, 3])));
        (0, vitest_1.expect)(result.isOk()).toBe(true);
        (0, vitest_1.expect)(result.Ok().peer.write).toHaveBeenCalledWith(new Uint8Array([1, 2, 3]));
    });
});
//# sourceMappingURL=tee-writer.test.js.map