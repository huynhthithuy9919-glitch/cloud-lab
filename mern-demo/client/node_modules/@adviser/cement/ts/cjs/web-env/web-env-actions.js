"use strict";
Object.defineProperty(exports, "__esModule", { value: true });
exports.BrowserEnvActions = void 0;
let once = undefined;
class BrowserEnvActions {
    static new(opts) {
        once = once ?? new BrowserEnvActions(opts);
        return once;
    }
    constructor(opts) {
        this.env = new Map();
        this.opts = opts;
    }
    get(key) {
        return this.env.get(key);
    }
    set(key, value) {
        if (value) {
            this.env.set(key, value);
        }
    }
    delete(key) {
        this.env.delete(key);
    }
    keys() {
        return Array.from(this.env.keys());
    }
    active() {
        return true;
    }
    register(env) {
        const sym = Symbol.for(this.opts.symbol || "CP_ENV");
        const browser = globalThis;
        browser[sym] = env;
        return env;
    }
}
exports.BrowserEnvActions = BrowserEnvActions;
//# sourceMappingURL=web-env-actions.js.map