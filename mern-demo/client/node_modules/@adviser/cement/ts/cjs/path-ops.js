"use strict";
var __classPrivateFieldGet = (this && this.__classPrivateFieldGet) || function (receiver, state, kind, f) {
    if (kind === "a" && !f) throw new TypeError("Private accessor was defined without a getter");
    if (typeof state === "function" ? receiver !== state || !f : !state.has(receiver)) throw new TypeError("Cannot read private member from an object whose class did not declare it");
    return kind === "m" ? f : kind === "a" ? f.call(receiver) : f ? f.value : state.get(receiver);
};
var _pathOpsImpl_instances, _pathOpsImpl_parts;
Object.defineProperty(exports, "__esModule", { value: true });
exports.pathOps = void 0;
class pathOpsImpl {
    constructor() {
        _pathOpsImpl_instances.add(this);
    }
    join(...paths) {
        const parts = __classPrivateFieldGet(this, _pathOpsImpl_instances, "m", _pathOpsImpl_parts).call(this, paths.filter((i) => i).join("/"));
        if (parts.dirname === "" || parts.dirname === ".") {
            return parts.basename ? parts.basename : ".";
        }
        return parts.dirname + "/" + parts.basename;
    }
    dirname(path) {
        return __classPrivateFieldGet(this, _pathOpsImpl_instances, "m", _pathOpsImpl_parts).call(this, path).dirname;
    }
    basename(path) {
        return __classPrivateFieldGet(this, _pathOpsImpl_instances, "m", _pathOpsImpl_parts).call(this, path).basename;
    }
}
_pathOpsImpl_instances = new WeakSet(), _pathOpsImpl_parts = function _pathOpsImpl_parts(path) {
    path = path
        .replace(/\/+/g, "/")
        .replace(/(\/\.\/)+/g, "/")
        .replace(/\/+$/, "");
    const splitted = path.split("/");
    if (splitted.length === 1) {
        return {
            dirname: ".",
            basename: splitted[0] === "." ? "" : splitted[0],
        };
    }
    const basename = splitted.pop();
    if (!basename) {
        throw new Error("basename is undefined");
    }
    return {
        dirname: splitted.join("/").replace(/^\.\//, ""),
        basename,
    };
};
exports.pathOps = new pathOpsImpl();
//# sourceMappingURL=path-ops.js.map