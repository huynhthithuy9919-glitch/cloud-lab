"use strict";
Object.defineProperty(exports, "__esModule", { value: true });
exports.CFRuntimeService = void 0;
exports.CFBasicSysAbstraction = CFBasicSysAbstraction;
const base_sys_abstraction_js_1 = require("../base-sys-abstraction.js");
const sys_env_js_1 = require("../sys-env.js");
const txt_en_decoder_js_1 = require("../txt-en-decoder.js");
class CFRuntimeService {
    constructor(ende, envFactory) {
        this._txtEnDe = ende;
        this._envFactory = envFactory;
    }
    Env() {
        return this._envFactory();
    }
    Args() {
        throw new Error("Args-Method not implemented.");
    }
    Stdout() {
        return CFWriteableStream((chunk) => {
            const decoded = this._txtEnDe.decode(chunk);
            console.log(decoded.trimEnd());
            return Promise.resolve();
        });
    }
    Stderr() {
        return CFWriteableStream((chunk) => {
            const decoded = this._txtEnDe.decode(chunk);
            console.error(decoded.trimEnd());
            return Promise.resolve();
        });
    }
}
exports.CFRuntimeService = CFRuntimeService;
function consumeReadableStream(reader, writeFn) {
    reader
        .read()
        .then(({ done, value }) => {
        if (done) {
            return;
        }
        writeFn(value)
            .then(() => {
            consumeReadableStream(reader, writeFn);
        })
            .catch((e) => {
            console.error("consumeReadableStream:writeFn", e);
        });
    })
        .catch((e) => {
        console.error("consumeReadableStream:read", e);
    });
}
function CFWriteableStream(writeFn) {
    const ts = new TransformStream();
    consumeReadableStream(ts.readable.getReader(), writeFn);
    return ts.writable;
}
let baseSysAbstraction = undefined;
function CFBasicSysAbstraction(param) {
    const ende = param?.TxtEnDecoder || (0, txt_en_decoder_js_1.TxtEnDecoderSingleton)();
    baseSysAbstraction =
        baseSysAbstraction ??
            new base_sys_abstraction_js_1.BaseBasicSysAbstraction({
                TxtEnDecoder: ende,
            });
    return new base_sys_abstraction_js_1.WrapperBasicSysAbstraction(baseSysAbstraction, {
        basicRuntimeService: new CFRuntimeService(ende, sys_env_js_1.envFactory),
        ...param,
    });
}
//# sourceMappingURL=cf-basic-sys-abstraction.js.map