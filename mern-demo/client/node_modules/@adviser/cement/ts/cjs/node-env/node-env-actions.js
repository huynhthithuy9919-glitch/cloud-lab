"use strict";
var __classPrivateFieldGet = (this && this.__classPrivateFieldGet) || function (receiver, state, kind, f) {
    if (kind === "a" && !f) throw new TypeError("Private accessor was defined without a getter");
    if (typeof state === "function" ? receiver !== state || !f : !state.has(receiver)) throw new TypeError("Cannot read private member from an object whose class did not declare it");
    return kind === "m" ? f : kind === "a" ? f.call(receiver) : f ? f.value : state.get(receiver);
};
var _NodeEnvActions_node;
Object.defineProperty(exports, "__esModule", { value: true });
exports.NodeEnvActions = void 0;
const runtime_js_1 = require("../runtime.js");
let once = undefined;
class NodeEnvActions {
    static new(opts) {
        once = once ?? new NodeEnvActions(opts);
        return once;
    }
    constructor(opts) {
        _NodeEnvActions_node.set(this, globalThis);
        this.opts = opts;
        this._env = this.active() ? __classPrivateFieldGet(this, _NodeEnvActions_node, "f").process.env : {};
    }
    register(env) {
        for (const key of env.keys()) {
            this._env[key] = env.get(key) || "";
        }
        return env;
    }
    active() {
        return (0, runtime_js_1.runtimeFn)().isNodeIsh;
    }
    keys() {
        return Object.keys(this._env);
    }
    get(key) {
        return this._env[key];
    }
    set(key, value) {
        if (value) {
            this._env[key] = value;
        }
    }
    delete(key) {
        delete this._env[key];
    }
}
exports.NodeEnvActions = NodeEnvActions;
_NodeEnvActions_node = new WeakMap();
//# sourceMappingURL=node-env-actions.js.map