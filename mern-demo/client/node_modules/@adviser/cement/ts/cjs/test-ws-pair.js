"use strict";
var __classPrivateFieldGet = (this && this.__classPrivateFieldGet) || function (receiver, state, kind, f) {
    if (kind === "a" && !f) throw new TypeError("Private accessor was defined without a getter");
    if (typeof state === "function" ? receiver !== state || !f : !state.has(receiver)) throw new TypeError("Cannot read private member from an object whose class did not declare it");
    return kind === "m" ? f : kind === "a" ? f.call(receiver) : f ? f.value : state.get(receiver);
};
var __classPrivateFieldSet = (this && this.__classPrivateFieldSet) || function (receiver, state, value, kind, f) {
    if (kind === "m") throw new TypeError("Private method is not writable");
    if (kind === "a" && !f) throw new TypeError("Private accessor was defined without a setter");
    if (typeof state === "function" ? receiver !== state || !f : !state.has(receiver)) throw new TypeError("Cannot write private member to an object whose class did not declare it");
    return (kind === "a" ? f.call(receiver, value) : f ? f.value = value : state.set(receiver, value)), value;
};
var _TestWSConnection_other, _TestWSConnection_onopen, _TestWSConnection_events;
Object.defineProperty(exports, "__esModule", { value: true });
exports.TestWSPair = exports.TestWSConnection = void 0;
const index_js_1 = require("./index.js");
class TestWSConnection {
    constructor(side, transfers) {
        _TestWSConnection_other.set(this, void 0);
        _TestWSConnection_onopen.set(this, void 0);
        _TestWSConnection_events.set(this, new Map());
        this.onmessage = (_e) => {
            throw new Error("OnMessage Method not implemented.");
        };
        this.onerror = (_e) => {
            throw new Error("OnError Method not implemented.");
        };
        this.onclose = (_e) => {
            throw new Error("OnClose Method not implemented.");
        };
        this.send = (data) => {
            const uint8 = (0, index_js_1.to_uint8)(data);
            this.transfers?.push({ from: this.side, data: uint8 });
            const msg = {
                data: uint8,
            };
            __classPrivateFieldGet(this, _TestWSConnection_other, "f").onmessage(msg);
            __classPrivateFieldGet(__classPrivateFieldGet(this, _TestWSConnection_other, "f"), _TestWSConnection_events, "f")
                .get("message")
                ?.forEach((listener) => (typeof listener === "function" ? listener(msg) : listener.handleEvent(msg)));
        };
        this.side = side;
        this.transfers = transfers;
    }
    get onopen() {
        return __classPrivateFieldGet(this, _TestWSConnection_onopen, "f");
    }
    set onopen(fn) {
        __classPrivateFieldSet(this, _TestWSConnection_onopen, fn, "f");
        fn();
    }
    removeEventListener(key, listener) {
        __classPrivateFieldGet(this, _TestWSConnection_events, "f").get(key)?.delete(listener);
    }
    addEventListener(key, listener) {
        __classPrivateFieldGet(this, _TestWSConnection_events, "f").set(key, (__classPrivateFieldGet(this, _TestWSConnection_events, "f").get(key) ?? new Set()).add(listener));
    }
    accept() {
    }
    connect(other) {
        __classPrivateFieldSet(this, _TestWSConnection_other, other, "f");
    }
}
exports.TestWSConnection = TestWSConnection;
_TestWSConnection_other = new WeakMap(), _TestWSConnection_onopen = new WeakMap(), _TestWSConnection_events = new WeakMap();
class TestWSPair {
    static create() {
        const pair = new TestWSPair();
        pair.p1.connect(pair.p2);
        pair.p2.connect(pair.p1);
        return pair;
    }
    constructor() {
        this.msgs = [];
        this.p1 = new TestWSConnection("p1", this.msgs);
        this.p2 = new TestWSConnection("p2", this.msgs);
    }
}
exports.TestWSPair = TestWSPair;
//# sourceMappingURL=test-ws-pair.js.map