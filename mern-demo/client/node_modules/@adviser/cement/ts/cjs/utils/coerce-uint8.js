"use strict";
Object.defineProperty(exports, "__esModule", { value: true });
exports.coerceIntoUint8 = coerceIntoUint8;
exports.asyncCoerceIntoUint8 = asyncCoerceIntoUint8;
const cement_1 = require("@adviser/cement");
const globalBuffer = globalThis;
function coerceIntoUint8(raw) {
    if (raw instanceof ArrayBuffer) {
        return cement_1.Result.Ok(new Uint8Array(raw));
    }
    if (ArrayBuffer.isView(raw)) {
        return cement_1.Result.Ok(new Uint8Array(raw.buffer, raw.byteOffset, raw.byteLength));
    }
    if (raw instanceof Blob) {
        return cement_1.Result.Err("Blob not supported");
    }
    if (globalBuffer.Buffer && globalBuffer.Buffer.isBuffer(raw)) {
        return cement_1.Result.Ok(new Uint8Array(raw.buffer, raw.byteOffset, raw.byteLength));
    }
    if (raw instanceof Uint8Array) {
        return cement_1.Result.Ok(raw);
    }
    if (cement_1.Result.Is(raw)) {
        if (raw.isErr()) {
            return cement_1.Result.Err(raw);
        }
        return coerceIntoUint8(raw.unwrap());
    }
    return cement_1.Result.Err("Not a Uint8Array");
}
async function asyncCoerceIntoUint8(raw) {
    let resolved = await raw;
    if (resolved instanceof Blob) {
        resolved = await resolved.arrayBuffer();
    }
    return coerceIntoUint8(resolved);
}
//# sourceMappingURL=coerce-uint8.js.map