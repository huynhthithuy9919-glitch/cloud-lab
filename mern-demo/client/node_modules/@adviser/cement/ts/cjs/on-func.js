"use strict";
Object.defineProperty(exports, "__esModule", { value: true });
exports.OnFuncReturn = void 0;
exports.OnFunc = OnFunc;
const is_promise_js_1 = require("./is-promise.js");
const result_js_1 = require("./result.js");
class OnFuncInstanceBase {
    constructor() {
        this._fns = new Set();
    }
    addFunction(fn) {
        this._fns.add(fn);
        return () => {
            this.unreg(exports.OnFuncReturn.UNREGISTER, fn);
        };
    }
    once(fn) {
        return this.addFunction((...a) => {
            (0, result_js_1.exception2Result)(() => fn(...a));
            return exports.OnFuncReturn.ONCE;
        });
    }
    unreg(ret, fn) {
        if (result_js_1.Result.Is(ret)) {
            if (ret.isErr()) {
                return undefined;
            }
            ret = ret.unwrap();
        }
        if (ret === exports.OnFuncReturn.UNREGISTER || ret === exports.OnFuncReturn.ONCE) {
            this._fns.delete(fn);
            return undefined;
        }
        return ret;
    }
    invoke(...a) {
        for (const fn of this._fns) {
            try {
                const couldByPlainOrPromise = (0, result_js_1.exception2Result)(() => fn(...a));
                if ((0, is_promise_js_1.isPromise)(couldByPlainOrPromise)) {
                    void couldByPlainOrPromise.then((ret) => {
                        this.unreg(ret, fn);
                    });
                }
                else {
                    this.unreg(couldByPlainOrPromise, fn);
                }
            }
            catch (e) {
            }
        }
    }
    clear() {
        this._fns.clear();
    }
    async invokeAsync(...a) {
        await Promise.allSettled(Array.from(this._fns).map((fn) => Promise.resolve(fn(...a)).then((ret) => this.unreg(ret, fn))));
    }
}
class OnFuncInstance extends OnFuncInstanceBase {
    constructor() {
        super(...arguments);
        this.onRegister = new OnFuncInstanceBase();
    }
    addFunction(fn) {
        this.onRegister.invoke(fn, Array.from(this._fns));
        return super.addFunction(fn);
    }
}
exports.OnFuncReturn = {
    UNREGISTER: Symbol("ONFUNC_UNREGISTER"),
    ONCE: Symbol("ONFUNC_ONCE"),
};
function OnFunc() {
    const instance = new OnFuncInstance();
    const ret = instance.addFunction.bind(instance);
    ret.invoke = instance.invoke.bind(instance);
    ret.invokeAsync = instance.invokeAsync.bind(instance);
    ret.clear = instance.clear.bind(instance);
    ret.once = instance.once.bind(instance);
    ret.onRegister = instance.onRegister.addFunction.bind(instance.onRegister);
    return ret;
}
//# sourceMappingURL=on-func.js.map