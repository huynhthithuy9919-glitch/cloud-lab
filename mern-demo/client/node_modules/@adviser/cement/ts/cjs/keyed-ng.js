"use strict";
var __classPrivateFieldSet = (this && this.__classPrivateFieldSet) || function (receiver, state, value, kind, f) {
    if (kind === "m") throw new TypeError("Private method is not writable");
    if (kind === "a" && !f) throw new TypeError("Private accessor was defined without a setter");
    if (typeof state === "function" ? receiver !== state || !f : !state.has(receiver)) throw new TypeError("Cannot write private member to an object whose class did not declare it");
    return (kind === "a" ? f.call(receiver, value) : f ? f.value = value : state.set(receiver, value)), value;
};
var __classPrivateFieldGet = (this && this.__classPrivateFieldGet) || function (receiver, state, kind, f) {
    if (kind === "a" && !f) throw new TypeError("Private accessor was defined without a getter");
    if (typeof state === "function" ? receiver !== state || !f : !state.has(receiver)) throw new TypeError("Cannot read private member from an object whose class did not declare it");
    return kind === "m" ? f : kind === "a" ? f.call(receiver) : f ? f.value : state.get(receiver);
};
var _KeyedNg_map;
Object.defineProperty(exports, "__esModule", { value: true });
exports.KeyedNg = void 0;
const lru_map_set_js_1 = require("./lru-map-set.js");
const sorted_object_js_1 = require("./utils/sorted-object.js");
class KeyedNg {
    constructor(opts) {
        _KeyedNg_map.set(this, void 0);
        this.opts = {
            ...opts,
            key2string: opts.key2string ??
                ((key) => {
                    if (typeof key === "string") {
                        return key;
                    }
                    if (typeof key === "number") {
                        return key.toString();
                    }
                    if (typeof key === "boolean") {
                        return key ? "true" : "false";
                    }
                    return JSON.stringify((0, sorted_object_js_1.toSortedObject)(key));
                }),
            ctx: opts.ctx ?? {},
            resetAfter: opts.resetAfter ?? 0,
        };
        __classPrivateFieldSet(this, _KeyedNg_map, new lru_map_set_js_1.LRUMap(opts.lru), "f");
    }
    onSet(fn) {
        return __classPrivateFieldGet(this, _KeyedNg_map, "f").onSet((_keyStr, item) => {
            fn(item);
        });
    }
    onDelete(fn) {
        return __classPrivateFieldGet(this, _KeyedNg_map, "f").onDelete((_keyStr, item) => {
            fn(item);
        });
    }
    setParam(params) {
        __classPrivateFieldGet(this, _KeyedNg_map, "f").setParam(params.lru);
    }
    asyncGet(key) {
        return key().then((k) => this.get(k));
    }
    has(keyOfFnKey) {
        if (typeof keyOfFnKey === "function") {
            keyOfFnKey = keyOfFnKey();
        }
        return __classPrivateFieldGet(this, _KeyedNg_map, "f").has(this.opts.key2string(keyOfFnKey));
    }
    delete(key) {
        __classPrivateFieldGet(this, _KeyedNg_map, "f").delete(this.opts.key2string(key));
    }
    keys() {
        return Array.from(__classPrivateFieldGet(this, _KeyedNg_map, "f").entries()).map(([_, item]) => item.givenKey);
    }
    forEach(fn) {
        return __classPrivateFieldGet(this, _KeyedNg_map, "f").forEach((item, _, ctx) => {
            fn(item, ctx.idx);
        });
    }
    entries() {
        let idx = 0;
        return Array.from(__classPrivateFieldGet(this, _KeyedNg_map, "f").entries()).map(([_, item]) => [item, idx++]);
    }
    getItem(key, ctx) {
        const keyStr = this.opts.key2string(key);
        let item = __classPrivateFieldGet(this, _KeyedNg_map, "f").get(keyStr);
        if (!item) {
            item = {
                refKey: keyStr,
                givenKey: key,
                ctx: ctx ?? this.opts.ctx,
                value: undefined,
            };
            item.value = this.opts.createValue(item);
            __classPrivateFieldGet(this, _KeyedNg_map, "f").set(keyStr, item);
        }
        return item;
    }
    get(key, ctx) {
        if (typeof key === "function") {
            key = key();
        }
        const item = this.getItem(key, ctx);
        return item.value;
    }
    values() {
        return Array.from(__classPrivateFieldGet(this, _KeyedNg_map, "f").entries()).map(([_, item]) => item);
    }
}
exports.KeyedNg = KeyedNg;
_KeyedNg_map = new WeakMap();
//# sourceMappingURL=keyed-ng.js.map