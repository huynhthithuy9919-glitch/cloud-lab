const possibleJSONre = new RegExp('^\\s*(?:\\{.*\\}|\\[.*\\]|"(?:[^"\\\\]|\\\\.)*"|true|false|null|-?\\d+(?:\\.\\d+)?(?:[eE][+-]?\\d+)?)\\s*$');
export function isJSON(str) {
    if (possibleJSONre.test(str)) {
        try {
            const parsed = JSON.parse(str);
            return { isJSON: true, parsed };
        }
        catch {
        }
    }
    return { isJSON: false };
}
//# sourceMappingURL=is-json.js.map