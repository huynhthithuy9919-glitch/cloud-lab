import { TxtEnDecoderSingleton } from "../txt-en-decoder.js";
export function string2stream(strOrArray, ende = TxtEnDecoderSingleton()) {
    const sa = Array.isArray(strOrArray) ? strOrArray : [strOrArray];
    return uint8array2stream(...sa.map((s) => ende.encode(s)));
}
export function uint8array2stream(...uint8) {
    return new ReadableStream({
        start(controller) {
            for (const chunk of uint8) {
                controller.enqueue(chunk);
            }
            controller.close();
        },
    });
}
//# sourceMappingURL=string2stream.js.map