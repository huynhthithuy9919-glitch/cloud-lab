import { toSorted } from "./to-sorted.js";
export function toSortedObjectArray(set, touchFn) {
    if (!set)
        return [];
    return toSortedArray(set, touchFn).map(([k, v]) => ({ [k]: v }));
}
export function toSortedArray(set, touchFn) {
    if (!set)
        return [];
    return Object.entries(toSorted(set, touchFn));
}
export function toSortedObject(set, touchFn) {
    if (!set)
        return set;
    return Object.fromEntries(toSortedArray(set, touchFn));
}
//# sourceMappingURL=sorted-object.js.map