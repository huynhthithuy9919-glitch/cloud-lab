import { LoggerImpl } from "@adviser/cement";
export default {
    fetch() {
        const logger = new LoggerImpl();
        for (let i = 0; i < 10; i++) {
            logger.Error().Any({ i }).Msg("Hello Logger World!");
        }
        const ret = new Response("Hello World!");
        return ret;
    },
};
//# sourceMappingURL=cf-test-main.js.map